# RUN-20260810-176 — RC10.2 Unified graphical operational dashboards

Status: `CI_VALIDATION_PENDING`

## Plan

Bounded objective: replace the RC10.1 graphical placeholder with real read-only operational telemetry inside `/ui/operations`, reusing the already accepted DTMO Prometheus instrumentation and preserving all existing RBAC, separation-of-duties, privacy, audit and human share-approval boundaries.

Prerequisite: PR #116 exact head `d41d9e60a4a67ddb30345eecee4042d1c19a6cf5` completed all registered workflows successfully and merged as `b000ef2275d52ff098d2d2bd8df76136cea3b051`.

Blocking gates: every registered GitHub workflow must succeed on one final exact PR head. Real staging, genuine VoiceOver/NVDA, independent penetration testing and remaining external assurance remain external blockers and are not inferred from repository CI.

## Do

- Added a read-only `/api/v1/operations/summary` endpoint backed directly by the in-process Prometheus registry.
- Aggregated only release-critical operational metrics already accepted by the existing operational-dashboard evidence: HTTP request volume/latency/in-flight, API/connector/storage/search alerts, queue backlog utilization, trace context and connector-run totals.
- Deliberately excluded request/response bodies, authorization material, cookies, query strings, student identifiers, object keys and other sensitive or unbounded dimensions.
- Replaced the RC10.1 synthetic ingestion placeholder with live accessible operational snapshot bars.
- Added live KPI cards for request count, average latency and active alerts.
- Added live alert-state, connector-state and traceability panels.
- Kept dashboard actions read-only; no connector run, admin configuration, review, share approval, audit mutation or token-revocation path was added.
- Added regression tests for endpoint shape, sensitive-dimension exclusion, real telemetry binding, placeholder removal, accessibility/responsive contracts and absence of privileged writes.

## Check

Repository inspection confirms the workspace now fetches `/api/v1/operations/summary`, `/health` and `/connectors` only. The new operational summary is an aggregate projection over existing Prometheus collectors and does not expose per-request labels or sensitive data. No CI PASS is claimed until the complete exact-head matrix succeeds.

## Act

Lesson: dashboard visualizations must represent real observability evidence or be explicitly absent; synthetic bars create an avoidable trust problem. Aggregating already accepted telemetry provides a safer UI boundary than exposing raw Prometheus label sets directly to browser clients.

Exactly one next priority after acceptance: RC10.3 Threat Intelligence Workspace — integrate search, CVE/KEV/vendor/provenance context into one analyst investigation flow without changing review/share-approval authority.
