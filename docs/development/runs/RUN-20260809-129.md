# RUN-20260809-129 — RC10.4 bounded storage-integrity alerting

Status: `PASS`

## Plan

Execute exactly one bounded Phase 7 objective after accepted RC10.1–RC10.3: observe the existing immutable raw-evidence size/SHA-256 verifier and emit critical, actionable, correlated storage-integrity alert evidence for controlled failure and recovery.

Phase 6 remains `BLOCKED_EXTERNAL` only for genuine VoiceOver/NVDA execution on supported real hosts. Issue #1 external production gates remain independently open.

## Do

Implemented on branch `rc10/rc10-4-storage-integrity-alerting`:

- `StorageIntegrityAlertManager` consuming only bounded storage name plus boolean integrity outcome;
- Prometheus integrity-check, active-alert and transition metrics;
- critical structured raise/active/clear events with correlation evidence and operator guidance;
- no raw object key, digest or payload content accepted by the alert observer;
- repeated-failure raise suppression and later-success clear behavior;
- critical Prometheus rule `DTMOStorageIntegrityFailure`;
- controlled tamper/recovery regression tests around the real `IntelligenceLake.verify()` size/SHA-256 verifier;
- explicit tests proving object key, expected digest, original payload and tampered payload do not appear in alert evidence;
- dedicated `RC10 Storage Integrity Alerting Gate` retaining exact-head JSON, JUnit and pytest evidence;
- RC10.4 QA contract.

## Check

PR #86 exact head `8aa56dacd64583de5e96c0fda188ba954437ffda` completed all 37 registered workflows successfully.

Dedicated workflow run `31325197952` completed both `storage-integrity-alerting` and fail-closed `storage-integrity-alerting-gate` jobs successfully. Retained artifact `9041327884`, digest `sha256:456b09902727552d62fa7e1c96f119c6050a692d2519e0f8cecdd160e8b1dab3`, was independently inspected and is exact-head bound.

Retained evidence confirms:

- decision `pass` on exact head `8aa56dacd64583de5e96c0fda188ba954437ffda`;
- existing `IntelligenceLake.verify()` reused as the size/SHA-256 integrity source;
- integrity failure signal, Prometheus metric/rule, safe correlation evidence and actionable operator guidance;
- successful re-verification clears the alert;
- repeated failure does not repeat the raise transition;
- object key, expected digest, original payload and tampered payload are excluded from alert evidence;
- observer does not mutate storage and publication approval remains unchanged;
- no production data used;
- JUnit/pytest: 5 tests, 0 failures, 0 errors, 0 skips.

PR #86 was squash-merged with expected-head protection as `4d7494e8b8fcdcddb73349bf87157d8c16763c33`.

## Act

RC10.4 is accepted as `PASS` for its bounded internal Phase-7 scope. Phase 7 remains `IN PROGRESS`.

A fresh threat-intelligence review identified a higher-severity blocker in the repository's pinned MinIO runtime (`RELEASE.2025-07-23T15-54-02Z`). That version falls within affected ranges for multiple later MinIO advisories, including the high-severity unauthenticated object-write vulnerability CVE-2026-41145. Exploitability of individual advisories depends on enabled features and network exposure, but the affected-version match itself is high confidence and requires remediation before additional Phase-7 feature work.

## Claim boundary

This run does not claim scheduled/fleet-wide production integrity scanning, external notification delivery, API-error/search-health alerting, dashboards, runbook exercise, on-call handover or Phase 7 completion. It also does not claim that every MinIO advisory is exploitable in DTMO's current deployment; only that the pinned version is within documented affected ranges and therefore fails the security-maintenance expectation until remediated and revalidated.

## Exactly one next priority

Remediate the newly discovered MinIO security blocker: move DTMO off `minio/minio:RELEASE.2025-07-23T15-54-02Z` to a supported/patched object-storage release or explicitly supported successor, then run the relevant security, recovery, storage-integrity and regression gates with retained exact-head evidence before resuming RC10.5 API-error alerting.
