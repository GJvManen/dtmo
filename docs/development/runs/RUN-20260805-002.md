# RUN-20260805-002 — Secured intelligence ingestion and search API

- **Started:** 2026-08-05 23:46 CEST
- **Workstream:** Backend, security, persistence, Intelligence Lake, search and documentation
- **Objective:** Connect PostgreSQL persistence, immutable raw-object storage and OpenSearch through authenticated and RBAC-protected API routes.

## Changes

- Added API-key authentication and auditable principal/role resolution in `backend/dtmo/auth/dependencies.py`.
- Added production API-key validation to `backend/dtmo/config.py`.
- Added validated request/response models in `backend/dtmo/api/schemas.py`.
- Added `POST /api/v1/intelligence` and `GET /api/v1/intelligence/search` in `backend/dtmo/api/routes.py`.
- Connected raw payload landing, SHA-256 receipts, normalized persistence, provenance and OpenSearch indexing.
- Registered the versioned API router and service shutdown in `backend/dtmo/main.py`.
- Added API authentication and OpenAPI contract tests.
- Extended production security tests for missing and short authentication keys.
- Updated `.env.example` with API authentication settings.
- Added `docs/api/INTELLIGENCE_API.md` and `docs/architecture/ADR-0001-SECURED-INTELLIGENCE-INGESTION.md`.

## Validation

Source files and tests were committed successfully. Static test intent covers valid and invalid API keys, role validation, OpenAPI route exposure and production configuration invariants.

## CI evidence

`PENDING`. GitHub Actions had not returned a successful status for the latest commit at the end of this run. Tests are therefore not claimed as passed.

## Known blockers

- Full integration still requires an observable GitHub Actions result and a live Compose environment.
- Search indexing currently uses synchronous request orchestration rather than a transactional outbox.
- Trusted role headers require an identity-aware reverse proxy in production.

## Next action

Add durable audit events and an outbox/retry worker for reliable OpenSearch indexing, then add integration tests against PostgreSQL, MinIO and OpenSearch services.

## Release gate

`PASS` for the committed bounded implementation and documentation. Overall RC4.8 remains `CI VALIDATION PENDING`.
