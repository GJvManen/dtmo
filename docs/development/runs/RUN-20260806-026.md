# RUN-20260806-026 — RC5.3 PyJWT type-contract remediation

- **Started:** 2026-08-06 CEST
- **Workstream:** Phase 2 — Application security and identity
- **Objective:** Resolve exactly the first deterministic failure from RC4 Quality Gate #185 without weakening authentication or static-analysis controls.

## Plan

Quality Gate #185 showed Ruff passing and strict MyPy failing only because `jwt.decode` expects PyJWT's typed `Options` contract rather than a generic `dict[str, Any]`. The blocking gate is a successful exact-head Quality Gate after the type-only correction.

## Do

- Imported `Options` from `jwt.types`.
- Typed the JWT validation options as `Options`.
- Preserved the complete required-claim list, HS256 restriction, issuer/audience validation, temporal validation and human/machine identity boundaries.
- Did not add a MyPy ignore, cast to `Any`, lint suppression or security exception.

Implementation commit: `694196b3fe1531f58f7b31cb01cdf7a67dde5ffd`.

## Check

Observed evidence from Quality Gate #185:

- dependency installation and `pip check`: `PASS`;
- Ruff: `PASS`;
- strict MyPy: `FAIL` with one error at `backend/dtmo/auth/tokens.py:54` because `dict[str, Any]` was not accepted as PyJWT `Options`;
- tests and compile after MyPy: not executed and therefore not `PASS`.

Replacement exact-head CI is required before acceptance.

## Act

Lesson: third-party libraries with typed configuration dictionaries must use the vendor's public typing contract; weakening strict typing would hide integration drift.

- **Run decision:** `BLOCKED` pending replacement exact-head CI.
- **Exactly one next priority:** Inspect the replacement RC4 Quality Gate for the new PR #11 head and either accept full success or resolve only its earliest deterministic failure.
