# RUN-20260806-042 — RC6.3 clean OpenSearch reconstruction

- **Started:** 2026-08-06 16:23 CEST
- **Completed:** 2026-08-06 16:38 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Reconstruct a clean OpenSearch index from canonical PostgreSQL intelligence while preserving immutable provenance references and proving deterministic source/target index equality.

## Plan

Blocking gates:

- PostgreSQL is the canonical normalized source;
- the OpenSearch target index must not exist before reconstruction;
- index mappings must be explicit and dynamically strict;
- indexed documents must retain canonical content hashes, governance state and provenance references;
- source and target manifests must have identical SHA-256 digests and counts;
- reconstruction duration and quiesced-source RPO basis must be recorded;
- machine-readable evidence must be retained;
- an independently observable fail-closed GitHub Actions gate must succeed on the exact branch head.

## Do

- added `tools/verify_opensearch_reconstruction.py`;
- added deterministic canonical projection from PostgreSQL;
- added a verified-clean target index and strict root mapping;
- added explicit strict provenance submapping;
- added reconstruction, refresh, sorted retrieval and exact manifest comparison;
- added provenance-reference and content-hash verification;
- added `.github/workflows/opensearch-recovery.yml` with isolated PostgreSQL and OpenSearch services;
- added retained `opensearch-reconstruction-evidence`;
- added a fail-closed recovery gate and workflow regression protection;
- remediated Recovery Gate #1 with `DISABLE_SECURITY_PLUGIN=true`;
- remediated Recovery Gate #3 by explicitly declaring all provenance child fields;
- preserved OpenSearch error response bodies for deterministic diagnosis;
- updated README, runlog, QA documentation, PR #25 and issues #2 and #3.

## Check

Exact head: `fbe3924d202d81ab59ebbcd10889a9a75b146941`.

- RC6 OpenSearch Recovery Gate #5, run `31111652425`: `SUCCESS`;
- `opensearch-reconstruction` job: `SUCCESS`;
- fail-closed `recovery-gate`: `SUCCESS`;
- evidence artifact `opensearch-reconstruction-evidence`: `8971961873`, digest `sha256:20407a47e2912b0106af4968638dfa83c5d7f1ad0d3dfa52956f75ab96087c1e`;
- RC4 Quality Gate #253, run `31111652503`: all release-critical jobs `SUCCESS`;
- aggregate release evidence: `8971960034`;
- PostgreSQL restore evidence: `8971952797`;
- MinIO restore evidence: `8971939713`;
- workflow contract evidence: `8971929978`;
- dependency audit evidence: `8971928709`.

PR #25 merged to `main` as `4b08640e612801898307b065f7f2413c34a090c2`.

## Act

Lessons:

- container entrypoint controls and OpenSearch settings are distinct contracts;
- strict mappings require every nested evidence field to be explicitly declared;
- search indexes are acceptable derived state only when clean reconstruction is deterministic, observable and integrity-checked;
- separate restore controls are not sufficient for Phase 3 closure without one combined cross-store recovery point.

- **Release gate:** `PASS`
- **Known blockers:** none for RC6.3; Phase 3 remains open pending combined multi-store recovery acceptance.
- **Exactly one next priority:** implement combined PostgreSQL, MinIO and OpenSearch recovery acceptance with one consistent recovery point, cross-store provenance verification and measured end-to-end RTO/RPO.
