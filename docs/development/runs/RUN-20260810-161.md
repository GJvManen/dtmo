# RUN-20260810-161 — 16.0.0rc5 frontend productionization

Decision: `CI_VALIDATION_PENDING`

## Plan

Bounded objective: turn the previously fragmented backend-rendered role pages into an immediately discoverable, governed DTMO operational console at the application root and make the documented Docker startup path reproducible for external validation.

Blocking gates: preserve RBAC, separation of duties, privacy, provenance, auditability and human share approval; do not treat browser visibility as authorization; do not weaken existing accessibility or external-assurance claim boundaries; require final exact-head CI before merge.

## Do

- Added `backend/dtmo/frontend.py` with a responsive DTMO Console at `/` and `/ui/console`.
- Added same-origin CSS/JS assets with strict CSP, visible focus, skip navigation, responsive reflow, reduced-motion handling and live status regions.
- Added API/runtime health, version/environment, connector status, intelligence search, governed review/share decisions, read-only audit evidence and CISO token revocation to one console.
- Added local/dev/staging test-identity support using per-tab `sessionStorage`; no bearer-token persistence is introduced.
- Kept all protected actions server-side RBAC controlled and retained the existing dedicated analyst/share/auditor/CISO pages.
- Bumped application/package version to `16.0.0rc5`.
- Updated Docker Compose to pass externally supplied `OPENSEARCH_INITIAL_ADMIN_PASSWORD` into OpenSearch.
- Updated `.env.example` with the required OpenSearch bootstrap input and explicit secret-handling warning.
- Added `backend/tests/test_frontend_console.py` and `docs/qa/FRONTEND_RELEASE_GATE.md`.
- Updated README and release documentation.

## Check

Code and documentation have been committed to branch `release/16.0.0rc5-frontend`. No PASS is claimed yet. The final exact-head workflow matrix must execute successfully. Genuine VoiceOver/NVDA, real staging deployment parity and Phase 9 external assurance remain separate external gates.

## Act

Lesson: the prior repository contained tested role-specific UI surfaces but no discoverable root application experience, which made successful Docker startup appear to have no user interface. A production candidate needs both functional role surfaces and an explicit deployable entry point.

## Exactly one next priority

Open the 16.0.0rc5 PR and verify every registered workflow on its final exact head. Merge only on complete success; otherwise remediate the first concrete failing gate.
