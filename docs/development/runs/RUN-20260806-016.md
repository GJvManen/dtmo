# RUN-20260806-016 — Repair CI packaging TOML parse failure

- **Started:** 2026-08-06 09:47 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Diagnose `RC4 Quality Gate` run `31082165346` and repair only its earliest shared deterministic repository failure.

## Plan

1. Inspect PR #7 at exact head SHA `cb98281f2416554137ae48373d7765be901bd9e6`.
2. Require an actual completed GitHub Actions run before drawing a CI conclusion.
3. Inspect job steps and logs to identify the earliest shared deterministic failure.
4. Apply one minimal packaging fix without changing application behaviour, RBAC, privacy, provenance or human share approval.
5. Keep Phase 1 blocked until the replacement head has completed successfully and required artifacts are verified.

## Do

- Confirmed `RC4 Quality Gate` run `31082165346` completed with conclusion `failure`.
- Confirmed `migrations`, `test` and `workflow-contracts` all failed during dependency installation before their substantive checks ran.
- Retrieved the `migrations` job log and identified `tomllib.TOMLDecodeError: Unescaped '\\' in a string (at line 55, column 6)` while parsing `pyproject.toml`.
- Replaced the coverage exclusion regex basic string with a TOML literal string, preserving the intended regex while preventing backslash escape interpretation.
- Fix commit: `8087033ba2ec1864b41e7fdd30fb7357344a5416`.

## Check

- Actual failed execution evidence exists for run `31082165346`; it is not accepted as release evidence.
- The failure occurred during `pip install -e '.[test]'`, so lint, type checks, tests and migrations were skipped and remain unexecuted for that run.
- The committed fix is syntactically targeted, but no completed replacement workflow run for commit `8087033ba2ec1864b41e7fdd30fb7357344a5416` had been verified when this record was written.
- Dependency-review also failed in the same run, but it is not addressed in this bounded objective because the packaging parse failure is the earliest shared blocker.

## Act

- **Run decision:** `BLOCKED` pending actual replacement CI execution.
- **Phase 1:** `BLOCKED`.
- **Release:** `CI VALIDATION PENDING`.
- **Lesson:** A prior fix cannot be treated as present on a fresh branch unless the exact branch content is inspected; CI diagnosis must bind evidence to the exact PR head and merge commit.
- **Exactly one next priority:** inspect the `RC4 Quality Gate` run for commit `8087033ba2ec1864b41e7fdd30fb7357344a5416` and resolve only its earliest remaining deterministic failure, or verify required artifacts if it succeeds.
