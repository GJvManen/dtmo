# RUN-20260806-012 — GitHub Actions execution canary

- **Started:** 2026-08-06 09:52 CEST
- **Completed:** 2026-08-06 10:00 CEST
- **Workstream:** Release engineering and QA
- **Objective:** Determine whether an independently minimal GitHub Actions workflow executes on `main`, separating repository-level Actions execution from defects in the RC4 quality-gate workflow.

## Plan

1. Inspect issue #2, recent commits, the RC4 workflow, commit status and QA documentation.
2. Add a minimal push/manual-dispatch canary with no project dependencies.
3. Inspect the resulting commit for observable CI status.
4. Record the outcome fail-closed.

## Changes

- Added `.github/workflows/actions-canary.yml`.
- Canary commit: `86c18aaa0fcd623d099c464f3bc4669392cc059b`.
- The canary contains one Ubuntu job and one shell step that prints immutable GitHub run identity (`repository`, `sha`, `run_id`, `event`).
- The workflow is triggered by its own addition to `main` and can also be started with `workflow_dispatch`.

## Verification evidence

| Check | Expected | Actual | Result |
|---|---|---|---|
| Existing RC4 workflow trigger contract | `push`, `pull_request`, `workflow_dispatch` present | Present in `.github/workflows/ci.yml` | PASS |
| Canary workflow committed to `main` | New commit created | `86c18aaa0fcd623d099c464f3bc4669392cc059b` | PASS |
| Canary commit exposes a CI status/check | At least one observable context | Combined status response contained zero statuses | FAIL |
| Project-independent execution isolation | Canary has no checkout, install, database, Docker or secret dependency | Confirmed by workflow content | PASS |

## Diagnosis

The missing execution evidence is not explained by Python dependencies, tests, migrations, Docker, PostgreSQL, dependency review or the aggregate release-gate logic: the independent canary does not depend on any of those components and still exposes no status context after its push to `main`.

The remaining highest-probability blocker is outside repository workflow code: GitHub Actions may be disabled or restricted in repository/account settings, or the connected GitHub integration may be unable to observe check-run data. The available integration exposes repository administration and file writes but does not expose the Actions policy/settings endpoint or a generic push-run listing endpoint, so this run cannot distinguish those two external causes or change the setting safely.

## Blocker

`CI-OPS-001`: No observable GitHub Actions execution exists for a minimal, dependency-free workflow committed directly to `main`. Repository/account Actions policy and check-run visibility require verification in GitHub settings or through an integration with Actions administration and check-run read access.

## Next action

Open **Repository Settings → Actions → General**, enable Actions for the repository (allow required GitHub-authored actions), save the setting, manually dispatch **GitHub Actions Canary**, and capture its run URL and conclusion. Then rerun the RC4 Quality Gate and attach both run identities to the QA report.

## Release gate

`BLOCKED`

RC4.8 remains `CI VALIDATION PENDING`. No test or workflow is represented as passing without observable execution evidence.
