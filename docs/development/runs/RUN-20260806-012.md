# RUN-20260806-012 — Repair CI package metadata parsing

- **Started:** 2026-08-06 08:41 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Repair the first independently observed CI failure by making `pyproject.toml` valid TOML.
- **Blocking gates:** A successful workflow run and artifacts are still required; fixing configuration alone is not a pass.

## Plan

1. Inspect the pull-request workflow execution and failed jobs.
2. Identify the earliest common deterministic failure.
3. Apply one minimal correction and let the pull-request trigger produce fresh evidence.

## Do

- Observed `RC4 Quality Gate` run `31075045431` for PR #5; conclusion `failure`.
- Inspected job `workflow-contracts` (`92531117310`).
- Confirmed `pip install -e '.[test]'` failed before dependency resolution with `tomllib.TOMLDecodeError: Unescaped '\\' in a string (line 55, column 6)`.
- Replaced the affected coverage exclusion string with a TOML literal string, preserving the intended regular expression while removing invalid TOML escaping.
- Commit: `72170c9b1b51ab04c0e43d08b48b2bfb38bcf219`.

## Check

- **Actual prior evidence:** workflow execution exists and failed; no absent execution is treated as pass.
- **Root cause evidence:** GitHub job logs identify invalid TOML at `pyproject.toml` line 55.
- **Post-change execution:** `PENDING` until GitHub completes the new PR-triggered run.
- **Security/governance impact:** packaging metadata only; RBAC, separation of duties, privacy controls, provenance and human share approval are unchanged.

## Act

The earlier assumption that Actions was disabled is disproven. The active blocker is now an executable repository defect. Phase 1 remains blocked until the new run is inspected.

- **Run decision:** `BLOCKED`
- **Phase 1:** `BLOCKED`
- **Release:** `CI VALIDATION PENDING`
- **Exactly one next priority:** Inspect the fresh `RC4 Quality Gate` run for commit `72170c9b1b51ab04c0e43d08b48b2bfb38bcf219` and resolve the earliest remaining deterministic failure.
