# RUN-20260809-100 — RC9.4 bounded auditor read-only browser journey

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective in roadmap order: add one critical auditor browser journey for read-only persistent audit evidence. Blocking gates are exact-head GitHub Actions, retained browser evidence and continued enforcement of backend `read:audit` RBAC. Responsive, keyboard, cross-browser and broad WCAG validation remain out of scope.

## Do

- Added `/ui/auditor` and `/ui/auditor.js` as a read-only audit evidence browser surface.
- Added `/api/v1/audit/events`, independently protected by backend `Permission.READ_AUDIT`.
- Added Chromium E2E `backend/tests/test_rc9_4_browser_auditor_readonly.py`.
- Added dedicated `RC9 Auditor Read-only Browser E2E Gate` with PostgreSQL, migrations, retained JUnit/server/JSON artifacts and a fail-closed terminal gate.
- The E2E seeds one synthetic persistent audit event, verifies an analyst cannot see the audit panel and receives HTTP 403 on a direct API request, verifies the auditor can render the persisted event, verifies the persistent audit chain and proves browser reads do not alter audit row count.
- Added `docs/qa/RC9_4_AUDITOR_READONLY_BROWSER_GATE.md`.

## Check

No PASS is claimed in this run. The new branch changes the exact head and therefore requires a fresh complete workflow set. Retained evidence does not exist until GitHub Actions executes the dedicated browser workflow. Missing, queued or unexecuted CI is not evidence.

Security/governance check: the existing role matrix already grants `read:audit` to `auditor`; RC9.4 does not expand permissions. UI visibility derives from backend session permissions, while the audit API independently enforces the same permission. The journey is read-only and uses synthetic fixture data. No production dependency or external provider is added.

Threat/CVE/vendor review: no production dependency or vendor integration changed, so no new advisory materially changes this bounded gate. Existing security/dependency workflows remain authoritative and may block acceptance.

## Act

Lesson: auditor UX must not rely on hidden controls as authorization. The same `read:audit` permission is enforced independently at the API boundary, and read-only behavior is evidenced by a stable audit-row count plus chain verification.

Phase 6 remains `IN PROGRESS`. External gates in issue #1 remain open.

## Exactly one next priority

Inspect every required workflow on the final RC9.4 PR head and independently inspect retained `browser-auditor-readonly-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful exact-head evidence.
