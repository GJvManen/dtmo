# RUN-20260807-051 — RC7.3 exact-head acceptance and merge

## Plan

Objective: complete exactly one bounded Phase 4 acceptance decision for RC7.3 by verifying the current pull-request head, independently observing every required exact-head workflow, validating retained connector-contract evidence, and merging only if no release-critical gate is absent, queued, cancelled or failed. Blocking gates: RC4 Quality Gate, RC6 OpenSearch Recovery Gate, RC6 Multi-store Recovery Gate, RC7 Live Connector Canary Gate, RC7 Connector State Gate, RC7 Connector Contract Gate, and retained connector-contract evidence.

## Do

Re-read PR #30 immediately before acceptance and confirmed it remained open, mergeable and unchanged at exact head `54ad16cd6694ba17aa2ab28d56c9beaeaf7b789f`.

Observed all required exact-head GitHub Actions executions to completion. Retrieved retained artifact `connector-contract-evidence` from Connector Contract Gate run `31145386898`, artifact ID `8981283640`, digest `sha256:e5be71c0a30ebe4249877a3a990abc8726375293dd0c20b43411ea2e0af13733`, not expired. Downloaded evidence records schema version 1, connector `cisa-kev-canary`, deterministic contract digest `b55692915fe4ad450355c48139371240c330bc997411f0f5ab39df86c56b09ef`, `credentials_present=true`, `decision=pass`, no duplicate connector IDs, and `publish_approved=false` at connector and aggregate levels.

After the exact head and evidence were re-confirmed, merged PR #30 using expected-head protection. Squash merge commit: `0e1d4d370aff0a1e340a10fe1fd373d282864abc`.

No RBAC, separation-of-duties, privacy, provenance, quarantine, auditability or human share-approval control was weakened in this acceptance run.

## Check

Exact head `54ad16cd6694ba17aa2ab28d56c9beaeaf7b789f` produced complete release evidence:

- RC4 Quality Gate #301, run `31145386886`: `PASS`;
- RC7 Live Connector Canary Gate #34, run `31145386892`: `PASS`;
- RC7 Connector Contract Gate #5, run `31145386898`: `PASS`;
- RC7 Connector State Gate #22, run `31145386874`: `PASS`;
- RC6 Multi-store Recovery Gate #43, run `31145386891`: `PASS`;
- RC6 OpenSearch Recovery Gate #53, run `31145386961`: `PASS`;
- retained artifact `connector-contract-evidence`, ID `8981283640`: present, not expired;
- artifact digest: `sha256:e5be71c0a30ebe4249877a3a990abc8726375293dd0c20b43411ea2e0af13733`;
- artifact decision: `pass`;
- duplicate connector IDs: none;
- publication approval: `false`.

Because all required exact-head gates executed successfully and retained evidence was independently observable, RC7.3 is accepted as `PASS`.

External issue #1 remains open for real production validation of connector credentials, provider rate limits, licences and terms. Repository contract validation does not replace that external gate.

## Act

Lesson: acceptance must be bound to the immutable PR head and retained evidence, not merely to prior green runs. Expected-head merge protection prevents accepting a moved branch after verification.

Phase 4 remains in progress. Governed connector contracts are now merged, but the next roadmap control must continue strengthening live connector reliability and provenance without bypassing external production approvals.

## Decision

`PASS`

## Exactly one next priority

Implement and evidence connector payload provenance and normalization enforcement at the ingestion boundary: every candidate record must retain immutable source URI, source timestamp where supplied, fetch timestamp, connector/run identity, payload digest and confidence; malformed or duplicate records must fail closed to quarantine, and successful ingestion must never imply publication approval.
