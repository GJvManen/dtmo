# RUN-20260806-023 — Restore clean RC5.1 delivery path

- **Started:** 2026-08-06 12:56 CEST
- **Workstream:** RC5.1 data integrity and release engineering
- **Objective:** Remove obsolete pull-request delivery paths and repair the single deterministic test defect blocking the current RC5.1 pull request.

## Plan

- Treat PR #9 as the sole active RC5.1 delivery line.
- Close superseded, non-mergeable CI probe PRs #5, #6 and #8 without merging them.
- Repair the failing migration contract test identified by `RC4 Quality Gate` run `31094445592` (#173).
- Preserve migration behavior, RBAC, separation of duties, privacy controls, provenance and human share approval.
- Do not claim CI success until a replacement exact-head workflow completes.

## Do

- Closed PRs #5, #6 and #8 as superseded.
- Updated `backend/tests/test_rc5_canonical_migration.py` so `ast.AnnAssign` is read through `node.target`, matching Python's AST contract.
- Fix commit: `8246c6bd8202e5814cff4197e8a503ac36a9b74b`.

## Check

Actual evidence from run `31094445592`:

- 48 tests passed before the first failure;
- lint passed;
- strict MyPy passed;
- dependency audit passed;
- container build and smoke test passed;
- migration upgrade, revision validation, downgrade and re-upgrade passed;
- workflow-contract validation and evidence upload passed;
- release gate failed because `test_canonical_migration_has_linear_reversible_revision_chain` raised `AttributeError: 'AnnAssign' object has no attribute 'targets'`.

The code correction is committed, but replacement CI for the new exact head has not yet completed. The test is therefore `PENDING`, not `PASS`.

## Act

Lesson: migration contract tests must model the exact Python AST node API; a successful runtime migration does not excuse a broken regression test. Superseded PRs should be closed promptly so only one authoritative delivery line remains.

- **Run decision:** `BLOCKED`
- **RC5.1 status:** implementation repaired; exact-head CI validation pending
- **Exactly one next priority:** inspect the replacement `RC4 Quality Gate` run for commit `8246c6bd8202e5814cff4197e8a503ac36a9b74b`; if successful, verify the corresponding observer evidence, otherwise address only the earliest deterministic failure.
