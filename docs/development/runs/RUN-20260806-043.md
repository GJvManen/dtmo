# RUN-20260806-043 — RC6.4 combined multi-store recovery acceptance

- **Started:** 2026-08-06 16:43 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove one bounded combined recovery acceptance control for PostgreSQL, MinIO and OpenSearch using one declared recovery point, cross-store provenance controls and measured end-to-end RTO/RPO.

## Plan

Blocking gates:

- one immutable recovery-point identifier must bind all three store recoveries;
- PostgreSQL must restore to a clean database with audit-chain and provenance integrity intact;
- MinIO must restore to a clean target with exact object digests and provenance references intact;
- OpenSearch must reconstruct from canonical PostgreSQL data into a non-existing index with exact source/target manifest equality;
- every store must report a bounded RPO;
- component recovery timings must produce a measured end-to-end RTO;
- combined machine-readable evidence must bind all component manifests cryptographically;
- missing or failed component evidence must fail closed;
- exact-head GitHub Actions and retained artifacts are required before `PASS`.

## Do

- added `tools/verify_multistore_recovery.py`;
- added one recovery-point binding using `commit:${GITHUB_SHA}`;
- added strict validation of component `decision`, clean-target, provenance, manifest, audit-chain and RPO controls;
- added deterministic SHA-256 binding across PostgreSQL manifest/audit/provenance, MinIO backup/object manifest and OpenSearch manifest;
- added measured aggregate RTO and effective maximum RPO;
- added `.github/workflows/multistore-recovery.yml` with isolated PostgreSQL, MinIO and OpenSearch recovery execution;
- added retained `multistore-recovery-evidence` containing component and aggregate evidence;
- added an independently observable fail-closed `recovery-acceptance-gate`;
- added regression tests for successful binding, missing provenance controls and unbounded RPO;
- updated README, run log, QA documentation and issues #1, #2 and #3.

## Check

Implementation and regression protection are committed. No exact-head workflow execution or retained artifact has completed yet. This run is therefore not accepted as a recovery pass.

## Act

Lesson: separate store-level recovery passes are necessary but insufficient. Production recovery acceptance must bind all derived and canonical stores to one declared recovery point and preserve a cryptographically reproducible evidence relationship across stores.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** exact-head multi-store recovery and Quality Gate evidence have not completed successfully.
- **Exactly one next priority:** inspect the exact-head RC6 Multi-Store Recovery Gate and remediate only its earliest deterministic failure, or merge after all required gates and retained artifacts succeed.
