# RUN-20260806-043 — RC6.4 combined multi-store recovery acceptance

- **Started:** 2026-08-06 16:42 CEST
- **Completed:** 2026-08-06 17:02 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove PostgreSQL, MinIO and OpenSearch recovery in one bounded acceptance run with one recovery-point identity, fail-closed cross-store integrity checks and measured end-to-end RTO/RPO.

## Plan

Blocking gates:

- one recovery-point identifier and start timestamp must bind all subsystem recovery actions;
- PostgreSQL restore must preserve canonical data, provenance hashes and the append-only audit chain;
- MinIO restore must preserve object digests and provenance references;
- OpenSearch must be reconstructed from canonical PostgreSQL data with deterministic source/target manifests;
- all subsystem evidence must report `pass` before combined acceptance can pass;
- the combined evidence must hash and bind all retained subsystem evidence;
- end-to-end RTO and zero-second quiesced-run RPO basis must be recorded;
- a separate `always()` recovery gate must fail closed;
- exact-head GitHub Actions evidence is mandatory.

## Do

- added `tools/verify_multistore_recovery.py`;
- added `.github/workflows/multistore-recovery.yml` with PostgreSQL 17, OpenSearch 2.19.1 and two isolated MinIO instances;
- established one recovery-point identity before any subsystem recovery starts;
- reused the evidenced PostgreSQL, MinIO and OpenSearch recovery verifiers in one sequential acceptance job;
- added combined evidence binding subsystem decisions, evidence-file SHA-256 digests, provenance checks and end-to-end timing;
- added fail-closed workflow regression protection in `backend/tests/test_rc6_multistore_recovery_workflow.py`;
- remediated headless PGDG key import with `set -euo pipefail` and `gpg --batch --yes --dearmor`;
- updated README, runlog, QA documentation and issues #2/#3.

## Check

Exact head `ba3389613341c84aa21b591b706b7819981b7a4b` was independently verified:

- RC6 Multi-store Recovery Gate #4, run `31113680268`: `SUCCESS`;
- `multistore-recovery` job: `SUCCESS`;
- fail-closed `recovery-gate` job: `SUCCESS`;
- RC4 Quality Gate #262, run `31113681659`: `SUCCESS`;
- RC6 OpenSearch Recovery Gate #14, run `31113680720`: `SUCCESS`;
- retained `multistore-recovery-evidence` artifact `8972811292`, digest `sha256:7739c8667acb87ab7d5377c0473586fce5959a3424432e0a78f04cf3ecd70502`;
- retained aggregate release, PostgreSQL, MinIO, workflow-contract and dependency-audit artifacts;
- PR #26 merged as `d25c2e4a9c5e5869071020a109ddf57638779a02`.

All blocking gates passed. Phase 3 recovery acceptance is evidenced.

## Act

Lessons:

- individually green restore jobs are insufficient; coordinated recovery needs one recovery-point identity and cross-store evidence binding;
- cryptographic package-key import in CI must be explicitly non-interactive;
- artifact absence correctly blocks acceptance even when unrelated quality gates are green.

- **Release gate:** `PASS`
- **Phase 3:** `PASS`
- **Exactly one next priority:** start Phase 4 with one controlled live connector canary including source provenance, timeout, rate limiting, retry/backoff, quarantine and fail-closed human share approval.
