# RUN-20260806-043 — RC6.4 combined multi-store recovery acceptance

- **Started:** 2026-08-06 16:42 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove PostgreSQL, MinIO and OpenSearch recovery in one bounded acceptance run with one recovery-point identity, fail-closed cross-store integrity checks and measured end-to-end RTO/RPO.

## Plan

Blocking gates:

- one recovery-point identifier and start timestamp must bind all subsystem recovery actions;
- PostgreSQL restore must preserve canonical data, provenance hashes and the append-only audit chain;
- MinIO restore must preserve object digests and provenance references;
- OpenSearch must be reconstructed from canonical PostgreSQL data with deterministic source/target manifests;
- all subsystem evidence must report `pass` before combined acceptance can pass;
- the combined evidence must hash and bind all retained subsystem evidence;
- end-to-end RTO and zero-second quiesced-run RPO basis must be recorded;
- a separate `always()` recovery gate must fail closed;
- exact-head GitHub Actions evidence is mandatory.

## Do

- added `tools/verify_multistore_recovery.py`;
- added `.github/workflows/multistore-recovery.yml` with PostgreSQL 17, OpenSearch 2.19.1 and two isolated MinIO instances;
- established one recovery-point identity before any subsystem recovery starts;
- reused the evidenced PostgreSQL, MinIO and OpenSearch recovery verifiers in a single sequential acceptance job;
- added combined evidence binding subsystem decisions, evidence-file SHA-256 digests, provenance checks and end-to-end timing;
- added fail-closed workflow regression protection in `backend/tests/test_rc6_multistore_recovery_workflow.py`;
- updated README, runlog, QA documentation and issues #2/#3 in the same branch;
- remediated the first observable Gate #1 failure by making the PGDG signing-key import non-interactive with `gpg --batch --yes --dearmor` and enabling `set -euo pipefail`.

## Check

RC6 Multi-store Recovery Gate run `31112695957` failed before recovery execution. Project installation completed, but the PGDG signing-key import attempted to open `/dev/tty`; `gpg` exited with code 2 and the downstream artifact upload failed closed because no evidence existed. RC4 Quality Gate #259 and OpenSearch Recovery Gate #11 passed on the prior exact head, but these do not substitute for combined recovery evidence.

A new exact-head execution is required for commit `fa5b6313baf3275892df2352f55aeca34a0c7778`. No combined recovery result is accepted as `PASS` until the multi-store recovery job, retained evidence artifact, fail-closed recovery gate and regular Quality Gate all succeed on that head.

## Act

Lesson: CI cryptographic key imports must be explicitly non-interactive; otherwise a valid package source can still fail on headless runners before recovery begins. Artifact absence correctly remained release-blocking.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** the remediated exact-head combined recovery gate has not yet completed successfully.
- **Exactly one next priority:** inspect the new exact-head RC6 Multi-store Recovery Gate and remediate only its earliest deterministic failure, or merge after complete success.
