# RUN-20260806-003 — CI workflow contract guard

- **Started:** 2026-08-06 00:31 CEST
- **Workstream:** QA, release engineering and CI observability
- **Objective:** Protect the release-blocking GitHub Actions workflow against accidental removal of mandatory jobs, triggers and commands.

## Plan

The latest commit exposed no GitHub Actions statuses and no workflow runs. Because the release remains `CI VALIDATION PENDING`, this run adds a repository-level regression test that verifies the committed CI workflow still contains the mandatory release gates.

### QA plan

| Gate | Method | Blocking |
|---|---|---:|
| Implementation | Commit a regression test for `.github/workflows/ci.yml` | Yes |
| CI contract | Assert required triggers, jobs and commands remain present | Yes |
| Security | Preserve dependency review with high-severity blocking | Yes |
| Release evidence | Inspect latest commit status and workflow-run evidence | Yes |
| Documentation | Record the run, QA outcome, blockers and next action | Yes |

## Do

Added `backend/tests/test_ci_workflow_contract.py`.

The test protects:

- push, pull-request and manual triggers;
- test, migration, container and dependency-review jobs;
- Ruff, strict MyPy and coverage commands;
- Alembic downgrade/re-upgrade validation;
- container build and `/health` smoke test;
- high-severity dependency blocking.

Implementation commit: `d6909ba033e056022634b0fddd60f24719ddde29`.

## Check

| Gate | Result | Evidence |
|---|---|---|
| File committed | PASS | GitHub returned commit `d6909ba033e056022634b0fddd60f24719ddde29` |
| CI contract definition | PASS | Regression test contains explicit mandatory workflow fragments |
| Automated execution | PENDING | No GitHub Actions status or workflow run was exposed for the latest inspected commit |
| Security gate preserved | PASS | Test requires dependency review and `fail-on-severity: high` |
| Overall RC4.8 release | BLOCKED | Successful CI evidence is still absent |

No claim is made that the new test has executed successfully. The bounded implementation is committed, but the run cannot receive `PASS` because its blocking automated-execution gate has no observable evidence.

## Act

- **Lesson:** A workflow file being present is not enough; its release-critical structure must also be regression protected and its execution must remain independently observable.
- **Unresolved blocker:** GitHub Actions exposes no successful status checks or workflow runs for the inspected latest commit.
- **Next action:** diagnose why GitHub Actions runs are absent and restore an observable workflow execution on `main`.
- **Run gate:** `BLOCKED`.
- **Overall release:** `CI VALIDATION PENDING`.
