# RUN-20260807-046 — Exact-head CI execution blocker

- **Started:** 2026-08-07 00:43 CEST
- **Workstream:** Phase 4 — live connector reliability and provenance
- **Objective:** Verify independently observable exact-head execution for RC7.2 and either accept complete evidence or record the precise external blocker.

## Plan

Blocking gates:

- the current PR head must have an observable RC7 Connector State Gate run;
- the regular Quality Gate and existing recovery/canary regression gates must execute on the same head;
- required tests, migrations and retained evidence must actually run;
- absent runs, queued work or committed tests are never `PASS`.

## Do

- inspected PR #29 and confirmed it is open, mergeable and based on current `main`;
- verified `.github/workflows/connector-state.yml` includes both `pull_request` and `workflow_dispatch` triggers without path exclusions;
- queried GitHub Actions for exact head `d489a60bcc2492b0a7dc0423bdf91c32211a07ed` and its generated merge commit;
- made no product, RBAC, privacy, provenance, audit or publication-control changes because there is no new deterministic product failure to remediate.

## Check

GitHub returned no workflow runs for exact head `d489a60bcc2492b0a7dc0423bdf91c32211a07ed`. Therefore:

- no Connector State Gate execution exists for this head;
- no PostgreSQL migration/test evidence exists for this head;
- no retained `connector-state-evidence` artifact exists for this head;
- the Quality, canary and recovery regressions are also not evidenced on this head.

The workflow definition itself is present and correctly subscribes to pull-request events. The missing evidence is an external GitHub Actions execution/registration blocker, not a successful test result.

## Act

Lesson: a valid workflow definition and a mergeable PR do not constitute execution evidence. Release acceptance remains fail-closed until GitHub registers and executes the required workflows on the exact head.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Precise blocker:** GitHub Actions has not registered any workflow run for the exact RC7.2 head despite the applicable pull-request trigger.
- **Exactly one next priority:** inspect the first RC7 Connector State Gate that GitHub registers for the current exact head and remediate only its earliest deterministic failure, or merge after all required gates and retained artifacts succeed.
