# RUN-20260807-050 — RC7.3 quality-gate lint remediation

## Plan

Objective: remediate exactly the first deterministic failure observed in RC4 Quality Gate #297 for the RC7.3 governed connector-contract change set. Blocking gates: RC4 Quality Gate must execute on the new exact head; existing RC6/RC7 gates must remain non-regressed; retained connector-contract evidence remains required before acceptance or merge.

## Do

Inspected exact-head workflow run `31142338808` for head `b333d013d2b183d2678a7aaec04010bc6b1549d1`. All release-critical jobs except the `test` job succeeded. The first deterministic failure occurred in Ruff rule `S105` at `backend/tests/test_rc7_connector_contracts.py:31`, where a deliberately synthetic redaction-test fixture was named `secret` and assigned a hardcoded string.

Remediation commit `4067119119fa85acc4bdb79dbe1082a20bcd6991` renames the fixture to `test_token`, makes its non-sensitive test purpose explicit, and narrowly suppresses `S105` on that fixture line only. The credential redaction assertion remains unchanged in behavior: the environment variable name may appear in evidence, while the fixture value must not.

No product authentication, RBAC, separation-of-duties, privacy, provenance, quarantine, auditability, or human share-approval behavior changed.

## Check

Observed evidence on the pre-remediation exact head:

- RC7 Connector Contract Gate #1: `PASS`;
- RC7 Live Connector Canary Gate #30: `PASS`;
- RC7 Connector State Gate #18: `PASS`;
- RC6 OpenSearch Recovery Gate #49: `PASS`;
- RC6 Multi-store Recovery Gate #39: `PASS`;
- RC4 Quality Gate #297: `FAIL`;
- first deterministic failure: Ruff `S105 Possible hardcoded password assigned to: "secret"`;
- type check, tests and compile in that job were skipped after lint failed.

The remediation is committed, but new exact-head CI execution is not yet accepted in this run. Therefore no PASS is claimed for RC7.3 after the remediation.

## Act

Lesson: security-oriented static-analysis rules can correctly flag synthetic credential fixtures; tests that validate secret nondisclosure must identify fixtures as non-production data without weakening repository-wide lint rules.

Backlog remains in roadmap Phase 4. External production connector credentials, provider rate limits, licences and terms remain gated by issue #1.

## Decision

`CI_VALIDATION_PENDING`

## Exactly one next priority

Inspect the first RC4 Quality Gate registered for exact head `4067119119fa85acc4bdb79dbe1082a20bcd6991`; remediate only its earliest deterministic failure, or accept and merge PR #30 only when all required exact-head gates and retained evidence succeed.
