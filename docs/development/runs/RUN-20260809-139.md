# RUN-20260809-139 — bounded operational dashboard provisioning

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 7 objective after accepted RC10.7: provision a reviewable, read-only operational dashboard over DTMO's existing bounded Prometheus metrics without introducing production credentials, anonymous access, publication authority or unbounded request data.

Blocking gates:

- RUN-137/138 and PR #94 must be accepted on exact-head CI plus retained artifact evidence;
- dashboard queries may use only already-bounded operational metrics and labels;
- Grafana provisioning must be read-only and contain no credentials;
- local Grafana access must disable anonymous/self-signup behavior;
- no floating or assumed-safe Grafana image may be introduced; image selection must fail closed to an externally supplied supported security-patched digest;
- dedicated retained dashboard evidence and the complete workflow matrix must succeed on one exact final head before acceptance.

## Do

Verified PR #94 exact head `5a2f60749f6eaf6ece9dcfcc3b70c866887c6cb8` completed 41/41 workflows successfully. Artifact `9042398103`, digest `sha256:2014a035338de6bc6ac474581279c06c15cafc6a49f3c86cfbeed111e666575a`, was exact-head bound and independently showed JUnit 10/10 with zero failures/errors/skips plus an internally consistent PASS record. PR #94 merged as `e52af08204d212cdfba0e9338bacb7a1c5fcfac7`.

Implemented RC10.8:

- added an opt-in `docker-compose.observability.yml` overlay rather than changing the default application runtime;
- Grafana image selection fails closed unless `GRAFANA_IMAGE` is supplied externally as a supported security-patched tag plus vendor-verified sha256 digest;
- Grafana administrative identity/password must be supplied outside source control;
- anonymous access, user sign-up and organization creation are disabled;
- local port exposure is loopback-only and `no-new-privileges` is enabled;
- provisioned a non-editable Prometheus datasource and non-editable dashboard provider;
- provisioned `DTMO Operations` with request rate, p95 latency, in-flight requests, API alerts, connector alerts/outcomes, queue utilization, storage-integrity alerts, search-health alerts and bounded trace-context decision telemetry;
- dashboard queries deliberately exclude raw request bodies, response bodies, raw URLs/query strings, credentials, identities, object keys and checksums;
- added regression tests and a dedicated retained `RC10 Operational Dashboard Gate`.

## Check

Fresh Grafana first-party review found 2026 security advisories including CVE-2026-27876 (critical RCE chain), CVE-2026-28383 (authenticated unbounded-memory allocation) and CVE-2026-21721 (cross-dashboard privilege escalation). Grafana's Docker documentation also states the current image repository is `grafana/grafana`. DTMO therefore does not pin or assume a current version in source; deployment must supply a currently supported, security-patched, digest-verified image and repeat advisory review at deployment time.

Provenance:

- https://grafana.com/security/security-advisories/cve-2026-27876/
- https://grafana.com/security/security-advisories/cve-2026-28383/
- https://grafana.com/security/security-advisories/cve-2026-21721/
- https://grafana.com/docs/grafana/latest/setup-grafana/installation/docker/

Confidence is high for these first-party vendor sources. No education-sector historical incident changes this bounded dashboard design; the material threats are observability-plane authorization, exposed administration, vulnerable runtime selection and accidental sensitive-data visualization.

No test or workflow is marked PASS before execution. RUN-139 remains `CI_VALIDATION_PENDING` until the dedicated workflow and every registered exact-head workflow succeed and retained `operational-dashboard-evidence` is independently inspected.

## Act

Lesson: operational dashboards should be source-controlled and read-only, but the visualization runtime itself remains a privileged management plane and must fail closed on image lifecycle and administrative credential configuration.

Production Grafana deployment, SSO/RBAC mapping, TLS/network controls, staging acceptance, runbooks, on-call handover, ownership/escalation and issue #1 external gates remain open.

## Exactly one next priority

Verify the complete exact-head workflow matrix and retained `operational-dashboard-evidence` artifact for RUN-139; merge only if both are complete and internally consistent.
