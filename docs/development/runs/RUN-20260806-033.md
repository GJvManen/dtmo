# RUN-20260806-033 — Governed decision audit integration

- **Started:** 2026-08-06 14:01 CEST
- **Workstream:** Phase 2 — application security and identity
- **Objective:** Transactionally couple live intelligence review and share-approval decisions to the persistent tamper-evident audit chain.

## Plan

Blocking gates: review and share state changes must be committed with their audit event; rollback must remove both; service accounts remain excluded; share approval requires a different human principal; denied separation-of-duties decisions must be durably recorded; exact-head CI must execute.

## Do

- Added governed decision service with row locking for intelligence transitions.
- Added review and share-approval API endpoints with permission dependencies and mandatory request IDs.
- Review stores `reviewed_by` and an `allow` audit event in the same transaction.
- Share approval enforces a distinct human approver and stores `share_approved_by` with an `allow` audit event.
- Separation-of-duties denial stores a persistent `deny` event before returning a conflict response.
- Added regressions for successful two-person review/approval, denied dual-role approval, chain verification and rollback atomicity.

## Check

Code and tests are committed. No local or CI execution is claimed. Exact PR-head GitHub Actions evidence is pending.

## Act

Lesson: governance state and its evidence must share one transaction; denied actions require an explicit durable-audit path because exception-driven request rollback would otherwise erase the evidence.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Exactly one next priority:** Inspect the exact-head Quality Gate and resolve only its earliest deterministic failure, or merge after complete success.
