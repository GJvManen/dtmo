# RUN-20260806-024 — RC5.2 least-privilege RBAC and separation of duties

- **Started:** 2026-08-06 13:04 CEST
- **Workstream:** Phase 2 — Application security and identity
- **Outcome:** `CI_VALIDATION_PENDING`

## Plan

Execute one bounded objective after the verified RC5.1 merge: separate intelligence ingestion from analyst review permission and enforce human/service-account separation for external share approval.

Blocking gates:

- PR #9 must be confirmed merged into `main`;
- ingestion must no longer require review authority;
- service accounts must not obtain human review or publication authority;
- one principal must not review and approve the same intelligence item;
- existing `reviewed` and `share_approved` separation must remain intact;
- new authorization tests must execute successfully in GitHub Actions before PASS.

## Do

- Confirmed PR #9 merged as commit `e7d745a37b09076fe5e50bc408120e58b98bde7c`.
- Created branch `rc5/rc5-2-rbac-authorization` from that merge commit.
- Added dedicated `Permission.INGEST_INTELLIGENCE`.
- Added explicit analyst, senior analyst, reviewer, publisher and service-account roles while retaining existing organizational roles.
- Changed `POST /api/v1/intelligence` from `REVIEW_INTELLIGENCE` to the least-privilege ingestion permission.
- Restricted service accounts to a single non-human role and denied mixed human/admin role composition.
- Added `require_separate_share_approval` to require an authorized human approver different from the reviewer.
- Added focused positive and negative regression tests in `backend/tests/test_rc5_rbac_authorization.py`.

## Check

Verified repository evidence:

- PR #9 state: merged;
- merge commit: `e7d745a37b09076fe5e50bc408120e58b98bde7c`;
- pre-merge Quality Gate #177 / run `31095343102`: successful with test, migration, dependency, container, workflow-contract and aggregate release jobs passing;
- no commit status contexts were exposed for the merge commit during this run;
- RC5.2 tests are committed but have not yet executed in GitHub Actions.

Security and governance review:

- ingestion still creates candidate intelligence only;
- review and share approval remain distinct permissions;
- service accounts cannot review or approve sharing;
- same-subject review and share approval is denied;
- no credentials or privacy-sensitive data were added.

## Act

Lesson: using review authority for ingestion violated least privilege and made automated connector identities unnecessarily powerful. Dedicated ingestion permission and explicit non-human identity constraints reduce privilege-escalation and publication risk.

Release decision: `CI_VALIDATION_PENDING`. The implementation is reviewable, but no new test or quality gate is marked PASS before execution.

## Exactly one next priority

Open one RC5.2 pull request and obtain an exact-head successful Quality Gate covering the new RBAC and separation-of-duties tests.
