# RUN-20260810-157 — Phase 8 runtime-smoke lifecycle regression remediation

## Status

`PASS` for the bounded CI-integrity remediation. PR #108 exact head `25ac24bfa40f2f9ccebb5d1307615c6fbd14cf05` completed all 48 registered workflows successfully. Phase 8 remains independently `BLOCKED_EXTERNAL` for one approved real staging deployment and all ten deployment-parity evidence classes.

## Plan

Objective: remediate exactly one higher-severity CI-integrity blocker discovered while verifying PR #108, without changing the accepted RUN-155 runtime-smoke claim boundary or the RUN-156 real-staging blocker.

Blocking gates at start:
- PR #108 previous exact head `c4c28938a49b2a3dcba90ab01e6bd1cb430a3439` completed 47/48 registered workflows successfully.
- `RC4 Quality Gate` run `31374642595` failed in the test job after lint and type-check succeeded.
- The first deterministic failing test was `test_runtime_qa_preserves_external_claim_boundary_and_governance` because it still required obsolete `CI_VALIDATION_PENDING` wording after the runtime-smoke QA had correctly advanced to bounded `PASS` from accepted PR #107 evidence.
- Phase 8 remains independently `BLOCKED_EXTERNAL` for one approved real staging deployment and all ten deployment-parity evidence classes.

## Do

Updated `backend/tests/test_phase8_staging_emulator_runtime.py` so the lifecycle regression requires the exact bounded accepted decision wording:

`PASS` for the bounded repository-controlled DTMO application-container runtime smoke only.

The regression continues to require:
- human share approval;
- explicit statement that the runtime smoke does not prove a real staging environment;
- explicit statement that it does not satisfy the ten deployment-parity evidence classes;
- explicit statement that it does not complete Phase 8 or production acceptance.

No application runtime, RBAC, separation-of-duties, privacy, provenance, auditability, secrets handling, connector behavior or human approval control was weakened.

## Check

Actual evidence from failed RC4 run `31374642595` on previous head `c4c28938a49b2a3dcba90ab01e6bd1cb430a3439`:
- lint: PASS;
- type check: PASS;
- pytest stopped at first failure with 51 tests passed and 1 failed;
- failure was the stale lifecycle assertion `assert "CI_VALIDATION_PENDING" in text` against QA content whose decision was bounded `PASS`;
- compile was unexecuted/skipped because the test step failed;
- release-gate failed as required.

After remediation, PR #108 exact head `25ac24bfa40f2f9ccebb5d1307615c6fbd14cf05` completed 48/48 registered workflows successfully. `RC4 Quality Gate` run `31375182061` succeeded end to end: lint PASS, mypy PASS, pytest 292 passed / 16 skipped with 84.96% coverage, compile PASS, container PASS, migrations PASS, PostgreSQL restore PASS, MinIO restore PASS, dependency audit PASS, workflow-contract PASS and aggregate release-gate PASS.

All three Phase 8 repository gates also succeeded on that exact head: `Phase 8 Staging Readiness Gate`, `Phase 8 Staging Emulator Gate`, and `Phase 8 Staging Emulator Runtime Gate`. Retained runtime artifact `9057841831`, digest `sha256:0e68feb37e9937b574a6ef80affeff13aeda162eb83c8805a8f220cb082999b1`, is bound to exact head `25ac24bfa40f2f9ccebb5d1307615c6fbd14cf05`. This evidence does not alter the real-staging claim boundary.

No absent, queued, cancelled, skipped or failed workflow is counted as PASS.

## Act

Lesson: lifecycle-contract regressions must follow evidence-backed state transitions while preserving immutable non-overclaim and governance assertions. A stale lifecycle token must not force accepted evidence back to a pending state.

Backlog impact:
- RUN-157 CI-integrity remediation: `PASS` from complete exact-head CI;
- RUN-156 remains the authoritative Phase 8 real-staging blocker;
- Phase 8 remains `BLOCKED_EXTERNAL` for the ten deployment-parity evidence classes tied to one immutable real staging deployment;
- Phase 6 genuine VoiceOver/NVDA execution remains separately `BLOCKED_EXTERNAL`;
- issue #1 external production-acceptance gates remain unchanged.

## Exactly one next priority

Provide or provision one approved real staging deployment and retain all ten deployment-parity evidence classes against the same immutable deployment identity. Do not begin or credit the staging acceptance suite before that gate is complete.
