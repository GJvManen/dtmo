# RUN-20260809-138 — RC10.7 CI-integrity remediation

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one higher-severity bounded objective discovered during RC10.7 acceptance: restore CI/workflow integrity for PR #94 without weakening lint/security policy or bypassing the failed gate.

Blocking gates:

- PR #94 exact head `cb889d2e643f4f00386bb6281ae3082f47031b98` had 41 registered workflows, but `RC4 Quality Gate` failed;
- failure must be diagnosed from actual job logs;
- remediation must preserve Ruff/Bandit `S105` rather than suppressing or excluding it;
- every registered workflow must rerun successfully on the new exact head;
- retained `distributed-trace-context-evidence` must be regenerated and exact-head bound before RC10.7 acceptance.

## Do

Inspected workflow run `31328513139` and failing job `93282799136`. The `Lint` step failed with two Ruff/Bandit `S105` findings in `backend/tests/test_rc10_7_distributed_trace_context.py` because synthetic privacy-test marker variables were named `secret_path` and `secret_query`.

Remediated only that deterministic lint failure by renaming the test-only variables to `synthetic_path_marker` and `synthetic_query_marker`. The marker values and privacy assertions are unchanged. No `# noqa`, per-file ignore, global ignore, workflow exception, skipped test or lint-policy weakening was introduced.

## Check

The previous exact head is not accepted despite 40 other successful workflows because one release-critical workflow failed. The dedicated trace-context gate success from that head is also insufficient for acceptance after the code change because evidence must bind to one exact final head.

No new threat-intelligence or vendor advisory changes are material to this bounded lint remediation. RUN-137's W3C Trace Context security/privacy provenance remains applicable; this run makes no new exploitability or threat-attribution claim.

Fresh CI on the remediated head is required. Missing, queued, cancelled, failed or unexecuted workflows are not PASS.

## Act

Lesson: synthetic security/privacy fixture names can trigger hardcoded-secret heuristics even when the values are intentionally fake. Prefer semantically neutral fixture names while preserving the scanner rule and test intent.

RC10.7 remains `CI_VALIDATION_PENDING`. Phase 7 does not advance to dashboards, runbooks or on-call work until PR #94 earns a complete exact-head PASS and retained trace-context evidence is independently verified.

## Exactly one next priority

Verify the complete registered workflow matrix and regenerated retained `distributed-trace-context-evidence` on the remediated PR #94 exact head; merge only if all evidence is complete and consistent.
