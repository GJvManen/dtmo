# RUN-20260806-036 — Operational revocation and authorization-denial audit

- **Started:** 2026-08-06 14:18 CEST
- **Workstream:** Phase 2 — application security and identity
- **Objective:** Provide a least-privilege operational token-revocation path and persist authorization denials in the append-only audit chain.

## Plan

Blocking gates are: only explicitly authorized human roles may revoke tokens; revocation requires JTI, future expiry, rationale and request correlation; successful revocation appends immutable evidence; permission denials append deny evidence before returning; audit failure may never turn a deny into an allow; exact-head CI must execute.

## Do

- Added `revoke:tokens`, granted only to CISO and administrator roles.
- Added validated revocation request/response contracts and `/api/v1/security/tokens/revoke`.
- Added Redis revocation execution with persistent allow audit evidence and reason provenance.
- Added generalized permission-denial recording with principal, permission, route and request correlation.
- Authorization remains fail closed when denial evidence cannot be persisted.
- Added focused regressions for authorized revocation, expired targets and denial-audit invocation.

## Check

Code and tests are committed but have not yet executed in GitHub Actions. No PASS is claimed.

## Act

The cross-system Redis/database operation prioritizes immediate revocation; a later resilience objective should add durable reconciliation/outbox evidence for rare audit-database failure after Redis acceptance.

- **CI evidence:** `PENDING`
- **Release gate:** `CI_VALIDATION_PENDING`
- **Exactly one next priority:** inspect the exact-head Quality Gate and resolve only its earliest deterministic failure, or merge after full success.
