# RUN-20260808-072 — RC8.3 bounded OpenSearch search-read performance harness

Release gate: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 5 objective: implement a bounded synthetic OpenSearch/search-read performance harness driven by the accepted RC8.1 workload profile. Blocking conditions are use of production personal data, weakening publication/review controls, absence of provenance checks, expansion into ingestion/queue/degraded-dependency load, or any attempt to claim PASS before exact-head CI and retained evidence execute.

## Do

- Added `dtmo.performance.search_read` with profile-driven search request rate and p95/p99/error budgets.
- Added deterministic synthetic search documents containing source provenance, confidence, fixture classification and `publish_approved=false`.
- Added bounded OpenSearch seeding for an isolated CI index and paced asynchronous query execution with correlation IDs, percentile/error calculation and fail-closed acceptance.
- Added focused regressions for profile/governance validation, synthetic provenance, publication-state preservation and search errors.
- Added `RC8 OpenSearch Search Read Performance Gate` using the repository's existing OpenSearch 2.19.1 CI topology, a 5,000-document synthetic CI corpus, 40 searches/s for five seconds, concurrency ceiling 20 and retained JSON/JUnit/OpenSearch environment evidence.
- Added `docs/qa/RC8_3_SEARCH_READ_PERFORMANCE_GATE.md`.
- No ingestion, queue pressure, connector burst or degraded-dependency load was added.

## Check

The accepted RC8.1 profile defines 40 searches/s, search p95 <= 800 ms, search p99 <= 1,500 ms and error rate <= 1%. This implementation reads those thresholds directly from the machine-readable profile.

The CI corpus is deliberately scaled to 5,000 synthetic documents for bounded regression execution and is explicitly recorded as `scaled_ci_fixture=true`; it does not represent the accepted 1,000,000-record production-readiness envelope and cannot satisfy the external load/stress gate in issue #1 by extrapolation alone.

Every returned hit must retain `external_id` and `source_uri`, and must retain `publish_approved=false`. Missing provenance, publication-approved hits, HTTP failures or budget violations fail the measurement.

At documentation time the new workflow has not yet executed on the final PR head. Therefore no latency, throughput, error-rate or test result is claimed as PASS. No configured, queued, missing or unexecuted CI is interpreted as success.

## Act

RC8.3 remains `CI_VALIDATION_PENDING`. The bounded CI harness establishes observable search-read behavior against a real OpenSearch service while preserving the distinction between regression-scale evidence and later representative/external load assurance.

RBAC, separation of duties, privacy, security, auditability, provenance and human share approval are unchanged. Direct OpenSearch CI access is limited to the isolated test service and does not alter production security requirements.

## Exactly one next priority

Inspect all exact-head workflows for the RC8.3 PR. Remediate only the earliest deterministic failure, or independently inspect retained search-read evidence and merge only if every required gate succeeds.