# RUN-20260827-377 — canonical UI-10 Audit / correlation screenshot candidate

## Scope

Create one unreviewed canonical UI-10 documentation screenshot candidate from the actual `/workbench/administration` Security & audit surface after accepted UI-09.

The candidate is `audit-correlation-workbench.png`. It must not replace `audit-correlation.png` until a separate governed visual review/promotion step accepts the exact generated artifact.

## Capture boundary

The Playwright capture renders the real DTMO frontend and targets the canonical read-only audit evidence panel with sanitized deterministic same-origin fixtures. It renders three representative persisted-event shapes: `share.review`, `misp.export.prepare` and `rbac.role.update`, including request IDs and event hashes for operator correlation context.

The capture does not submit token revocation, review, share, publication or other mutation requests. The historical `/ui/auditor` route is not used.

## Evidence classification

This is **documentation illustration only**. The candidate demonstrates the current canonical read-only append-only audit evidence projection and its visible correlation attributes. It does not prove production activity, live external connectivity, owner acceptance, production-equivalent behavior or independent assurance. Viewing audit evidence does not grant review, sharing or publication authority.

Request IDs, resource identifiers and event hashes are deterministic sanitized fixture values. They demonstrate the rendered evidence shape only and do not establish that any represented action occurred in staging or production.

## Acceptance

- canonical route: `/workbench/administration`;
- canonical section: Security & audit / Recent append-only audit evidence;
- candidate: `audit-correlation-workbench.png`;
- capture is exact-head gated and fail closed;
- fixture-backed state is explicit;
- audit evidence is explicitly read-only;
- request-ID correlation attributes and event hashes are rendered;
- no mutation is executed;
- token revocation is not executed;
- production activity is explicitly not claimed;
- review, sharing and publication authority remain absent;
- governed `audit-correlation.png` remains unchanged pending separate review.

After this bounded UI-10 capture slice is accepted, the next screenshot-migration priority is governed visual review/promotion of the canonical candidates, beginning with the oldest still-unpromoted candidate and preserving exact-run, exact-head and digest binding.
