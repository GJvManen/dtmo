# RUN-20260806-039 — RC5.12 storage-layer retention enforcement

## Plan

Implement one bounded privacy-control objective: persist privacy-minimized audit projections and enforce expiry through a scheduled, bounded purge while preserving immutable source audit records and explicit legal holds.

Blocking gates: linear reversible migration, no direct identifiers in projection storage, legal holds prevent deletion, purge is bounded and idempotent, source audit records remain untouched, exact-head CI and evidence artifacts succeed.

## Do

- added `AuditProjectionRecord` with purpose-bound references, source event hash, expiry and legal-hold metadata;
- added migration `0004_privacy_projection` with a restrictive foreign key to immutable audit evidence;
- added idempotent projection persistence and explicit legal-hold management;
- added bounded `FOR UPDATE SKIP LOCKED` purge batches;
- added `python -m dtmo.privacy.retention_cli` and a daily 02:17 UTC cron definition without embedded credentials;
- added regressions for minimization, source preservation, legal hold, batch limits, migration lineage and schedule safety;
- corrected RC5.11 documentation to reflect Quality Gate #221 and merged PR #19.

## Check

Code and tests are committed for GitHub Actions execution. No local or CI pass is claimed. Exact-head Quality Gate evidence is `PENDING`.

## Act

Lesson: retention must operate on derived, independently purgeable records; deleting or rewriting cryptographically linked source audit events would destroy evidentiary integrity.

Exactly one next priority: inspect the exact-head RC5.12 Quality Gate and either remediate only its earliest deterministic failure or merge after full success.

**Release gate:** `CI_VALIDATION_PENDING`
