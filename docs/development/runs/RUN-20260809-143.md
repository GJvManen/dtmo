# RUN-20260809-143 — RC10.10 exercise evidence-validator remediation

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one higher-severity Phase-1 CI/workflow-integrity remediation discovered while accepting RUN-142 / RC10.10. Preserve the exercise scenarios, security/privacy boundaries, RBAC, separation of duties, provenance and human share approval; do not weaken any gate or claim the exercise accepted until a fresh exact-head matrix and retained artifact succeed.

Blocking gates:

- PR #97 exact head `1862b1c4e9e768da82baef3470464845cadf3967` must be inspected rather than inferred;
- the failed dedicated workflow must have a deterministic cause;
- remediation must fail closed for both required-true and required-false safety controls;
- all 44 workflows must rerun on one new exact final head;
- regenerated `operational-runbook-exercise-evidence` must be exact-head bound and independently verified.

## Do

Verified PR #97 exact head `1862b1c4e9e768da82baef3470464845cadf3967` completed **43/44 workflows successfully**. The only failure was `RC10 Operational Runbook Exercise Gate` run `31331042196`.

Job `93289209513` showed the scenario tests themselves passed **5/5** and the evidence JSON was created and uploaded. The failure occurred only in `Validate evidence contract`: `assert all(e["controls"].values())` incorrectly treated the deliberately false safety controls `production_data_used=false` and `production_credentials_used=false` as failures.

Remediation:

- retained the evidence schema and all scenario/control values;
- replaced the blanket `all()` assertion with an explicit list of controls that must be `True`;
- added explicit `is False` assertions for `production_data_used` and `production_credentials_used`;
- preserved `not any(claim_boundary.values())` so no human-tabletop/on-call/ownership/Phase-7/production-completion claim can pass accidentally;
- introduced no exception, skipped check, or relaxed governance condition.

## Check

This is a deterministic gate-logic defect. The previous head is **not accepted**, despite 43 successful workflows and passing scenario tests, because its dedicated evidence validator failed. Prior-head artifact `9042942892` cannot be reused for acceptance.

No new threat-intelligence or vendor finding changes the risk model for this narrow CI-integrity remediation. RUN-142's CISA provenance remains applicable; no new operational or attribution claim is made.

## Act

Lesson: evidence schemas containing negative safety assertions must not be validated with a blanket truthiness check. Required-true and required-false controls must be encoded and asserted separately so the gate fails closed without rejecting correct safety evidence.

## Exactly one next priority

Verify all 44 workflows on the new exact PR #97 head and independently inspect regenerated `operational-runbook-exercise-evidence`; merge only if every workflow succeeds and retained evidence is exact-head bound and internally consistent.
