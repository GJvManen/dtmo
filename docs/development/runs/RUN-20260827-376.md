# RUN-20260827-376 — canonical UI-09 Administration/RBAC screenshot candidate

## Scope

Create one unreviewed canonical UI-09 documentation screenshot candidate from the actual `/workbench/administration` surface after accepted UI-08.

The candidate is `administration-rbac-workbench.png`. It must not replace `administration-rbac.png` until a separate governed visual review/promotion step accepts the exact generated artifact.

## Capture boundary

The Playwright capture renders the real DTMO frontend with sanitized deterministic same-origin fixtures for session, integration configuration, RBAC roles, principals and the separation-of-duties matrix. It performs no configuration, identity, connector, role or principal mutation.

Credential values remain server-side and are not exposed. The visible `credential_configured` state is configuration metadata only.

## Evidence classification

This is **documentation illustration only**. The candidate demonstrates the current canonical Administration/RBAC interaction surface with fixture-backed data. It does not prove RBAC enforcement against an external identity provider, token reissue, connector health, owner acceptance, production-equivalent behavior, independent assurance, penetration-test acceptance or production authorization.

The fixture also does not grant or prove human review/share authority. Service-account and separation-of-duties boundaries remain authoritative server-side policy.

## Acceptance

- canonical route: `/workbench/administration`;
- candidate: `administration-rbac-workbench.png`;
- capture is exact-head gated and fail closed;
- fixture-backed state is explicit;
- no credential value is exposed;
- no mutation is executed;
- RBAC enforcement and token reissue are explicitly not claimed;
- external/production assurance claims remain false;
- governed `administration-rbac.png` remains unchanged pending separate review.

After this bounded slice is accepted, the next screenshot-migration priority is UI-10 Audit / correlation surface.
