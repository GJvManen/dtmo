# RUN-20260810-166 — PR #112 residual reflow and focus-contrast remediation

## Plan

Bounded objective: remediate only the two remaining RC9 workflow failures observed on PR #112 exact head `854cbe9b6687ed65569d0551b280593a973a9cfd`.

Blocking gates:

- RC9 Reflow Accessibility Gate failed because an `.sr-only` analyst-search label was positioned at `left: -10000px`, which the 320 CSS px evidence collector correctly classified as a visible overflowing element.
- RC9 Contrast Accessibility Gate failed because the analyst primary search button used focus outline `#f8df6b` against primary background `#57baff`, measured at 1.596:1 against the control, below the required 3:1 focus contrast.

No RBAC, authorization, separation-of-duties, privacy, audit, publication approval or production-CSP boundary is in scope.

## Do

- Replaced the off-canvas `.sr-only` technique with a 1px clipped in-viewport technique so assistive text remains available without creating horizontal overflow evidence.
- Changed the RC9-compatible primary-button background to a dark blue with white text and a light-blue control border. The existing yellow focus outline now contrasts against both the control and surrounding dark surface.
- Preserved the accepted professional UX structure and all server-side authorization behavior.

## Check

Evidence from exact head `854cbe9b6687ed65569d0551b280593a973a9cfd`:

- 46 of 48 registered workflows succeeded.
- `RC9 Reflow Accessibility Gate`: FAILED; direct log showed exactly one overflowing element, an off-screen `label` at left `-10001`.
- `RC9 Contrast Accessibility Gate`: FAILED; direct log showed `search-submit` focus contrast ratio 1.596 against the control, minimum 3.0.
- All previously failing CISO, text resize and text spacing gates passed on that exact head.

A fresh full workflow matrix on the new exact head is required. No PASS is claimed until all registered workflows complete successfully.

## Act

Lesson: visually-hidden content must remain within the measured viewport geometry, and focus-indicator contrast must be evaluated against both the surrounding surface and the focused control fill. Future design-system changes must preserve these two contracts.

Decision: `CI_VALIDATION_PENDING`.

Exactly one next priority: verify every registered workflow on the final RUN-166 exact head; merge PR #112 only on complete exact-head success, otherwise remediate the first concrete remaining failure.
