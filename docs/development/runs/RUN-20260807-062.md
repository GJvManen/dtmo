# RUN-20260807-062 — RC7.8 bounded connector retry/backoff

Status: `CI_VALIDATION_PENDING`

## Plan

Bounded objective: implement and evidence connector retry/backoff behavior in Phase 4, immediately after RC7.7 source-health/failure isolation.

Blocking gates:

1. retries have a deterministic maximum-attempt budget;
2. exponential backoff is bounded;
3. provider `Retry-After` is honored only within an explicit cap;
4. invalid provider delay, non-retryable failures and exhaustion fail closed;
5. retry decisions remain connector-local and cannot affect unrelated connector scheduling state;
6. retry state can never imply intelligence publication approval;
7. dedicated and regression CI must actually execute on the exact PR head before PASS;
8. issue #1 external credential/rate-limit/licence/terms gates remain open.

## Do

- Added `backend/dtmo/connectors/retry.py` with `ConnectorRetryPolicy`, immutable `ConnectorRetryDecision`, deterministic exponential backoff, bounded provider `Retry-After`, maximum attempts and fail-closed exhaustion/non-retryable/invalid-delay decisions.
- Added `backend/tests/test_rc7_connector_retry.py` covering deterministic delays, backoff caps, provider-delay caps, invalid provider delay, attempt exhaustion, non-retryable failure, connector-local behavior and negative input validation.
- Added `.github/workflows/connector-retry.yml` with a dedicated RC7 Connector Retry Gate, JUnit output, retained JSON evidence and a fail-closed terminal gate.
- Added `docs/qa/RC7_8_CONNECTOR_RETRY_GATE.md`.
- Preserved `publish_approved=false` on every retry decision; no RBAC, privacy, separation-of-duties, audit or human share-approval control was weakened.

## Threat/vendor intelligence review

NVD's current developer guidance states that rate limits apply to API access and recommends deliberately spacing automated requests; it also recommends modified-date windows for efficient maintenance. This supports bounded retry/backoff rather than aggressive immediate retry. Provider-specific production credentials and enforced limits remain external evidence in issue #1.

Provenance/confidence:

- NVD Developer Start Here — primary NIST/NVD source; high confidence.
- NVD API User Workflows — primary NIST/NVD source; high confidence.

No new public intelligence observed in this bounded run changed the roadmap priority or introduced a higher-severity blocker.

## Check

Repository changes are committed on branch `rc7-8-connector-retry-backoff`. No test or CI result is treated as PASS merely because the workflow is configured. Exact-head GitHub Actions execution and retained artifact inspection are still required.

## Act

Lesson: retry policy must be independently bounded from the connector health circuit. Retry exhaustion is a per-execution fail-closed decision; repeated failed executions continue to feed the existing connector-local health/isolation control instead of creating shared/global retry state.

Backlog/status:

- RC7.8: `CI_VALIDATION_PENDING`.
- Phase 4: `IN PROGRESS`.
- issue #1 external provider gates: remain open.

## Exactly one next priority

Inspect the exact-head RC7 Connector Retry Gate and required RC4/RC6/RC7 regressions after GitHub registers and executes them; remediate only the earliest deterministic failure, or accept/merge only when all required exact-head evidence succeeds and retained retry evidence confirms bounded attempts/delays and `publish_approved=false`.
