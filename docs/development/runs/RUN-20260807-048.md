# RUN-20260807-048 — RC7.2 exact-head acceptance and merge

## Plan

Accept RC7.2 only if the exact pull-request head has independently observable successful execution for the connector-state gate, the regular quality gate and all required recovery/canary regression gates, including a retained connector-state evidence artifact. Otherwise record the first precise blocker and do not merge.

Blocking gates:

- RC7 Connector State Gate succeeds on the exact PR head;
- Alembic executes through `0005_connector_state`;
- targeted connector-state and migration tests execute successfully;
- persistence evidence executes against PostgreSQL;
- the independent fail-closed aggregate gate succeeds;
- `connector-state-evidence` is retained and bound to the exact head;
- RC4 Quality, live-canary, OpenSearch-recovery and multi-store-recovery gates succeed on the same head.

## Do

Inspected PR #29 at exact head `af4625b0f285da6e2b0d5135a623c418a9f3b9d4`.

Verified workflow run `31132196662` and its jobs. The primary connector-state job executed container initialization, dependency installation, Alembic migration, targeted pytest execution, PostgreSQL-backed persistence verification and fail-closed artifact upload. The separate aggregate gate also completed successfully.

Verified retained artifact `connector-state-evidence`:

- artifact ID: `8976473782`;
- digest: `sha256:a3f23e76eb550c058d942918582488aa9782b78b8845e4c76b5d9bdf5a9139b9`;
- exact head: `af4625b0f285da6e2b0d5135a623c418a9f3b9d4`;
- expiry: 2026-09-05;
- expired: false.

Merged PR #29 using the exact expected head. Squash merge commit: `ac31b9d4409b97d6db734791365a3dd814255c9d`.

No RBAC, separation-of-duties, privacy, provenance, quarantine, auditability or human share-approval control was weakened or bypassed.

## Check

Exact-head results:

| Gate | Run | Result |
|---|---:|---|
| RC7 Connector State Gate | 31132196662 / #17 | PASS |
| RC7 Live Connector Canary Gate | 31132196637 / #25 | PASS |
| RC4 Quality Gate | 31132196680 / #292 | PASS |
| RC6 OpenSearch Recovery Gate | 31132196636 / #44 | PASS |
| RC6 Multi-store Recovery Gate | 31132196665 / #34 | PASS |

Connector-state job evidence:

- Alembic upgrade through `0005_connector_state`: PASS;
- targeted connector-state and migration tests: PASS;
- PostgreSQL-backed isolation-state fixture: PASS;
- retained artifact upload: PASS;
- independent fail-closed aggregate gate: PASS.

RC7.2 is accepted as `PASS`. Phase 4 remains in progress because the roadmap also requires explicit credential, rate-limit, licence and terms validation plus malformed/duplicate quarantine evidence across approved live connectors.

## Act

Lessons recorded:

- exact-head status must be paired with step-level execution and retained artifact evidence;
- ORM parent/child ordering must be verified against the production database engine, not inferred from unit tests alone;
- connector recovery may restore candidacy but must never imply publication approval.

Backlog update:

- close RC7.2 persistent connector state and failure isolation as evidenced;
- continue Phase 4 in roadmap order.

## Exactly one next priority

Implement and evidence governed connector contract validation for approved live connectors: credentials presence without secret disclosure, rate-limit/backoff behavior, licence/terms provenance and fail-closed quarantine for malformed or duplicate records, while preserving human review before publication.
