# RUN-20260828-386 — R5 collection volume analytics

## Scope

One bounded R5 recovery slice after merged PR #385: expose persisted connector collection volume in canonical Visual Analytics.

## Repository evidence

- Command Center aggregates `ConnectorRun.inserted` grouped by `ConnectorRun.connector_id` directly from canonical persistence.
- Visual Analytics renders collection volume as an accessible chart and table.
- Canonical-store failure returns `collection_volume_distribution: []` with `data_state=unavailable`; values are never synthesized.
- Existing RBAC, server-side credentials, provenance and human review/share/publication boundaries are unchanged.

## Claim boundary

Collection volume is historical persisted execution evidence only. It does not prove live connector health, freshness, current upstream availability, local exposure, successful present-time collection, review completion, sharing approval, publication authority, staging operation, production-equivalent operation, penetration-test results or independent assurance.

## CI discipline

PR #386 must be accepted only on its exact head after all registered workflows for that head complete successfully. A green repository gate is engineering evidence only.
