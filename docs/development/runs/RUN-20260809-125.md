# RUN-20260809-125 — RC10.2 controlled connector-failure alerting

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 7 objective after accepted RC10.1: add an actionable alert signal for terminal connector failures, retain correlation evidence, prove recovery/clear behavior and fail closed on missing exact-head CI.

Phase 6 remains `BLOCKED_EXTERNAL` only for genuine VoiceOver/NVDA execution on supported real hosts; this run does not reclassify or bypass that blocker.

## Do

Created branch `rc10/rc10-2-connector-failure-alerting` from authoritative `main` and added:

- `ConnectorAlertManager` with bounded Prometheus connector-run, active-alert and transition metrics;
- structured `connector_alert_raised`, `connector_alert_active` and `connector_alert_cleared` events;
- safe correlation evidence for request-triggered and scheduler-triggered connector runs;
- explicit operator action guidance without copying raw connector error text into alert logs;
- repeat-raise suppression while an alert remains active;
- successful-run clear behavior;
- Prometheus rule `DTMOConnectorFailure`;
- controlled failure, recovery, repeat-failure and alert-contract regression tests;
- dedicated exact-head `RC10 Connector Failure Alerting Gate` with retained JSON, JUnit and pytest evidence;
- RC10.2 QA contract.

The existing RC7 bounded retry/backoff and connector-local failure-isolation controls remain authoritative. RC10.2 consumes only the terminal connector result and does not create an alternative retry/circuit-breaker policy.

## Check

Source-level design checks completed:

- failure signal is scoped by bounded connector ID labels;
- no raw error string is emitted in the alert event;
- alert state transition cannot approve publication;
- recovery clears the active signal only after a successful terminal connector result;
- repeated failure retains active state without another raise transition;
- Prometheus rule contains severity, operator action and clear-condition annotations.

No test result is claimed before GitHub Actions executes the final exact head.

## Act

Open RC10.2 for exact-head CI and retained evidence inspection. Status remains `CI_VALIDATION_PENDING` until all registered workflows and the dedicated alerting gate complete successfully.

## Claim boundary

Notification delivery, queue/storage/API/search alerting, dashboards, runbook exercise, on-call handover and Phase 7 completion remain outside this run.

## Exactly one next priority

Inspect every required workflow on the final RC10.2 pull-request head and retained `connector-alerting-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful evidence.
