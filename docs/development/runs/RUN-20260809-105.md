# RUN-20260809-105 — RC9.6 exact-head acceptance

Status: `PASS`

## Plan

Execute exactly one bounded objective: independently validate RC9.6 on the exact final PR #61 head, inspect retained responsive-layout evidence, and merge only if every registered workflow is complete and successful. Blocking gates are exact-head identity, complete GitHub Actions execution, retained artifact integrity, preserved backend-derived RBAC and no unsupported cross-browser or broad WCAG 2.2 AA claim.

## Do

- Reconciled repository state before execution; RC9.5 was already accepted and RC9.6 PR #61 was the authoritative next priority.
- Inspected PR #61 exact head `7e75f45fca15dc11be3a3c10d2d26797bdcdf92a`.
- Inspected all 25 pull-request workflow runs associated with that exact head.
- Independently fetched and inspected retained artifact `browser-responsive-layout-evidence`, artifact id `9038042763`, digest `sha256:9a0f218d68ea82a6cd564c923e8b5e90ec6550a43de853999faee87be8bfa62c`.
- Inspected retained JUnit XML, machine-readable JSON, health output and server log.
- Merged PR #61 using expected-head protection only after the gate passed; merge commit `a21cd14033f89a9294b060ef7bd071f7f026b281`.

## Check

All 25/25 workflows on exact head `7e75f45fca15dc11be3a3c10d2d26797bdcdf92a` completed with conclusion `success`, including `RC9 Responsive Layout E2E Gate` and all regression gates.

Retained evidence independently proves:

- `decision: pass` bound to exact head `7e75f45fca15dc11be3a3c10d2d26797bdcdf92a`;
- Chromium execution;
- representative viewports 360×800, 768×1024 and 1440×900;
- coverage of share approval, analyst search, CISO token revocation and auditor read-only surfaces;
- no blocking horizontal overflow;
- visible interactive controls remain within the viewport;
- minimum rendered control dimension is 24 px;
- `/api/v1/ui/session` remains real and backend-derived for RBAC visibility;
- no business mutations and no production data;
- no cross-browser or broad WCAG 2.2 AA claim.

JUnit reports 1 test with 0 failures, 0 errors and 0 skips. The retained server log shows HTTP 200 for health, each UI surface and real `/api/v1/ui/session` calls at each representative viewport. Health evidence retains `publication_gate: human-approval-required` and `authentication: api-key-and-rbac`.

No higher-severity blocker was discovered. RC9.6 adds no production dependency, connector, provider or credential; existing security/dependency gates remain authoritative for CVE/vendor findings. No new public threat-intelligence fact materially changes this bounded responsive-layout acceptance decision.

## Act

RC9.6 is accepted as `PASS`. Phase 6 remains `IN PROGRESS` because supported-browser breadth and broad WCAG 2.2 AA/accessibility completion remain unevidenced. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval remain mandatory. External production assurance gates in issue #1 remain open and are not implied by this internal acceptance.

Exactly one next priority: implement one bounded supported-browser critical-journey gate for the already accepted Phase-6 surfaces, preserving backend-derived RBAC and human share approval, with exact-head CI and retained evidence before any PASS claim.
