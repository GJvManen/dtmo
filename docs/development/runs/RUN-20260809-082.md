# RUN-20260809-082 — RC8.5 branch reconciliation against current main

## Plan

Objective: resolve the first deterministic blocker preventing RC8.5 exact-head CI: PR #42 had diverged 30 commits behind `main`, was not mergeable, and had no workflow runs for head `d96db2694afb720dba37f3236c2cfcff82c39b92`.

Blocking gates: exact-head GitHub Actions, retained queue-burst evidence, issue #1 independent representative load/stress gate, and later degraded-dependency testing.

## Do

Rebased the RC8.5 branch content onto current `main` by resetting the branch to main and restoring only the bounded RC8.5 implementation: queue-burst harness, focused regressions and the dedicated retained-evidence workflow. No Phase 6+, degraded-dependency or external-load work was added.

## Check

Historical evidence from original RC8.5 head is retained as provenance only and is not reused as acceptance. The reconciled exact head must execute all required workflows again. Missing, queued or unexecuted CI is not PASS.

Governance invariants remain unchanged: synthetic-only fixtures, `publish_approved=false`, preserved provenance, human review and separate share approval. External production load/stress remains unsatisfied by this bounded CI test.

## Act

Lesson: long-lived feature branches must be reconciled to accepted mainline governance/documentation before final exact-head acceptance; prior-head CI cannot be inherited after head movement.

Release decision: `CI_VALIDATION_PENDING` until the reconciled exact head executes successfully and retained queue-burst evidence is independently inspected.

Exactly one next priority: inspect all required workflows on the reconciled PR #42 head and either remediate the first deterministic failure or, only after complete success and artifact inspection, accept and merge RC8.5.
