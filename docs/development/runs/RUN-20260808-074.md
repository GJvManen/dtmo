# RUN-20260808-074 — RC8.4 bounded ingestion-throughput performance harness

Release gate: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 5 objective after RC8.3 acceptance: implement a bounded synthetic ingestion-throughput harness driven by the accepted RC8.1 workload profile. The objective is limited to governed normalization/replay throughput with measurable sustained throughput, zero data loss and zero duplicate candidate creation.

Blocking conditions are any weakening of provenance, privacy, RBAC/separation-of-duties or human share approval; any ability for performance execution to publish; any use of production personal data; or any attempt to include queue pressure, burst traffic or degraded dependencies in the same run.

## Do

- Added `backend/dtmo/performance/ingestion.py`.
- Bound the harness to `config/performance/phase5_workload_profile.json` and the existing governed `normalize_connector_records` ingestion path.
- Added deterministic synthetic education-sector intelligence fixtures only.
- Added an in-memory replay registry for the bounded CI fixture so the second pass must fail closed as replay rather than create duplicate candidates.
- Added focused regressions in `backend/tests/test_rc8_ingestion_performance.py`.
- Added `.github/workflows/ingestion-performance.yml` with retained JUnit and machine-readable performance evidence and an independent fail-closed aggregate gate.
- Added the RC8.4 QA record and updated the run log and issues #1/#2/#3.

## Check

No performance result is marked PASS in this development run because exact-head GitHub Actions has not yet been accepted. The implementation's acceptance contract requires:

- at least 100 sustained records/s from the accepted RC8.1 profile;
- zero data-loss records;
- zero duplicate candidates on an identical replay pass;
- zero first-pass quarantine for the valid synthetic fixture;
- provenance on every accepted candidate;
- `publish_approved=false` for candidates, replay quarantine and aggregate result;
- synthetic-only fixtures and no production personal data;
- human review and share-approval separation preserved;
- retained machine-readable evidence and executed regression JUnit.

The CI fixture is deliberately scaled: by default five seconds at 100 records/s = 500 synthetic records. It is not external representative-load evidence and does not close issue #1's independent load/stress gate.

Public threat intelligence, historical incident and CVE/vendor content do not materially change this bounded ingestion-throughput control objective; no new external threat claim is introduced into the acceptance logic.

## Act

RC8.4 remains `CI_VALIDATION_PENDING` until all required exact-head workflows execute successfully and the retained ingestion evidence is independently inspected. Missing, queued or unexecuted workflow evidence is not PASS.

RBAC, separation of duties, privacy, provenance, auditability and human share approval remain unchanged. Performance execution cannot publish or approve sharing.

## Exactly one next priority

Inspect the exact PR head across all required RC4/RC6/RC7/RC8 workflows. Remediate only the earliest deterministic failure, or independently inspect the retained ingestion-performance artifact and merge with expected-head protection only if every required gate succeeds.
