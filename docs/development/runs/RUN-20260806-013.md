# RUN-20260806-013 — Harden executable CI evidence

- **Started:** 2026-08-06 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Make test and container execution produce durable, regression-protected evidence while improving container failure diagnostics.

## Plan

1. Preserve lint, type, test, migration, container and dependency gates.
2. Add machine-readable unit-test and coverage artifacts.
3. Expand the container smoke test beyond process startup.
4. Ensure container diagnostics and cleanup always execute.
5. Regression-protect the new evidence requirements.

## Do

- Added JUnit XML and coverage XML generation to the test job.
- Added an always-uploaded `test-evidence` artifact with fail-on-missing behavior.
- Hardened the container job with `set -euo pipefail`, deterministic cleanup and container log capture.
- Added smoke validation for `/health` and `/openapi.json`.
- Added semantic checks for healthy status, mandatory human publication approval and OpenAPI path presence.
- Extended workflow contract tests to guard these controls against regression.

## Check

- Repository changes committed on the active pull-request branch.
- Contract structure is present in code.
- Actual execution evidence for the new commits is pending and may not be treated as success.
- No RBAC, privacy, provenance or human-approval control was weakened.

## Act

The CI pipeline now retains stronger evidence and exposes container startup failures more clearly. Phase 1 remains blocked until GitHub Actions executes the new head and all required jobs and artifacts are independently verified.

- **Run decision:** `BLOCKED`
- **Phase 1:** `BLOCKED`
- **Release:** `CI VALIDATION PENDING`
- **Exactly one next priority:** Inspect the next `RC4 Quality Gate` run for the current PR head and resolve the earliest deterministic failure from lint, type checking, tests, container smoke or dependency audit.
