# RUN-20260809-084 — RC8.6 degraded-dependency performance/correctness

## Plan

**Phase:** 5 — Performance and scalability  
**Objective:** implement exactly one bounded degraded-dependency gate proving that a temporary representative internal downstream dependency outage does not cause data loss, duplicate candidate delivery or governance bypass.

Blocking acceptance gates:

1. the accepted RC8.1 synthetic workload profile remains authoritative;
2. a temporary dependency outage is actually observed by the harness;
3. all submitted synthetic records are delivered after recovery;
4. data loss = 0 and duplicate candidates = 0;
5. provenance remains intact and `publish_approved=false` remains fail-closed;
6. recovery remains within the accepted Phase-5 recovery budget;
7. focused tests and machine-readable evidence execute on the exact PR head;
8. all previously required RC4/RC6/RC7/RC8/open-source-governance workflows remain successful.

Explicit exclusions: independent representative production load/stress, production OpenSearch security hardening, Phase 6 accessibility/UX and external assurance.

## Do

Implemented:

- `backend/dtmo/performance/degraded_dependency.py`
  - synthetic-only workload driven by `phase5_workload_profile.json`;
  - representative downstream dependency named `opensearch-index-sink`;
  - injected temporary-unavailable fault window;
  - bounded queueing and retry without publication privilege;
  - zero-loss, zero-duplicate, recovery, provenance and non-publication evaluation;
  - machine-readable JSON evidence;
  - explicit `external_load_gate_satisfied=false` and `production_opensearch_hardening_satisfied=false` boundaries.
- `backend/tests/test_rc8_degraded_dependency_performance.py`
  - workload-budget loading;
  - publication/privacy fail-closed regression checks;
  - outage observation and recovery without loss;
  - impossible-recovery-budget negative test;
  - invalid outage validation.
- `.github/workflows/degraded-dependency-performance.yml`
  - focused pytest/JUnit execution;
  - bounded harness execution;
  - retained JSON/JUnit evidence;
  - aggregate fail-closed terminal gate.

No production threat-intelligence records, CVE feeds, vendor advisories or personal data are used by this synthetic performance objective. No RBAC, review/share-approval or connector publication behavior is weakened.

## Check

PR #45 was opened from implementation head `fd5739d7ae03a7e8574282afc39c3a0c83a205b8`. Eighteen workflows registered.

The dedicated `RC8 Degraded Dependency Performance Gate` completed successfully and retained artifact `9030943239` (`sha256:9de7af1dfe2eb6666601152b0452607e3a427af5224f4b33d59eed777eeeb39f`) was independently inspected. That superseded head showed:

- 100 submitted / 100 delivered;
- 20 records buffered during the injected outage;
- 298 dependency failure/retry events;
- dependency unavailability observed = true;
- 0 data-loss records;
- 0 duplicate candidate deliveries;
- 0.25 s injected outage;
- 1.013 s recovery after dependency availability, within the 900 s accepted budget;
- provenance preserved = true;
- publication state preserved = true;
- `load_test_may_publish=false`;
- JUnit: 6 tests, 0 failures, 0 errors, 0 skipped.

However, the release-wide RC4 Quality Gate failed in its lint step with Ruff `F841`: local variable `dependency_name` was assigned but unused in `degraded_dependency.py`. Type-check and full tests were skipped by that failed job. This is the first deterministic failure and therefore blocks acceptance despite the dedicated RC8.6 evidence being successful.

The unused variable was removed in commit `682a876a4339485ac1ee3708deaeef0ca70c0f65`. Because this changed the PR head, evidence from `fd5739d...` is historical/superseded and **must not** be used to accept the new exact head.

The harness uses in-process fault injection for a representative downstream indexing dependency. It proves a bounded application-level correctness contract only; it does not prove production OpenSearch hardening or issue #1's independent representative external load/stress gate.

## Act

Lesson: focused performance evidence can succeed while a broader release-quality gate correctly blocks acceptance. Exact-head identity is therefore mandatory after every remediation, even when the remediation is a one-line lint fix.

**Release gate:** `CI_VALIDATION_PENDING` after remediation. No PASS is claimed until the remediated final head executes every required workflow successfully and its retained artifact is independently inspected.

**Exactly one next priority:** inspect all required workflows on the final RC8.6 PR head and independently inspect the new retained degraded-dependency artifact; remediate only the earliest deterministic failure, or accept/merge only after complete successful evidence.
