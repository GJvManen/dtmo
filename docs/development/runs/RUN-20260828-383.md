# RUN-20260828-383 — R5 source contribution analytics

## Scope

This bounded R5 recovery slice adds one cross-workspace statistic: attributable source contribution for canonical intelligence. The Command Center read model groups persisted `IntelligenceItem` records by `source_id`; Visual Analytics renders the returned counts as both a chart and an accessible table.

## Preserved boundaries

- Counts come from canonical DTMO persistence and are not synthesized from UI placeholders.
- A datastore/read-model failure returns unavailable/empty analytics rather than invented values.
- Source contribution is historical persisted content evidence, not proof of source reachability, connector health or current upstream availability.
- No new mutation, connector, review, share, publication or responder authority is introduced.
- Existing server-side RBAC, provenance and credential boundaries are unchanged.

## Acceptance evidence

Repository-controlled acceptance verifies the canonical grouped query, the fail-closed empty trend contract, frontend build compatibility, and an accessible table/text equivalent for the source-contribution chart.

This evidence does not establish staging or production-equivalent operation, current external-source availability, owner usability acceptance, penetration-test results, independent assurance or production authorization.
