# RUN-20260808-070 — RC8.2 bounded API-read performance harness

Release gate: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 5 objective: implement a bounded synthetic API/read performance harness against the accepted RC8.1 workload profile. Blocking conditions are any divergence from the accepted performance budgets, use of production personal data, weakening of RBAC/human publication controls, expansion into search/ingestion/degraded-dependency load, or absence of actual exact-head CI evidence.

## Do

- Added `dtmo.performance.api_read`, which loads API-read traffic and latency/error budgets from `config/performance/phase5_workload_profile.json`.
- Added bounded paced asynchronous request execution with a concurrency ceiling, correlation IDs, percentile/error calculation and fail-closed acceptance evaluation.
- Added governance validation that rejects a performance profile permitting publication, production personal data, absent human review or collapsed review/share-approval separation.
- Added focused regressions for budget loading, unsafe-profile rejection, governance-marker enforcement, HTTP failures and invalid targets.
- Added `RC8 API Read Performance Gate`, which starts the real DTMO FastAPI application, executes 500 `/health` reads over five seconds at the accepted 100 req/s target, retains JSON/JUnit/server-log evidence and has an independent fail-closed aggregate job.
- Added `docs/qa/RC8_2_API_READ_PERFORMANCE_GATE.md`.
- No search load, ingestion load, queue pressure, connector burst or degraded-dependency scenario was added.

## Check

At documentation time the implementation has been committed but exact-head GitHub Actions evidence has not yet been accepted. Therefore no latency, throughput, error-rate or regression result is reported as PASS in this run.

The runtime evidence contract requires p95 <= 300 ms, p99 <= 750 ms, error rate <= 1%, `publication_gate=human-approval-required`, and `authentication=api-key-and-rbac`. The test target is read-only `/health`; mutating review/share-approval routes are outside scope. The external load/stress gate in issue #1 remains open.

No configured, queued, skipped, missing or unexecuted test is interpreted as success.

## Act

RC8.2 remains `CI_VALIDATION_PENDING`. The lesson is that Phase 5 should begin with a small, observable read-only performance surface whose thresholds come from the accepted workload contract and whose runtime response proves governance markers remain intact under load.

RBAC, separation of duties, privacy, security, auditability, provenance and human share approval remain unchanged.

## Exactly one next priority

Inspect all exact-head workflows for the RC8.2 PR. Remediate only the earliest deterministic failure; otherwise independently inspect retained API-read evidence and merge only if every required gate succeeds.
