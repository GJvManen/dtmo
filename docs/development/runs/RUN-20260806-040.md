# RUN-20260806-040 — RC6.1 clean PostgreSQL backup and restore evidence

- **Started:** 2026-08-06 15:01 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove one reproducible logical PostgreSQL backup can be restored into a clean database while preserving canonical intelligence, provenance and the tamper-evident audit chain.

## Plan

Blocking gates:

- the source schema must be upgraded to the current Alembic head;
- the restore target must be demonstrably empty before recovery;
- `pg_dump` must create a non-empty custom-format backup with a recorded SHA-256 digest;
- `pg_restore` must complete with fail-fast error handling;
- source and restored canonical manifests must match exactly;
- provenance content hashes and the complete audit-chain tail must survive recovery;
- measured restore duration and the quiesced-snapshot RPO basis must be retained as machine-readable evidence;
- the new restore job must be release blocking and exact-head GitHub Actions must succeed before PASS.

## Do

- added `tools/verify_postgres_backup_restore.py` to seed a controlled reviewed/share-approved intelligence fixture with authoritative provenance and two governed audit events;
- added deterministic source and target manifests covering intelligence, provenance, audit records and Alembic revision state;
- added `pg_dump` custom-format backup creation and `pg_restore --exit-on-error` into a clean target database;
- added SHA-256 backup evidence, measured restore duration, explicit quiesced-snapshot RPO and row-count/integrity evidence;
- added a release-critical `postgres-restore` GitHub Actions job with a PostgreSQL 17 service and retained `postgres-restore-evidence` artifact;
- extended workflow-contract regressions so removal, skipping or omission from the aggregate release gate blocks CI acceptance;
- preserved RBAC, separation of duties, privacy-minimized projections, append-only source audit evidence and human share approval;
- remediated runner/server version mismatch by installing the PostgreSQL 17 client from the signed PGDG repository and verifying client/server major-version compatibility before backup.

## Check

Quality Gate #229 completed successfully on exact PR head `d1d0e809ffcee6458cb8a8f31ad2d10d481fefb0` (run ID `31105612270`). All release-critical jobs passed, including the clean-target `postgres-restore` job. Evidence artifacts were retained:

- `postgres-restore-evidence` — artifact `8969397478`;
- `release-gate-evidence` — artifact `8969403121`;
- `workflow-contract-evidence` — artifact `8969379199`;
- `dependency-audit-evidence` — artifact `8969379447`.

PR #22 was merged to `main` as merge commit `3441e5be486fd9bcca8ab1d8f531ca8e5d38958b`.

## Act

Lesson: schema migration success is not recovery evidence. Recovery acceptance must start from an empty target, compare canonical source and target state, cryptographically verify audit continuity and retain a digest-bound backup artifact plus measured recovery timing. Client tooling must also be pinned to a server-compatible major version and validated before backup execution.

- **Release gate:** `PASS`
- **Known blocker:** Phase 3 still lacks MinIO object restoration, OpenSearch reconstruction and combined recovery acceptance.
- **Exactly one next priority:** implement clean-environment MinIO object backup and restore evidence with object digest and provenance-reference verification.
