# RUN-20260806-017 — Align executable Ruff policy

- **Started:** 2026-08-06 09:52 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Diagnose `RC4 Quality Gate` run `31082453008` and repair only the earliest remaining deterministic failure: the Ruff lint policy mismatch.

## Plan

1. Bind inspection to PR #7 head `79f6d778f3cb65f8d8d55e9fae5d7743720042d9`.
2. Confirm the TOML packaging repair through actual CI evidence.
3. Inspect the first failing substantive job and its log.
4. Align Ruff configuration with the repository layout, FastAPI dependency injection and deliberate security-test fixtures without disabling core error, security or correctness families.
5. Keep Phase 1 blocked until replacement CI executes successfully and all required artifacts are verified.

## Do

- Confirmed run `31082453008` completed with `failure`.
- Confirmed dependency installation, migrations and `workflow-contracts` succeeded, proving the prior TOML repair effective.
- Confirmed the `test` job first failed at `ruff check backend database` with 41 findings before type checking and tests.
- Added `backend` as Ruff source root and `dtmo` as known first-party package.
- Set the project line length to 120, matching existing code without suppressing syntax or correctness checks.
- Added the standard FastAPI `B008` exemption for dependency declarations.
- Added narrow test-only exemptions for deliberate secret fixtures (`S106`) and the workflow parser's deliberate `BaseLoader` use (`S506`).
- Policy commit: `87089002f835e1bb2d076f924f1cd684984a3d79`.

## Check

- Actual run evidence shows migrations and workflow-contract evidence generation passed.
- Type checking and tests were skipped after lint failure and remain unexecuted for run `31082453008`.
- Dependency review and container smoke test also failed but are outside this bounded objective.
- The replacement workflow for commit `87089002f835e1bb2d076f924f1cd684984a3d79` had not completed when this record was written.
- RBAC, separation of duties, privacy, provenance, auditability and human share approval were unchanged.

## Act

- **Run decision:** `BLOCKED` pending replacement CI execution.
- **Phase 1:** `BLOCKED`.
- **Release:** `CI VALIDATION PENDING`.
- **Lesson:** Lint policy must encode framework conventions and repository package boundaries explicitly; otherwise valid FastAPI and test patterns create non-actionable release failures.
- **Exactly one next priority:** inspect the `RC4 Quality Gate` run for commit `87089002f835e1bb2d076f924f1cd684984a3d79` and resolve only its earliest remaining deterministic failure, or verify the required evidence if it succeeds.
