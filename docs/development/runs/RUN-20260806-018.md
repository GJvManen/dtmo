# RUN-20260806-018 — Restore a mergeable lint-validation probe

- **Started:** 2026-08-06 10:43 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Re-establish one clean, mergeable pull-request probe from current `main` carrying only the previously identified Ruff policy alignment, because PR #7 became non-mergeable before replacement CI could execute.

## Plan

1. Inspect PR #7 and bind conclusions to its exact head.
2. Confirm whether a replacement `RC4 Quality Gate` run exists for that head.
3. Treat a non-mergeable PR with no run as an invalid execution probe, not as CI evidence.
4. Create a fresh branch from current `main` and carry forward only the bounded Ruff policy alignment already justified by run `31082453008`.
5. Open a new PR and leave all gates blocked until an actual run completes.

## Do

- Confirmed PR #7 head `4b0d966c98c16dc68341f7992500132567523fb3` is non-mergeable.
- Confirmed GitHub exposes zero pull-request workflow runs for that exact head.
- Created branch `pdca/run-20260806-018-lint-ci-probe` from current `main`.
- Carried forward the bounded Ruff policy alignment in `pyproject.toml` without changing application behaviour, authorization, data handling or publication controls.
- Implementation commit: `ea8259d6e32c00a964d993af7e92b19d7e4f7327`.

## Check

- Historical run `31082453008` remains valid evidence that dependency installation, migrations and workflow contracts passed before lint failed.
- No replacement workflow run exists for the stale PR #7 head; therefore lint, type checking, tests, container smoke and dependency review are not marked passed for this run.
- The fresh branch is based on current `main`, which already contains the TOML packaging repair.
- RBAC, separation of duties, privacy, provenance, auditability and human share approval remain unchanged.

## Act

- **Run decision:** `BLOCKED` pending actual CI execution on the fresh PR.
- **Phase 1:** `BLOCKED`.
- **Release:** `CI VALIDATION PENDING`.
- **Lesson:** Audit and CI remediation branches must remain based on current `main`; once a probe becomes non-mergeable, absence of a replacement run cannot be interpreted as a result.
- **Exactly one next priority:** inspect the first completed `RC4 Quality Gate` run for the fresh PR and resolve only its earliest deterministic failure, or verify the required evidence if it succeeds.
