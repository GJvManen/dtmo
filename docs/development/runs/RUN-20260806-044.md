# RUN-20260806-044 — RC7.1 governed live connector canary

- **Started:** 2026-08-06 17:07 CEST
- **Completed:** 2026-08-06 17:22 CEST
- **Workstream:** Phase 4 — live connector reliability and provenance
- **Objective:** Prove one controlled live CISA KEV canary with explicit source provenance, licensing metadata, timeout, bounded retry/backoff, rate limiting, deduplication, quarantine and fail-closed publication state.

## Plan

Blocking gates:

- only HTTPS source and terms URLs are accepted;
- licence metadata is mandatory;
- timeout and retry count are bounded;
- retry backoff is observable and capped;
- malformed, duplicate and overflow records are quarantined;
- accepted records retain source URL, timestamp, confidence and raw-evidence hash;
- imported records remain candidate-only and `publish_approved` must remain false;
- exact-head GitHub Actions and retained evidence are mandatory.

## Do

- added `dtmo.connectors.canary` with policy validation, controlled HTTP execution, bounded exponential backoff and evidence models;
- added CISA KEV parsing with provenance preservation, deduplication and quarantine;
- added a CLI runner producing machine-readable JSON evidence;
- added positive and negative regression tests;
- added `RC7 Live Connector Canary Gate` with retained `live-connector-canary-evidence` and a fail-closed aggregate job;
- updated README, run log, QA documentation and issues #2/#3 in the same branch;
- remediated strict MyPy type narrowing for required CISA KEV provenance fields without changing runtime or governance behavior.

## Check

Exact head `c82e20c110354c1163b58ac8b9820756f829a4ae` passed:

- RC7 Live Connector Canary Gate #3, run `31115437860`;
- RC4 Quality Gate #270, run `31115438813`;
- RC6 OpenSearch Recovery Gate #22, run `31115440019`;
- RC6 Multi-store Recovery Gate #12, run `31115438461`.

Retained evidence:

- `live-connector-canary-evidence` — artifact `8973407243`, digest `sha256:437b09bf13746fecf4e929921e1a63ac74bdbba1f1ecb08e0d04b99f763a3f53`;
- `release-gate-evidence` — artifact `8973424158`;
- `postgres-restore-evidence` — artifact `8973421161`;
- `dependency-audit-evidence` — artifact `8973411040`;
- `minio-restore-evidence` — artifact `8973409186`;
- `workflow-contract-evidence` — artifact `8973408182`.

PR #28 merged as `aeeb0709a26ecb1f20620d7ac21f823fec35e98f`.

## Act

Lessons:

- live-source availability never substitutes for governance or publication approval;
- runtime validation and static type narrowing must express the same provenance contract;
- retained exact-head evidence is required before accepting a connector control as passed.

- **Release gate:** `PASS`
- **Known blocker:** none for RC7.1.
- **Exactly one next priority:** RC7.2 — persistent connector-run state, source-health history and failure isolation with quarantined recovery and no automatic publication.
