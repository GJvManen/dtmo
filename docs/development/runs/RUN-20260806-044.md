# RUN-20260806-044 — RC7.1 governed live connector canary

- **Started:** 2026-08-06 17:07 CEST
- **Workstream:** Phase 4 — live connector reliability and provenance
- **Objective:** Prove one controlled live CISA KEV canary with explicit source provenance, licensing metadata, timeout, bounded retry/backoff, rate limiting, deduplication, quarantine and fail-closed publication state.

## Plan

Blocking gates:

- only HTTPS source and terms URLs are accepted;
- licence metadata is mandatory;
- timeout and retry count are bounded;
- retry backoff is observable and capped;
- malformed and duplicate records are quarantined;
- accepted records retain source URL, timestamp, confidence and raw-evidence hash;
- imported records remain candidate-only and `publish_approved` must remain false;
- exact-head GitHub Actions and retained evidence are mandatory.

## Do

- added `dtmo.connectors.canary` with policy validation, controlled HTTP execution, bounded exponential backoff and evidence models;
- added CISA KEV parsing with provenance preservation, deduplication and quarantine;
- added a CLI runner producing machine-readable JSON evidence;
- added positive and negative regression tests;
- added `RC7 Live Connector Canary Gate` with retained `live-connector-canary-evidence` and a fail-closed aggregate job;
- updated README, run log, QA documentation and issues #2/#3 in the same branch;
- after Canary Gate #1 passed, remediated four strict MyPy errors in the CISA KEV parser by replacing aggregate `all(...)` validation with explicit `isinstance` and non-empty checks that narrow the three required provenance fields to `str` before deduplication and record construction.

## Check

RC7 Live Connector Canary Gate #1 succeeded on exact head `b6e31f63b481fe6ba40089ba7ddd2fe848d6aa8a`. Existing recovery regression gates also succeeded. RC4 Quality Gate #268 failed in strict MyPy before tests because `dict.get()` values remained typed as `Any | None` after the aggregate validation expression. No release `PASS` was claimed.

The deterministic type-narrowing remediation is committed as `6029ba5d8e2963f0c348aee6dc3b3f8be8a33ea5`. A new exact-head Quality Gate and Canary Gate must both complete successfully before merge.

## Act

Lesson: runtime aggregate validation does not necessarily provide static type narrowing. Required connector provenance fields should be validated individually so runtime safety and strict typing express the same contract.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** the remediated exact-head Quality Gate and live connector canary evidence have not both completed successfully.
- **Exactly one next priority:** inspect the new exact-head Quality Gate and RC7 Live Connector Canary Gate; merge only after both pass with retained evidence.
