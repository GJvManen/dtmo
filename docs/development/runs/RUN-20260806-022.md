# RUN-20260806-022 — Reversible canonical intelligence migration

- **Started:** 2026-08-06 12:41 CEST
- **Workstream:** Phase 3 — Data integrity and recovery
- **Objective:** Add one reversible, data-preserving Alembic migration that moves existing RC4.8 intelligence and provenance records into the RC5.1 canonical data foundation.

## Plan

1. Treat the successful RC5.1 domain-model workflow as evidence for the prior bounded objective, not as proof that existing databases can be upgraded safely.
2. Add one linear Alembic revision after `0001_rc4_core`.
3. Backfill canonical confidence and provenance fields before removing legacy confidence columns.
4. Preserve review status, human share approval and existing intelligence content without modification.
5. Add a deterministic migration contract test and require the existing CI migration job to prove upgrade, downgrade and re-upgrade.

### Blocking gates

- migration revision chain is linear and reversible;
- legacy confidence data is copied before legacy columns are removed;
- confidence and education-relevance ranges are constrained;
- provenance deduplication and immutable revision constraints are present;
- actual CI upgrade/downgrade/re-upgrade evidence is required before PASS.

## Do

- Added `database/migrations/versions/0002_rc5_canonical_intelligence.py`.
- Added canonical confidence score, level and rationale columns with deterministic RC4 backfill.
- Added source reliability, primary-source, content-integrity and canonical confidence fields to provenance.
- Added uniqueness and range constraints for provenance and confidence data.
- Added the immutable `intelligence_revisions` table with ordered revision, content-hash and cascade-integrity controls.
- Implemented a downgrade that restores representable RC4 confidence columns before removing RC5-only structures.
- Added `backend/tests/test_rc5_canonical_migration.py` to regression-protect revision identity, backfill-before-drop ordering and governance/integrity controls.

## Check

- Repository evidence confirms the new revision follows `0001_rc4_core`.
- Static contract tests are committed but have not yet been observed executing on the new exact head.
- The migration is designed for PostgreSQL and relies on the existing CI migration job for actual upgrade, current-revision validation, downgrade and re-upgrade evidence.
- No security, privacy, RBAC, review-status or human share-approval semantics were weakened or migrated automatically.
- Public threat intelligence, historical education incidents, CVE data and vendor advisories were not material to this schema-only objective; no external claims were introduced.

## Act

- **Run decision:** `BLOCKED` pending actual CI migration and regression-test execution for the new exact head.
- **Phase 3:** `IN PROGRESS`; database migration automation now covers the RC5.1 canonical foundation, but backup and restoration evidence remains absent.
- **Production readiness:** `BLOCKED`; external gates in issue #1 remain open.
- **Lesson:** ORM and domain-model success does not prove safe upgrade of existing production-like data; schema evolution requires explicit backfill order and reversible evidence.
- **Exactly one next priority:** inspect the new exact-head workflow and, if successful, add a clean-environment restoration test for PostgreSQL plus canonical provenance integrity.
