# RUN-20260809-135 — bounded API-error alerting

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 7 objective: implement bounded API-error alerting on top of the accepted request-observability baseline, without exposing raw request data or changing RBAC, provenance, review/share separation or human publication approval.

Blocking gates:

- RUN-134 reconciliation must be accepted and merged;
- only bounded route templates may be used as alert/metric identifiers;
- repeated server errors must have deterministic raise behavior and recovery must have deterministic clear behavior;
- correlation evidence and actionable operator guidance must be retained without request URL/query/body/header/identity leakage;
- repeated failures while active must not repeat the raise transition;
- Prometheus alert state/rule must be independently observable;
- dedicated RC10.5 CI plus the complete registered workflow matrix must succeed on one exact final head before acceptance.

## Do

Verified PR #91 exact head `d81caaa372b0cf3e079023eb255a57fd4892d6e0` completed all 38 registered workflows successfully and merged with expected-head protection as `23af430c041e3f0e203b7a7f7a6c69f3eea79055`.

Implemented RC10.5:

- added `backend/dtmo/api_alerts.py` with a bounded `ApiErrorAlertManager`;
- observes only route template, status code and correlation identifier;
- raises after 3 consecutive HTTP 5xx outcomes for the same route template;
- clears only after 2 consecutive non-5xx outcomes while active;
- exposes bounded request-result, streak, active-state and transition Prometheus metrics;
- suppresses duplicate raise transitions while the alert remains active;
- emits structured correlated raise/active/recovering/clear evidence with actionable guidance and `publish_approved=false`;
- wired the existing HTTP request middleware to the observer for both returned responses and unhandled exception/500 paths;
- added `DTMOApiServerErrors` to `ops/prometheus/dtmo-alerts.yml`;
- added controlled integration/regression tests, including route-template privacy evidence and recovery behavior;
- added independently observable workflow `RC10 API Error Alerting Gate` with exact-head machine-readable evidence and retained JUnit/log artifacts.

No production data, raw request URL, query string, request/response body, authorization header, identity value or publication approval was added to alert labels/log evidence.

## Threat intelligence / advisory review

Fresh public dependency review on 2026-08-09 identified Starlette advisories CVE-2026-48817 and CVE-2026-48818, affecting Starlette versions through 1.0.1 and fixed in 1.1.0 according to NVD/upstream references. DTMO declares FastAPI but does not directly pin Starlette in `pyproject.toml`; therefore this run does **not** claim DTMO is affected or exploitable. The exact resolved dependency set remains subject to the existing dependency/security CI gates, and any failure there blocks acceptance.

Provenance:

- NVD CVE-2026-48817: https://nvd.nist.gov/vuln/detail/CVE-2026-48817
- NVD CVE-2026-48818: https://nvd.nist.gov/vuln/detail/CVE-2026-48818
- Starlette security advisories: https://github.com/Kludex/starlette/security/advisories

Confidence: high for advisory affected/fixed ranges; DTMO exploitability confidence is intentionally `not established` without resolved-version and code-path evidence.

No new education-sector incident changed this bounded implementation objective. API 5xx alerting is treated as operational detection, not as attribution or proof of malicious activity.

## Check

Pre-CI review confirms:

- alert labels use route templates rather than raw paths;
- `?` query strings and whitespace-bearing/raw URL-like route identifiers are rejected by the alert manager;
- controlled middleware tests use a synthetic student-like path value and secret-like query value and assert neither appears in Prometheus alert evidence;
- raise threshold is 3 consecutive 5xx outcomes;
- clear threshold is 2 consecutive non-5xx outcomes;
- repeated active failures do not repeat the raise transition;
- the Prometheus rule is warning severity, actionable and documents raise/clear policy;
- `publish_approved` remains false.

No test is marked PASS before execution. This run remains `CI_VALIDATION_PENDING` until the dedicated RC10.5 workflow and every registered GitHub Actions workflow complete successfully on the exact final PR head. Missing, queued, cancelled, failed or unexecuted workflows are not PASS.

## Act

Lesson: API error alerting should be driven by already-normalized route templates, not request URLs. This keeps metric cardinality bounded and prevents identifiers/query material from entering alert labels. Small consecutive-outcome hysteresis provides controlled failure/recovery evidence without turning a single transient 500 into an operational incident.

Backlog/state:

- RC10.5 implementation is present but not accepted until exact-head CI and retained evidence are verified;
- search-health alerting, distributed tracing, dashboards, runbooks and on-call handover remain open Phase 7 work;
- Phase 6 genuine VoiceOver/NVDA evidence remains `BLOCKED_EXTERNAL`;
- issue #1 external production gates remain open.

## Exactly one next priority

Verify the complete exact-head workflow matrix and retained `api-error-alerting-evidence` artifact for the RC10.5 PR; merge only if every registered workflow succeeds and the retained evidence is exact-head bound and internally consistent.
