# RUN-20260807-065 — RC7.9 exact-head acceptance and merge

Release gate: `PASS`

## Plan

Accept or reject PR #36 only from objective evidence on exact head `0eee765b72d016489af248d2bf29e3b1dbd593db`. Blocking conditions were any absent, queued, failed or cancelled required RC4/RC6/RC7 workflow; missing retained timeout evidence; timeout evidence that did not prove connector-local cancellation; or any state implying publication approval.

## Do

- Inspected PR #36 and confirmed its head remained `0eee765b72d016489af248d2bf29e3b1dbd593db` and was mergeable.
- Inspected all GitHub Actions workflow runs associated with that exact head.
- Retrieved retained artifact `connector-timeout-evidence` (`9003952153`, digest `sha256:b448608ae258086bacf0f4cb69c2c18f566b58851f7e55ace322fb30b5dc353f`) from RC7 Connector Timeout Gate run `31203965560` / run number `1`.
- Independently inspected the JSON evidence and JUnit XML.
- Merged PR #36 with expected-head protection. Merge commit: `3c5e1ed278d86b5279a285e546d24e12fbaabd3f`.

## Check

All 12 required exact-head workflows completed successfully:

- RC4 Quality Gate #345;
- RC6 OpenSearch Recovery Gate #97;
- RC6 Multi-store Recovery Gate #87;
- RC7 Connector State Gate #42;
- RC7 Live Connector Canary Gate #78;
- RC7 Connector Contract Gate #49;
- RC7 Payload Provenance Gate #40;
- RC7 Connector Replay Gate #13;
- RC7 Connector Freshness Gate #9;
- RC7 Connector Failure Isolation Gate #5;
- RC7 Connector Retry Gate #2;
- RC7 Connector Timeout Gate #1.

Retained evidence records `decision=pass`. JUnit records 8 tests with 0 failures, 0 errors and 0 skips. The `cisa-kev` operation exceeded its 0.02 second test budget, was actually cancelled and failed closed as `timeout_budget_exhausted`. The independent `nvd` connector completed as `nvd-ok`. Both timeout and successful-completion decisions retain `publish_approved=false`. Scheduler cancellation and missing provenance are covered by the executed regression suite.

No absent or unexecuted CI was interpreted as pass.

## Act

RC7.9 is accepted as `PASS` and merged. The lesson is that timeout policy is only acceptable when cancellation, connector-local isolation, provenance and non-publication are proven by retained runtime evidence rather than configuration alone.

Phase 4 cannot yet be declared complete because issue #1 still requires external validation of live-connector credentials, provider-enforced rate limits, licences/terms and provider-specific production acceptance. Those checks cannot be proven from source code or CI alone. RBAC, separation of duties, privacy, auditability and human share approval remain unchanged.

## Exactly one next priority

Reconcile Phase 4 against the external live-connector validation gate in issue #1 and record the precise external dependency blocker; do not advance to Phase 5 unless credentials, provider limits, licences/terms and connector production acceptance are objectively evidenced.