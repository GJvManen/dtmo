# RUN-20260808-069 — RC8.1 exact-head acceptance and merge

Release gate: `PASS`

## Plan

Accept or reject PR #38 only from objective evidence on exact head `9e45a0e1f8991d42c841ebfa4e03b42fe64d4dbb`. Blocking conditions were any absent, queued, failed or cancelled required RC4/RC6/RC7 workflow, a moved PR head, or any attempt to treat the workload budgets as measured performance results.

## Do

- Re-read PR #38 metadata and confirmed it remained open, mergeable and unchanged at exact head `9e45a0e1f8991d42c841ebfa4e03b42fe64d4dbb`.
- Inspected all pull-request workflow runs associated with that exact head.
- Confirmed all required RC4/RC6/RC7 regression workflows completed successfully.
- Merged PR #38 with expected-head protection using squash merge. Merge commit: `67ff969554fbced6b2efbd0e84b7d050bd16c3cc`.
- No load generation or performance-result claim was added in this acceptance run.

## Check

All 12 required exact-head workflows completed with `conclusion=success`:

- RC4 Quality Gate #352;
- RC6 OpenSearch Recovery Gate #104;
- RC6 Multi-store Recovery Gate #94;
- RC7 Connector State Gate #44;
- RC7 Live Connector Canary Gate #85;
- RC7 Connector Contract Gate #56;
- RC7 Payload Provenance Gate #47;
- RC7 Connector Replay Gate #15;
- RC7 Connector Freshness Gate #11;
- RC7 Connector Failure Isolation Gate #7;
- RC7 Connector Retry Gate #4;
- RC7 Connector Timeout Gate #3.

No absent, configured-only, queued or unexecuted workflow was interpreted as PASS. RC8.1 remains a workload and acceptance-budget contract only; the latency, throughput and capacity thresholds are targets and not measured results.

## Act

RC8.1 is accepted as `PASS` and merged. The lesson is that Phase 5 load generation must be driven by a reviewable, versioned workload contract so later measurements cannot silently change the test envelope.

RBAC, separation of duties, privacy, provenance, auditability and mandatory human share approval remain unchanged. The external load/stress gate in issue #1 remains open because this run does not execute representative load.

## Exactly one next priority

Implement a bounded, synthetic API/read performance harness against the accepted Phase 5 workload profile, with machine-readable latency/error evidence and fail-closed acceptance evaluation. Do not yet add search, ingestion or degraded-dependency load in the same run.
