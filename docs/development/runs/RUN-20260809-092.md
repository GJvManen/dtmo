# RUN-20260809-092 — RC9.1 governed browser share-approval E2E

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective: add a browser-based end-to-end test for the highest-risk governed decision journey, intelligence review followed by separate share approval. Blocking gates are UI/backend permission inconsistency, any path that allows a reviewer to approve their own review, service-account review/share capability, missing browser execution, missing retained evidence, or any regression in RBAC, privacy, provenance, auditability or human share approval.

## Do

- Added `backend/dtmo/ui.py` with a minimal governed decision page and backend-derived session capabilities.
- Added the UI router to the FastAPI application.
- Added Playwright as a test-only dependency; it is not a production runtime dependency.
- Added `backend/tests/test_rc9_browser_share_approval.py`.
- Added `.github/workflows/browser-share-approval-e2e.yml` with PostgreSQL, Alembic migration, Chromium installation, real Uvicorn startup, browser execution, JUnit/server-log retention and machine-readable evidence.
- Added `docs/qa/RC9_1_BROWSER_SHARE_APPROVAL_E2E_GATE.md`.

The browser journey seeds one synthetic intelligence candidate. An admin principal reviews it, then attempts self-approval and must receive HTTP 409 from the existing governance boundary. A distinct publisher subsequently approves sharing. A service-account browser context must expose neither review nor share-approval controls. Final database state must prove distinct `reviewed_by` and `share_approved_by` principals.

## Check

No browser test or workflow has been treated as successful merely because it is committed. RC9.1 remains pending until GitHub Actions executes on the exact final PR head and retained browser evidence is independently inspected.

The implementation deliberately keeps permission visibility separate from resource-state separation of duties: an administrator can possess both permissions and therefore see both controls, but the backend must still reject self-approval of the same reviewed item. This tests the highest-risk mismatch rather than relying on UI hiding alone.

Threat/CVE/vendor review is scoped to the change: Playwright/Chromium are CI-only test components, no production dependency or live connector changed, and existing dependency/security scanning remains release-blocking for any material finding.

## Act

RC9.1 is `CI_VALIDATION_PENDING`. Phase 6 is `IN PROGRESS`, not PASS. No broad WCAG, responsive, keyboard or cross-browser claim is made by this run.

## Exactly one next priority

Inspect all exact-head workflows for the RC9.1 PR and independently inspect retained `browser-share-approval-evidence`; repair only the earliest deterministic failure, or accept/merge only when all required exact-head gates succeed.
