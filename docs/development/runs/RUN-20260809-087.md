# RUN-20260809-087 — RC8.7 exact-head evidence acceptance

Status: `CI_VALIDATION_PENDING`

## Plan

Objective: validate exactly one bounded Phase-5 objective: RC8.7 exact-head GitHub Actions plus independently retained concurrency-saturation evidence, then accept/merge only if the final documentation head is also fully green.

Blocking gates: all registered required workflows must complete successfully on the exact PR head; the retained JSON/JUnit artifact must be present, head-bound, unexpired and independently inspected; synthetic-only/privacy/governance invariants must remain fail-closed; no external representative production load/stress claim may be inferred.

## Do

Reviewed PR #46 head `adf18135e91c0e28c151f8255563aba69b8df008` and its GitHub Actions evidence. All 19 registered workflows completed with conclusion `success`, including `RC8 Concurrency Saturation Performance Gate` run `31293634918`.

Downloaded and independently inspected retained artifact `9032235183` (`concurrency-saturation-performance-evidence`, digest `sha256:66099a09e34099c3befc63918bdea0a8d0baf2302368138303eb6c96ccc1852d`). The artifact is explicitly bound to the same PR head.

Observed evidence: 20 maximum inflight operations at requested concurrency 20; 40 synthetic reads and 40 synthetic ingests in the scaled CI fixture; read p95 5.734 ms; 0.0% errors; 0 ingest data-loss records; 40/40 unique ingested records; publication disabled and publication state preserved. JUnit records 6 tests, 0 failures, 0 errors and 0 skipped.

The artifact explicitly records `external_load_gate_satisfied=false` and `representative_production_saturation_satisfied=false`.

This acceptance record changes the PR head, so the prior green execution is evidence for the implementation but cannot by itself authorize merge of the new exact documentation head. The final head must execute the registered workflows again before PASS/merge.

## Check

RC8.7 implementation evidence is successful and internally consistent. The acceptance boundary remains narrow: this is a regression-scale synthetic concurrency gate, not production-representative capacity assurance.

No dependency, connector, identity, threat-model or production configuration changed in this acceptance-only run. No new public threat/CVE/vendor advisory was material to the bounded decision; unrelated intelligence was not introduced into the release record.

## Act

Lesson: evidence acceptance documentation itself changes the review head and therefore must trigger a fresh exact-head validation before merge. Historical green evidence remains auditable but is never reused as final-head authorization.

Phase 5 remains `IN PROGRESS`; issue #1 external gates remain open.

## Exactly one next priority

Validate every registered workflow on the final PR #46 head created by this acceptance record; if all are successful, verify the retained RC8.7 evidence remains head-bound and merge PR #46 with expected-head protection, otherwise repair only the first deterministic failure.
