# RUN-20260808-073 — RC8.3 exact-head acceptance and merge

Release gate: `PASS`

## Plan

Accept or reject PR #40 only from objective evidence on exact head `78da9e8bc6ca6799bc6cf48d21ac79054bc9e8ae`. Blocking conditions were any absent, queued, failed, skipped or cancelled required RC4/RC6/RC7/RC8 workflow; missing retained search-read evidence; latency/error results outside the accepted RC8.1 budgets; loss of provenance; any returned or generated `publish_approved=true`; or evidence that misrepresented the scaled CI fixture as satisfying the external representative load/stress gate.

## Do

- Re-read PR #40 and confirmed the head remained `78da9e8bc6ca6799bc6cf48d21ac79054bc9e8ae`, open and mergeable.
- Inspected all GitHub Actions workflow runs associated with that exact head.
- Inspected RC8 OpenSearch Search Read Performance Gate run `31263541925` / run number `3`, including job and execution logs.
- Verified retained artifact `search-read-performance-evidence`, ID `9023474648`, digest `sha256:06b4f0c6bf7273f7e4a2c5967c5e0e0445315b6282588503791209bbae1c939f`, retained for 30 days.
- Verified the executed OpenSearch service was version `2.19.6` and the synthetic fixture contained 5,000 documents.
- Merged PR #40 with expected-head protection. Merge commit: `635a9736f9cb3b5091f00b99fc89eb47574858ae`.

## Check

All 14 required exact-head workflows completed successfully:

- RC4 Quality Gate #364;
- RC6 OpenSearch Recovery Gate #116;
- RC6 Multi-store Recovery Gate #106;
- RC7 Connector State Gate #48;
- RC7 Live Connector Canary Gate #97;
- RC7 Connector Contract Gate #68;
- RC7 Payload Provenance Gate #59;
- RC7 Connector Replay Gate #19;
- RC7 Connector Freshness Gate #15;
- RC7 Connector Failure Isolation Gate #11;
- RC7 Connector Retry Gate #8;
- RC7 Connector Timeout Gate #7;
- RC8 API Read Performance Gate #4;
- RC8 OpenSearch Search Read Performance Gate #3.

RC8.3 focused regression execution completed 6 tests with no failure before the bounded load step. Runtime search evidence recorded:

- `decision=pass`;
- 200 total requests, 200 successful, 0 failed;
- achieved rate 40.161 requests/second against a 40 requests/second target;
- error rate 0.0% against a maximum 1.0%;
- p50 4.528 ms;
- p95 7.700 ms against an 800 ms maximum;
- p99 12.131 ms against a 1,500 ms maximum;
- maximum observed latency 40.517 ms;
- provenance preserved: `true`;
- publication state preserved: `true`;
- load test may publish: `false`;
- external load gate satisfied: `false`.

The executed OpenSearch container was `opensearchproject/opensearch:2.19.6`. The test used an isolated CI service with the security plugin disabled; this is acceptable for the bounded internal search-read benchmark but is explicitly not evidence for production OpenSearch hardening.

No absent or unexecuted workflow was interpreted as PASS. The scaled 5,000-document CI fixture is not represented as the accepted 1,000,000-record representative envelope.

## Act

RC8.3 is accepted as `PASS` and PR #40 is merged. The lesson is that search performance evidence must preserve both workload-scale provenance and intelligence governance: a fast result is not acceptable if provenance or non-publication semantics are lost, and a scaled CI result must not be promoted to external production-scale assurance.

RBAC, separation of duties, privacy, security, auditability, provenance and human share approval remain unchanged. Issue #1's independent representative load/stress test and production OpenSearch-hardening gates remain open.

## Exactly one next priority

Implement a bounded synthetic ingestion-throughput performance harness driven by the accepted RC8.1 profile, measuring sustained ingestion correctness and throughput with zero data loss and zero duplicate candidate creation. Queue pressure, connector bursts and degraded-dependency testing remain outside that next objective.