# RUN-20260806-035 — Isolate production JWKS regression from external Redis

- **Started:** 2026-08-06 14:13 CEST
- **Workstream:** Phase 2 — application security and identity
- **Objective:** Resolve only the earliest deterministic Quality Gate #211 failure without weakening fail-closed production token-state enforcement.
- **Plan:** Preserve mandatory Redis-backed revocation/replay checks in production; replace the external Redis dependency only inside the pre-existing trusted-principal unit test with a controlled active-state double.
- **Do:** Updated `test_production_resolves_roles_only_from_jwks_validated_token` to monkeypatch `TokenStateStore.from_url` and assert the validated JTI reaches the state check before roles are accepted.
- **Check:** The reported failure was caused by DNS resolution of host `redis` in a unit-test job without a Redis service. No evidence showed a product-code defect. New exact-head CI execution is required; no PASS is claimed.
- **Act:** Keep fail-closed production behavior unchanged. Unit tests that exercise production authentication must explicitly provide token-state behavior rather than depend on infrastructure not declared by the test job.
- **Known blockers:** Exact-head Quality Gate after commit is pending.
- **Next action:** Inspect the first Quality Gate for the updated PR #16 head and resolve only its earliest deterministic failure, or merge after full success.
- **Release gate:** `BLOCKED`
