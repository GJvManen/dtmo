# RUN-20260809-153 — Phase 8 staging emulator runtime smoke

## Plan

Objective: execute exactly one bounded Phase 8 improvement after accepting PR #104 — prove that the repository-built DTMO application container can actually start in production configuration mode and pass a privacy-safe runtime smoke probe in CI.

Blocking gates: the previous emulator configuration/topology gate must first be accepted; this run must not be interpreted as a real staging deployment or as satisfying the ten deployment-parity evidence classes.

## Do

1. Verified PR #104 exact head `93d1a659b7b136546ffcf73102890f5d2d00ba84` completed 47/47 registered workflows successfully, including RC4 and the Phase 8 Staging Emulator Gate.
2. Independently inspected retained artifact `9045039742`, digest `sha256:959586b389579dfd37bda60eecdfb67e0251eaf4a78daed214986cefe771ce65`: exact-head machine-readable PASS and JUnit 4/4 with zero failures/errors/skips; all external/production claim-boundary values remained false.
3. Merged PR #104 with expected-head protection as `3c7a4b7f56e8d8a757541963bbd261fe42a7269c`.
4. Added `tools/phase8_staging_emulator_runtime_smoke.py` to probe a running DTMO container for `/health`, `/ready`, connector-disable behavior, security headers, correlation ID and Prometheus metrics, while emitting JSON and JUnit evidence.
5. Added `.github/workflows/phase8-staging-emulator-runtime.yml` to build the exact-head DTMO image, run it with `DTMO_ENVIRONMENT=production`, loopback-only exposure, read-only root filesystem, tmpfs, `no-new-privileges` and dropped capabilities, then retain privacy-safe runtime evidence.
6. Added regression tests and `docs/qa/PHASE8_STAGING_EMULATOR_RUNTIME_GATE.md`.

## Check

Pre-PR review confirms the runtime gate preserves human publication approval and human share approval, disables live connectors and AI analyst features, does not retain synthetic sensitive markers in logs, and binds evidence to the exact PR head.

This run does not execute the complete dependency topology. PostgreSQL, Redis, OpenSearch, object storage and the external TLS gateway are explicitly outside this bounded runtime-smoke objective. Therefore no real staging environment, deployment parity, Phase 8 completion or production acceptance is claimed.

Final decision for RUN-153: `CI_VALIDATION_PENDING` until the complete exact-head workflow matrix and retained `phase8-staging-emulator-runtime-evidence` are independently verified.

## Act

Lesson: a configuration-valid emulator is useful but does not prove the application container can start under production-only validation. Runtime smoke evidence should be independently observable and must remain separate from real staging deployment evidence.

Backlog: keep the ten deployment-parity evidence classes open; after this runtime gate is accepted, the next bounded objective is complete dependency-topology emulation or approved real staging provisioning, without treating CI emulation as external staging acceptance.

## Exactly one next priority

Verify every registered workflow on the RUN-153 exact PR head and independently inspect retained `phase8-staging-emulator-runtime-evidence`. Merge only on complete success.
