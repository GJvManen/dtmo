# RUN-20260807-047 — RC7.2 parent-before-child persistence ordering

- **Workstream:** Phase 4 — live connector reliability and provenance
- **Objective:** Remediate the earliest deterministic failure from RC7 Connector State Gate #13 by enforcing parent-before-child persistence for connector runtime state and health events.

## Plan

Blocking gates:

- the PostgreSQL-backed connector-state workflow must migrate successfully;
- connector-state unit and migration tests must execute successfully;
- a new connector runtime-state row must exist before any health-event row references it;
- quarantine and health records must remain non-publishable;
- exact-head Quality, Connector State, canary and recovery gates must execute successfully;
- retained connector-state evidence must exist before acceptance.

## Do

- inspected exact-head workflow run `31129850744` on head `1fea5a9b07fa21cf16476a723bb1ddd656d0b39e`;
- confirmed Alembic upgrade through `0005_connector_state` succeeded;
- confirmed all five targeted connector-state tests passed;
- isolated the first deterministic failure to PostgreSQL foreign-key ordering in the evidence fixture;
- added an explicit `Session.flush([state])` after creating or updating the parent `ConnectorRuntimeState` and before inserting `ConnectorHealthEvent` children;
- left connector isolation, quarantine recovery, RBAC, privacy, provenance and human share-approval policy unchanged.

## Check

Evidence from Connector State Gate #13:

- Quality Gate #288: `PASS`;
- Live Connector Canary Gate #21: `PASS`;
- OpenSearch Recovery Gate #40: `PASS`;
- Multi-store Recovery Gate #30: `PASS`;
- Connector State Gate #13: `FAIL`;
- migration step: `PASS`;
- targeted tests: `5 passed`;
- evidence fixture: `FAIL` with `ForeignKeyViolation` because `connector_health_events.connector_id = cisa-kev` was inserted before the new `connector_runtime_states` parent row;
- retained connector-state artifact: absent because the primary job failed before upload.

The remediation commit is `407fd75e149b50d28bb2830a17d26a877b09d9c4`. It is not acceptance evidence until exact-head workflows execute.

## Act

Lessons:

- database foreign keys require explicit flush ordering when ORM entities are linked only by scalar key values and no relationship dependency is declared;
- passing unit tests does not replace PostgreSQL-backed execution evidence;
- the independent fail-closed gate correctly rejected missing evidence.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** exact-head workflows and retained connector-state evidence have not yet completed for the remediation head.
- **Exactly one next priority:** inspect the RC7 Connector State Gate for the current exact head and remediate only its earliest deterministic failure, or merge PR #29 after every required exact-head gate and retained artifact succeeds.
