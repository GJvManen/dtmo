# RUN-20260807-060 — RC7.7 connector source-health and failure isolation

Date: 2026-08-07
Status: `CI_VALIDATION_PENDING`
Roadmap phase: Phase 4 — Live connector reliability and provenance

## Plan

Bounded objective: add an enforceable, connector-local execution health guard over the existing persisted connector health state so repeated upstream failures are bounded by circuit isolation, unrelated connectors remain independent, and health/recovery state can never grant publication approval.

Blocking gates for this run:

1. dedicated failure-isolation regressions must execute;
2. retained evidence must prove the failure threshold, open circuit, independent connector execution and recovery behavior;
3. health decisions/events must retain `publish_approved=false`;
4. all required exact-head RC4/RC6/RC7 regression workflows must succeed before acceptance or merge;
5. external connector credentials, provider-enforced limits, licences/terms and production acceptance remain open in issue #1.

## Do

Implemented:

- `backend/dtmo/connectors/health.py`
  - immutable `ConnectorExecutionDecision`;
  - connector-local health evaluation;
  - fail-closed `require_connector_execution` guard while a circuit is open;
  - explicit recovery-probe decision after isolation expiry;
  - hard invariant that operational health decisions cannot approve publication.
- `backend/tests/test_rc7_connector_failure_isolation.py`
  - repeated failures open a bounded circuit;
  - isolation remains scoped to the failing connector;
  - fail-closed guard behavior;
  - recovery after isolation expiry;
  - UTC handling for persisted timestamps;
  - invalid connector identifiers fail closed.
- `.github/workflows/connector-failure-isolation.yml`
  - dedicated observable gate;
  - JUnit test evidence;
  - deterministic JSON evidence;
  - 30-day retained artifact;
  - final gate fails closed if the evidence-producing job is missing or unsuccessful.
- `docs/qa/RC7_7_FAILURE_ISOLATION_GATE.md`
  - acceptance criteria, evidence contract and external-gate separation.

No RBAC, separation-of-duties, privacy, audit, quarantine or human share-approval behavior was weakened.

## Check

At documentation time, code and workflow definitions exist on branch `rc7-7-source-health-isolation`, but exact-head GitHub Actions evidence has not yet been observed. Therefore no test or CI PASS is claimed in this run record.

Threat/incident context reviewed:

- CISA historical K-12 reporting documents that a single vendor incident affected thousands of school websites, reinforcing the need to prevent one unhealthy upstream source from cascading into unrelated connectors. Confidence: high; primary government source.
- CISA resilience guidance recommends isolation of affected systems and continuity planning. Confidence: high; primary government source.
- No newly reviewed connector-specific CVE or vendor advisory changes the scope of this bounded reliability control. CVE/provider monitoring remains provenance-sensitive and separate from publication approval.

## Act

Lesson: persisted health state alone is insufficient unless connector execution has a fail-closed decision boundary. RC7.7 therefore adds an explicit execution guard while retaining connector-local failure domains and non-publication semantics.

Backlog/status:

- Phase 4 remains `IN PROGRESS`.
- RC7.7 remains `CI_VALIDATION_PENDING` until exact-head workflows execute and retained evidence is independently inspected.
- Issue #1 external production gates remain open.

## Exactly one next priority

Inspect the fresh exact-head RC7 Connector Failure Isolation Gate and all required RC4/RC6/RC7 regressions; remediate only the earliest deterministic failure, or accept and merge RC7.7 only when every required gate succeeds and retained failure-isolation evidence independently proves connector-local blocking, bounded recovery and `publish_approved=false`.
