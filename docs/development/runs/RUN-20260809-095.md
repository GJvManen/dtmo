# RUN-20260809-095 — RC9.1 exact-head CI validation and acceptance

Date: 2026-08-09
Roadmap phase: 6 — Frontend accessibility and operational UX
Objective: validate all required GitHub Actions workflows for PR #50 exact head `005512e124ff6c37a5acd3d2b8e4ba8c823d4a01`, independently inspect retained browser evidence, and merge only if every gate is satisfied.

## Plan

Blocking acceptance gates:
- all 20 registered pull-request workflows must complete successfully on the exact PR head;
- the RC9 Browser Share Approval E2E Gate must execute successfully in Chromium against PostgreSQL-backed DTMO;
- no pending or unexecuted workflow may be treated as PASS;
- retained browser evidence must be identity-bound to the exact head and independently inspected;
- RBAC, separation of duties, privacy, provenance, auditability and distinct human share approval remain mandatory;
- issue #1 external production gates remain independent and open.

## Do

PR #50 was intentionally kept unchanged while CI executed. The first evidence check showed 19 of 20 workflows completed successfully with RC4 Quality still in progress. A later exact-head re-check showed all 20 workflows `completed/success`, including RC4 Quality and the dedicated RC9 Browser Share Approval E2E Gate.

The retained RC9 artifact was fetched and independently inspected before merge.

Artifact: `browser-share-approval-evidence`, ID `9036392289`, digest `sha256:111d879e048f5978927472da996020f398448dd0752407f60a3366dbfbbf0fd6`, workflow run `31307320689`, head SHA `005512e124ff6c37a5acd3d2b8e4ba8c823d4a01`.

Machine-readable evidence states `decision=pass`, Chromium execution, reviewer self-approval blocked, distinct publisher approval succeeds, service-account approval controls hidden, UI permissions backend-derived, and human share approval required.

JUnit records 1 browser test, 0 failures, 0 errors and 0 skips. Server logs show health 200, UI/session 200, review POST 200, same-principal share-approval POST 409, distinct publisher share-approval POST 200, and service-account UI/session access without approval action.

After all exact-head evidence was verified, PR #50 was squash-merged with expected-head protection as `ef59eba29d7fa8b2d88b5674e7bb00e98c0dab18`.

## Check

- 20/20 workflows: `completed/success` on exact head;
- RC4 Quality: `completed/success`;
- RC9 Browser Share Approval E2E Gate: `completed/success`;
- retained artifact exact-head binding verified;
- browser evidence decision: PASS;
- JUnit: 1 test, 0 failures/errors/skips;
- self-approval denial observed as HTTP 409;
- distinct publisher approval observed as HTTP 200;
- expected-head merge succeeded.

RC9.1 decision: `PASS`.

Phase 6 remains `IN PROGRESS`; RC9.1 alone does not satisfy responsive, keyboard, WCAG 2.2 AA, supported-browser breadth, error/loading/empty-state coverage or additional analyst/CISO/audit journey gates.

## Threat, CVE and vendor context

This acceptance run made no new dependency or runtime change. RC4 dependency-review completed successfully on the accepted exact head. No newly identified threat-intelligence, historical-incident, CVE or vendor-advisory finding materially altered this bounded gate decision.

## Act

Lesson: identity-bound CI must remain stable while evidence is collected. Audit documentation was kept separate from PR #50 during validation and reconciled only after the product merge.

Issues #1, #2 and #3 are coordinated without closing any external production gate.

Decision: `PASS`.

Exactly one next priority: Phase 6 / RC9.2 — add one bounded critical analyst browser journey, including explicit error/loading/empty-state behavior and backend RBAC consistency, while leaving responsive, keyboard and WCAG-wide acceptance for later Phase-6 objectives.