# RUN-20260808-071 — RC8.2 exact-head acceptance and merge

Release gate: `PASS`

## Plan

Accept or reject RC8.2 only from exact-head evidence for PR #39 head `e8ab9132bab6da753087d4cc830bac6541eb99ff`. Blocking conditions were any missing, queued, failed, cancelled or skipped release-critical workflow; absent retained performance evidence; latency/error results outside the accepted RC8.1 budgets; missing governance markers; or a moved PR head.

## Do

- Re-read PR #39 and confirmed it remained open, mergeable and unchanged at exact head `e8ab9132bab6da753087d4cc830bac6541eb99ff` before acceptance.
- Inspected all GitHub Actions workflow runs associated with that exact head.
- Retrieved retained artifact `api-read-performance-evidence`, artifact ID `9022168980`, digest `sha256:ca930454d8795be70844ba22befe2ff4420c8c7002289b2c97c979bd9f889d30`, from `RC8 API Read Performance Gate` run `31258791875` / run number `1`.
- Independently inspected the retained JSON, JUnit XML and API server log.
- Merged PR #39 with expected-head protection as squash commit `13fdadcfa83170b64713f3e72f7261501829e585`.

## Check

All 13 required exact-head workflows completed successfully:

- RC4 Quality Gate #357;
- RC6 OpenSearch Recovery Gate #109;
- RC6 Multi-store Recovery Gate #99;
- RC7 Connector State Gate #45;
- RC7 Live Connector Canary Gate #90;
- RC7 Connector Contract Gate #61;
- RC7 Payload Provenance Gate #52;
- RC7 Connector Replay Gate #16;
- RC7 Connector Freshness Gate #12;
- RC7 Connector Failure Isolation Gate #8;
- RC7 Connector Retry Gate #5;
- RC7 Connector Timeout Gate #4;
- RC8 API Read Performance Gate #1.

Retained evidence reports `decision=pass` for profile `education-sector-phase5-v1`. The bounded run executed 500 requests against `http://127.0.0.1:8765/health`, with 500 successful requests, 0 failures, achieved throughput `100.142` requests/second, error rate `0.0%`, p50 `1.636 ms`, p95 `1.878 ms`, p99 `11.059 ms`, and maximum `14.67 ms`. Accepted budgets were p95 <= 300 ms, p99 <= 750 ms and error rate <= 1%.

Governance evidence confirmed `authentication_boundary_reported=true`, `publication_gate_preserved=true` and `load_test_may_publish=false`. JUnit reports 6 tests with 0 failures, 0 errors and 0 skips. The server log shows successful HTTP 200 execution against the real FastAPI process.

No absent or unexecuted CI was interpreted as PASS. This internal bounded API-read test does not satisfy issue #1's independent representative load/stress gate.

## Act

RC8.2 is accepted as `PASS` and merged. The lesson is that performance acceptance must bind measured latency/error evidence to the exact code head while preserving security and human-publication invariants; workflow success alone is insufficient without retained measurements.

RBAC, separation of duties, privacy, provenance, auditability and human share approval remain unchanged.

No objective-specific public threat-intelligence, education-incident, CVE or vendor-advisory update materially changed this bounded API-read acceptance decision.

## Exactly one next priority

Implement a bounded synthetic OpenSearch/search-read performance harness driven by the accepted RC8.1 workload profile, with exact-head retained latency/error evidence and fail-closed governance evaluation. Ingestion, queue pressure, connector bursts and degraded-dependency testing remain out of scope for that run.
