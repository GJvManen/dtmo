# RUN-20260809-080 — Apache-2.0 and open-source governance baseline

## Plan

Execute exactly one bounded documentation/governance objective: establish a correct Apache License 2.0 project baseline and independently observable regression protection. Do not advance RC8.5 or change application behavior.

Blocking gates:

- no existing root `LICENSE` was present on `main` at run start;
- project licensing must not be confused with third-party dependency/data/provider rights;
- exact-head CI must execute; configured or queued workflows are not PASS;
- external assurance and provider-specific licence/terms gates in issue #1 remain independent.

## Do

Added:

- root `LICENSE` with the complete Apache License, Version 2.0 text;
- root `NOTICE`;
- `SECURITY.md`;
- `CONTRIBUTING.md`;
- `CODE_OF_CONDUCT.md`;
- `SUPPORTED_VERSIONS.md`;
- `docs/legal/LICENSING.md`;
- `docs/legal/THIRD_PARTY.md`;
- `backend/tests/test_open_source_governance.py`;
- `.github/workflows/open-source-governance.yml`;
- `docs/qa/OPEN_SOURCE_GOVERNANCE.md`.

Updated:

- `pyproject.toml` with SPDX project licence metadata `Apache-2.0`;
- `README.md` with licensing and governance entry points.

The licensing policy follows official Apache guidance that Apache-2.0 can be used for non-ASF software and that a distribution should include the licence text and appropriate notice information. The project policy explicitly avoids relicensing dependencies, feeds, CVE/advisory content, trademarks or other external material.

## Check

Pre-PR repository inspection established that `main` had no root `LICENSE`. The existing project metadata also did not declare a project licence.

Focused regression protection now checks required governance-file presence, canonical Apache-2.0 markers, Python package SPDX metadata, README entry points and the third-party non-relicensing boundary.

A dedicated `Open Source Governance Gate` retains JUnit and governance-manifest evidence and fails closed if its evidence job does not succeed.

No exact-head CI result is claimed in this run record until the pull request exists and workflows execute. Threat-intelligence, historical-incident, CVE and vendor-advisory review was not materially required for this licensing-only objective because no runtime dependency version, connector, threat model or security implementation changed.

## Act

Lesson: project licensing and third-party content rights are separate release controls. Apache-2.0 is suitable as DTMO's project licence, but a production distribution still requires version-specific dependency/SBOM and provider licence/terms evidence.

Current decision: `CI_VALIDATION_PENDING`.

Exactly one next priority: validate this Apache-2.0/open-source-governance change on exact-head CI and inspect retained governance evidence; merge only after every required gate succeeds. After acceptance, return to RC8.5 / PR #42 as the sole Phase-5 product priority.
