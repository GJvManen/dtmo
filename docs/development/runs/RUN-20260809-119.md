# RUN-20260809-119 — RC9.14 WCAG 2.2 Text Spacing Evidence

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective: establish retained WCAG 2.2 SC 1.4.12 Text Spacing evidence on the four accepted critical surfaces. PASS is blocked unless the exact final PR head has every registered workflow completed successfully and the dedicated retained artifact is independently inspectable.

Normative basis: W3C WCAG 2.2 SC 1.4.12 requires no loss of content or functionality when line height is set to at least 1.5x font size, paragraph spacing to at least 2x font size, letter spacing to at least 0.12x font size, and word spacing to at least 0.16x font size. W3C F104 identifies clipped or overlapped content after spacing adjustment as a failure mode.

## Do

Added `backend/tests/test_rc9_14_text_spacing.py` and `.github/workflows/browser-text-spacing.yml`. The Chromium gate applies all four spacing overrides simultaneously, checks for clipped visible text inside `main`, and verifies governed critical controls remain visible and focusable. Exact-head JSON, JUnit and server logs are retained.

RBAC remains backend-derived from `/api/v1/ui/session`; separation of duties, auditability and separate human share approval are unchanged. No production data, credentials or business mutation are used.

## Check

Implementation is present but CI has not yet been accepted. Missing, queued, failed, cancelled or unexecuted workflows are not PASS. No assistive-technology or product-wide WCAG claim is made.

Threat/advisory review: no new public threat intelligence, education-sector incident, CVE or vendor advisory was identified as relevant to this narrowly scoped rendering/accessibility objective; the current normative W3C criterion is the material external source.

## Act

Phase 6 remains `IN PROGRESS`. Remaining gaps after this implementation are complete focus-order evidence and genuine assistive-technology behavior, plus acceptance of this text-spacing gate itself.

## Exactly one next priority

Verify every registered workflow on the final RC9.14 PR head and independently inspect retained `browser-text-spacing-evidence`; repair only the first deterministic failure, or merge only after complete successful exact-head evidence.
