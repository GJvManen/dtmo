# RUN-20260828-384 — R5 intelligence type analytics

Status: **CI_VALIDATION_PENDING**

PR: **#384 — Recovery: add canonical intelligence type analytics**

Base: `main` at `a3ba714d48f774285188c5f1f0dccfedfaa35896` after accepted merge of #383.

## Objective

Continue the active R5 cross-workspace statistics and trends recovery with one bounded persisted-data statistic: canonical intelligence type distribution.

## Repository-controlled change

- group canonical `IntelligenceItem.item_type` values from persistence;
- preserve the complete canonical `IntelligenceType` vocabulary, including explicit zero counts only when the datastore is available;
- expose `type_distribution` through the existing read-only Command Center snapshot;
- render the distribution in Visual Analytics with both chart and table equivalents;
- preserve unavailable/empty fail-closed behavior when canonical persistence cannot be read;
- synchronize the existing Phase 11 Command Center fail-closed contract and the R5 roadmap.

## Security and authority boundary

This slice does not add or weaken mutation authority, connector execution, RBAC, provenance, server-side credentials, human review, sharing approval, publication authority or responder authority. Persisted type counts do not prove live connectivity, source reachability, local exposure, upstream availability, compromise, review completion, staging behavior or production behavior.

## Acceptance boundary

Repository CI may demonstrate exact-head build and deterministic contract behavior only. It does not establish production-equivalent operation, owner functional acceptance, penetration-test results, independent assurance or production authorization.

The PR remains draft and `CI_VALIDATION_PENDING` until every workflow registered for the final exact head is `completed/success`. Any new commit invalidates prior exact-head evidence.
