# RUN-20260810-155 — Phase 8 staging-emulator runtime smoke fresh-base remediation

## Plan

Execute exactly one bounded objective: preserve the already-started PR #105 application-container runtime-smoke work on current `main`, resolve its deterministic RC4 lint failure, and eliminate stale-base documentation conflicts without changing the runtime claim boundary.

Blocking gates:
- PR #106 must first be accepted on complete exact-head CI;
- no failed, stale or unexecuted workflow is treated as PASS;
- the runtime probe must be restricted to loopback HTTP targets;
- runtime evidence must remain privacy-safe and exact-head bound;
- real staging and all ten deployment-parity evidence classes remain independently blocking.

## Do

Verified PR #106 exact head `ff0a490e46c2f9529441d8a5294030af498dbe14` completed 47/47 workflows successfully and merged with expected-head protection as `b57a6daa775d2f1f88a2d1b67b191da757fa743f`.

Inspected existing PR #105. Its exact head `51607417c6bc3c64c5bb9fbc1221a0e1e0e48ba2` executed the dedicated `Phase 8 Staging Emulator Runtime Gate` successfully, but `RC4 Quality Gate` failed during Ruff before type-check/tests because S310 flagged URL request construction in `tools/phase8_staging_emulator_runtime_smoke.py`. PR #105 was also stale/conflicted against current `main` after PR #106 merged.

Created fresh-base branch `feat/run-20260810-155-staging-runtime-rebase` from current `main`, ported the bounded runtime-smoke workflow/test/QA/probe, and strengthened the probe with explicit URL parsing that rejects any non-HTTP or non-loopback host before request construction. The narrow S310 suppression now applies only after that runtime validation.

No application runtime behavior, RBAC model, publication authority, connector enablement, production secret, privacy control or external staging claim was expanded.

## Check

Observed prior PR #105 evidence:
- all workflows except RC4 succeeded;
- dedicated runtime gate succeeded;
- RC4 test job stopped at Ruff S310;
- type check, unit tests and compile in that RC4 job were therefore unexecuted and are not accepted;
- the failure was deterministic and confined to the probe helper's URL-construction lint contract.

The fresh-base RUN-155 branch has not yet completed exact-head CI. Therefore this run remains `CI_VALIDATION_PENDING`.

Public-threat/CVE review was not expanded in this remediation because no production dependency or deployed platform changed. Deployment-time security/CVE/vendor-advisory review remains required as deployment-parity evidence class 10 for a real staging deployment.

## Act

Lesson: bounded security probes must encode their network scope in executable validation, not only in comments or workflow topology. Stale parallel PRs should be rebased onto the authoritative documentation baseline rather than merged with conflicting lifecycle records.

Backlog effect:
- RUN-153/RUN-154 documentation reconciliation: accepted through PR #106;
- PR #105: superseded by RUN-155 fresh-base remediation;
- Phase 8 runtime-smoke gate: `CI_VALIDATION_PENDING` on RUN-155;
- Phase 8 real staging deployment-parity gate: still `BLOCKED_EXTERNAL`;
- issue #1 external gates: unchanged.

## Exactly one next priority

Verify every registered workflow on the RUN-155 exact final PR head and independently inspect retained `phase8-staging-emulator-runtime-evidence`. Merge only on complete success.
