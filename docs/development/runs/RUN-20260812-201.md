# RUN-20260812-201 — RC13 post-#163 status reconciliation

Date: 2026-08-12

## Purpose

Record the current release decision after PR #163 completed exact-head CI and merged. This run does not rewrite RUN-200; RUN-200 remains immutable point-in-time evidence from before CI completed.

## Trigger

The prior accountable owner retest confirmed that the PR #160 runtime-packaging and PR #161 Grafana datasource-provisioning startup defects no longer blocked the observed local path. The same run then exposed a supported source-catalog bootstrap HTTP 500 caused by an internal secret-reference contract mismatch.

PR #163 repaired that repository defect.

## PR #163 evidence

- title: `RC13: repair source catalog secret-reference bootstrap contract`
- final exact head: `4198f06e360929d3937065b8528237741cbe189a`
- every returned workflow on that exact head: `completed/success`
- dedicated gate: `RC13 Source Catalog Bootstrap Gate` — `completed/success`
- actual merge commit: `adc027143f1274c604a16446fe1ad2bdc7bc835f`
- merge used expected-head protection

The repair centralizes logical source secret-reference validation, accepts executable `env:VARIABLE`, normalizes legacy `env://VARIABLE`, preserves external secret-manager references, rejects raw secret values and regression-tests complete supported catalog bootstrap for idempotency and disabled-by-default registration.

## Owner-evidence boundary

The earlier owner run is sufficient to mark the bounded #160/#161 startup failures as no longer observed on that path. It is not sufficient to close RC13 because the owner has not yet retested the newly merged #163 behavior or completed the remaining functional acceptance journey.

The prior MinIO `InvalidAccessKeyId` is not classified as a repository defect because the owner explicitly identified the local `.env` as incorrect and asked to skip that point.

## Current decision

- Phases 1–7: `PASS`
- PRs #159, #160, #161, #163: repository-controlled repair `PASS`
- RC13: `AWAITING_OWNER_RETEST_AFTER_REPAIR`
- Phase 8: `PAUSED_PENDING_RC13_OWNER_RETEST`
- Phase 9: `NOT COMPLETE`
- Phase 10: `NOT STARTED`
- DTMO: not production ready

## Exactly one next priority

Accountable project-owner local Compose and functional console retest of current merged `main` under issue #150.

## Acceptance rule

Do not convert repository CI or synthetic/runtime evidence into accountable owner acceptance. RC13 closes only after explicit owner acceptance of current merged product behavior.
