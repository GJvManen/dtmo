# RUN-20260810-167 — PR #112 visual accessibility evidence-scope remediation

## Status

`CI_VALIDATION_PENDING`

## Plan

Bounded objective: restore the final two RC9 workflow contracts on PR #112 by correcting visual accessibility evidence collection so intentionally screen-reader-only labels are not misclassified as visible clipped text.

Blocking gate: PR #112 exact head `b33f270b201527249f847107863ee1184954f352` completed 46/48 registered workflows successfully. The only failed workflows were RC9 Text Spacing Accessibility Gate and RC9 Text Resize Accessibility Gate. Each workflow also failed its fail-closed aggregate job, producing four failed checks in the GitHub UI.

Security/claim boundary: do not remove accessible labels, do not weaken production CSP, RBAC, separation of duties, privacy, append-only auditability or human share approval. No external Phase 6, Phase 8, Phase 9 or production acceptance claim is advanced.

## Do

Inspected the exact failing workflow jobs and decoded logs.

Observed common root cause:

- RC9 Text Spacing reported `clippedText: ['label']` on the Analyst surface.
- RC9 Text Resize reported `label-6` with `client_width=1`, `scroll_width=236`, `client_height=1`, `scroll_height=29` on the Analyst surface.
- The element is the deliberate `.sr-only` label for the intelligence search input. It remains available to assistive technology but is visually hidden by design.

Remediation:

1. `backend/tests/test_rc9_12_text_resize.py`
   - visual text selector now uses `label:not(.sr-only)`;
   - comment documents that AT-only labels remain in the accessibility tree and are outside visual resize/clipping geometry evidence.
2. `backend/tests/test_rc9_14_text_spacing.py`
   - visual clipping scan explicitly skips `.sr-only` elements;
   - comment records the same visual-versus-AT evidence boundary.

No production UI, authorization, CSP, API or data-handling code was changed in this run.

## Check

Actual prior-head evidence:

- exact head: `b33f270b201527249f847107863ee1184954f352`;
- registered workflows: 48;
- successful workflows: 46;
- failed workflows: 2;
- RC9 Reflow: success;
- RC9 Contrast: success;
- RC9 Text Spacing: failure due to visually hidden `.sr-only` label being treated as visible clipped text;
- RC9 Text Resize: failure due to the same `.sr-only` label being treated as visible 1px text geometry.

The remediation is committed, but a fresh full exact-head workflow matrix is required. Unexecuted CI is not PASS.

## Act

Lesson: visual geometry evidence must distinguish intentionally AT-only content from rendered visual text. Screen-reader-only labels must remain present for accessible naming; tests should not force product code to expose or remove them merely to satisfy visual clipping measurements.

Backlog/claim update: RUN-166 is `FAILED_CI` at 46/48. RUN-167 is `CI_VALIDATION_PENDING` until all 48 registered workflows complete successfully on one final exact head.

## Exactly one next priority

Verify all 48 registered workflows on the final RUN-167 exact head of PR #112. Merge only on complete exact-head success; if any workflow fails, remediate only the first concrete remaining root cause.
