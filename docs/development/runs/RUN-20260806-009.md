# RUN-20260806-009 — Deterministic CI evidence pair verification

- **Started:** 2026-08-06 05:43 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Add one deterministic, reviewable verifier that rejects incomplete, failed or mismatched primary and observer CI evidence.

## PLAN

- Preserve the rule that configured or committed CI is not proof of execution.
- Validate the immutable identity relationship between `workflow-contract-evidence` and `ci-observation-evidence`.
- Block release progression when workflow, run ID, head SHA, conclusion or canonical URL differ.
- Keep the verifier independent of GitHub API availability so downloaded artifacts can be checked locally or in a later release job.

## DO

- Added `tools/verify_ci_evidence.py`.
- Added `backend/tests/test_ci_evidence_verifier.py`.
- The verifier requires a successful `RC4 Quality Gate`, a positive run ID, a full lowercase commit SHA and a canonical repository/run URL.
- The observer record must refer to the same workflow, run ID, commit SHA, successful conclusion and URL.
- Invalid JSON, missing fields, failed runs and identity mismatches return a non-zero result.

## CHECK

- Static inspection confirms the verifier and positive/negative test cases are committed.
- Test cases cover matching evidence, unsuccessful primary execution, malformed SHA, non-canonical URL, run-ID mismatch, SHA mismatch and conclusion mismatch.
- No successful GitHub Actions execution, test result or downloadable artifact is observable for this run; automated tests are therefore `PENDING`, not `PASS`.

## ACT

- Phase 1 remains blocked until real primary and observer artifacts are available and pass the verifier.
- The verifier provides the deterministic acceptance control needed once those artifacts exist.

## QA decision

- **Bounded implementation:** `BLOCKED` pending executed test evidence.
- **Phase 1:** `BLOCKED`.
- **Release:** `CI VALIDATION PENDING`.
- **External blocker:** GitHub Actions execution/workflow enablement remains unverified.

## Exactly one next priority

Obtain one completed `RC4 Quality Gate` run and one matching `RC4 CI Observer` run, download both artifacts, and execute `python tools/verify_ci_evidence.py <primary-json> <observer-json>` against them.
