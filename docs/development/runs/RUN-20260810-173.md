# RUN-20260810-173 — rc9 stale admin-version regression remediation

Status: `CI_VALIDATION_PENDING`

## Plan

Bounded objective: remediate the single concrete root cause behind the two red checks on PR #115 exact head `d7b51cc9aace6f87cfe34b8aeb50be59e15b800a` without changing product behavior, security boundaries or source-execution semantics.

Blocking gate: RC4 Quality Gate must pass on a new exact head. The aggregate RC4 `release-gate` is expected to remain red whenever its required `test` job is red and is not treated as an independent product defect.

## Do

The RC4 test job showed lint and mypy passing, then one pytest failure in `backend/tests/test_rc8_admin_source_registry.py::test_admin_workspace_is_wired_and_accessible`. The rc8 regression test hard-coded `version="16.0.0rc8"` in `main.py`, so advancing the application to rc9 caused a false regression even though the rc8 admin workspace wiring remained present.

The test has been narrowed to its intended contract: it still verifies admin router wiring, application construction, skip-link/status accessibility, source API wiring and session-scoped local identity, but no longer freezes the repository-wide application version at rc8.

No production code, RBAC, SSRF controls, connector execution, provenance, auditability, review/share approval or external gate claim was changed.

## Check

Evidence from exact head `d7b51cc9aace6f87cfe34b8aeb50be59e15b800a`:

- 47 of 48 registered workflows succeeded.
- RC4 Quality Gate failed.
- Inside RC4, lint succeeded.
- Inside RC4, mypy succeeded with no issues in 68 source files.
- pytest stopped after one failure: the stale rc8 version assertion; 259 tests had passed before that failure.
- RC4 `release-gate` failed only because the required `test` job failed.

A new commit `a36f04edf89d0d65bc738543ccd3173cacca1714` changes only that stale regression contract. Complete exact-head CI evidence for the new head is not yet available; therefore no PASS is claimed.

## Act

Lesson: release-specific regression suites must protect the capability introduced by that release, not pin unrelated global release metadata. Version identity belongs in release/version consistency checks, not feature-wiring tests from an earlier release.

Backlog unchanged: graphical dashboard/framework integration remains deferred until rc9 exact-head CI is fully green.

## Exactly one next priority

Verify every registered workflow on the new PR #115 exact head. Merge only if all required workflows succeed; otherwise remediate only the first concrete remaining root cause.
