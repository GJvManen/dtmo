# RUN-20260808-068 — RC8.1 Phase 5 workload profile

Release gate: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 5 objective: define the representative education-sector CTI workload and measurable performance/capacity budgets before implementing load generation.

Blocking conditions are any attempt to claim measured performance without execution evidence; missing privacy/governance constraints; thresholds that cannot be machine-tested; use of production personal data; or expansion into actual load/stress execution within this run.

## Do

- Re-read the production roadmap, current Phase 4 acceptance status, issue #3 and the DTMO application stack.
- Added `config/performance/phase5_workload_profile.json` as a machine-readable workload/budget contract.
- Added `docs/performance/PHASE5_WORKLOAD_PROFILE.md` with dataset envelope, traffic envelope, latency/throughput/resource budgets, required future scenarios and evidence requirements.
- Added `backend/tests/test_performance_workload_profile.py` to regression-protect privacy, governance, workload-cardinality and threshold invariants.
- Added `docs/qa/RC8_1_PERFORMANCE_WORKLOAD_GATE.md`.
- Preserved fail-closed governance: load fixtures are synthetic, RBAC remains active, review/share approval remain separated, service accounts cannot approve sharing and performance success cannot imply publication approval.

## Check

The machine-readable contract defines:

- 250 education organisations and 200 analyst/CISO/audit users, with 50 peak concurrent users;
- 1,000,000 intelligence records, 500,000 vulnerability records, 5,000,000 IOCs, 20,000,000 graph edges, 2,000,000 raw-evidence objects and 500 GiB raw-evidence capacity;
- 100 API reads/s, 25 API writes/s, 40 searches/s, 10 dashboard loads/s, 100 records/s sustained ingestion and 250 records/s burst ingestion for 10 minutes;
- API-read p95 <= 300 ms and p99 <= 750 ms;
- search p95 <= 800 ms and p99 <= 1,500 ms;
- dashboard p95 <= 1,500 ms;
- <= 1.0% error rate, zero data loss and zero duplicate candidate creation;
- bounded average CPU/memory/heap budgets and <= 900 seconds queue recovery after burst.

These are synthetic acceptance targets, not observed performance results. The new tests are committed but have not yet been accepted as executed until GitHub Actions reports exact-head results. Therefore no performance gate is marked PASS.

No new public CVE, historical education incident or vendor advisory materially changes this workload-definition objective; threat-intelligence content is intentionally decoupled from synthetic capacity cardinalities. Provenance requirements remain mandatory for later performance fixtures.

## Act

RC8.1 remains `CI_VALIDATION_PENDING`. The key lesson is that Phase 5 needs a stable workload and budget contract before test tooling is built, otherwise later benchmark results are not comparable or release-gateable.

Phase 5 remains `IN PROGRESS`; the external load/stress gate in issue #1 remains open because this run defines targets only and performs no external load test.

## Exactly one next priority

Inspect exact-head CI for the RC8.1 PR. If all required regression gates succeed, accept and merge this workload contract; otherwise remediate only the earliest deterministic failure. Do not implement load generation in the acceptance run.
