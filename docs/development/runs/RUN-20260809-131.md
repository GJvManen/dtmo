# RUN-20260809-131 — supported object-storage remediation blocker

## Status

`BLOCKED_EXTERNAL`

## Plan

Bounded objective: verify whether the higher-severity MinIO affected-version blocker can be remediated with a currently supported/patched object-storage runtime without weakening production-readiness gates.

Blocking gates:
- replacement must be currently supported and security-maintained;
- known affected MinIO legacy container releases must not be substituted merely to obtain a newer tag;
- S3 compatibility, persistence, recovery, security and storage-integrity behavior must remain re-testable;
- missing runtime/support evidence is not PASS.

## Do

Verified `main` after PR #87 merged as `4af2f1ceb24ead103690584473118db738f169d3`.

Verified `docker-compose.yml` still pins `minio/minio:RELEASE.2025-07-23T15-54-02Z`.

Reviewed current upstream MinIO primary sources. The upstream `minio/minio` repository is archived and explicitly marked no longer maintained. Its community edition documentation states that legacy precompiled binary/container releases are historical and will not receive updates; source builds remain possible but are not a maintained production support channel. Upstream points users to AIStor Free / AIStor Enterprise as successors, but this run did not find repository-local entitlement, deployment contract, image pin, or production support evidence sufficient to perform a reviewable migration safely.

No runtime substitution was made. In particular, this run did not replace the vulnerable pin with another legacy MinIO image or an unsupported source build, because that would remove neither the support-lifecycle blocker nor the need for recovery/security regression evidence.

## Check

Evidence:
- PR #87 exact documentation head `036e3a6035794bb115d578919327f0d87fa1c596` completed 37/37 registered workflows successfully before protected merge.
- Merge commit: `4af2f1ceb24ead103690584473118db738f169d3`.
- Current `docker-compose.yml` still contains the affected MinIO 2025-07-23 runtime pin.
- Current upstream MinIO repository status is archived/no-longer-maintained; legacy binary releases are not maintained.

Decision: `BLOCKED_EXTERNAL` for the remediation objective. A patched-but-unsupported community build is insufficient for a production-readiness support gate. Configuration-specific exploitability of individual CVEs remains bounded and is not overstated.

## Threat-intelligence provenance and confidence

Primary sources reviewed on 2026-08-09:
- MinIO upstream GitHub repository / README and Security pages;
- MinIO upstream releases and current maintenance guidance.

Confidence:
- affected-version match for the repository pin: high;
- upstream maintenance-status finding: high;
- successor suitability for DTMO production use: not yet evidenced; no acceptance claim made.

## Act

Lessons:
- vulnerability remediation and lifecycle support are separate production gates;
- replacing an affected image with an unmaintained but patched build would create false assurance;
- object-storage successor selection requires explicit operational/support acceptance before implementation.

Backlog impact:
- Phase 7 remains `IN PROGRESS` but normal RC10.5 work stays deferred behind this higher-severity blocker;
- Phase 6 remains independently `BLOCKED_EXTERNAL` for genuine VoiceOver/NVDA evidence;
- issue #1 external production gates remain open.

## Exactly one next priority

Obtain and record an explicit supported object-storage target for DTMO (including supported product/image or deployment method, lifecycle/support source, and any required entitlement/credential boundary). Once that evidence exists, perform one bounded migration implementation and rerun security, recovery, storage-integrity and full regression gates on its exact head.