# RUN-20260809-123 — RC10.1 request observability baseline

## Status

`CI_VALIDATION_PENDING`

## Plan

Phase 6 remains blocked only by genuine assistive-technology execution on real supported VoiceOver and NVDA hosts. That external dependency does not prevent internally actionable Phase 7 work.

Execute exactly one bounded Phase 7 objective: establish trustworthy HTTP request observability with safe correlation IDs, structured request logs and bounded Prometheus request metrics.

Acceptance requires exact-head CI plus retained evidence. Missing, queued, cancelled, failed or unexecuted CI is never `PASS`.

## Do

Created branch `rc10/rc10-1-request-observability` from `main` and implemented:

- bounded correlation-ID validation/replacement;
- explicit binding of correlation ID and HTTP method into `structlog.contextvars`;
- structured request-completion and request-failure events;
- route-template-labelled HTTP request counters and latency histograms instead of raw-path labels;
- HTTP in-flight request gauge;
- regression tests covering safe/unsafe correlation IDs, structlog context, correlated completion logs and route-template metrics;
- dedicated `RC10 Request Observability Gate` with exact-head JUnit, pytest log and machine-readable retained evidence;
- RC10.1 QA contract.

No business mutation, production credential or production data was added or used.

## Check

Source inspection identified two concrete observability gaps in the accepted baseline:

1. the request correlation ID was stored in a standalone Python `ContextVar`, while the configured processor consumes structlog context variables; therefore correlation was not reliably present in structured logs;
2. Prometheus request metrics used raw URL paths as labels, which can produce high-cardinality time series for dynamic paths.

The branch contains regression evidence for both gaps, but this run is not accepted until every required workflow succeeds on the final exact head and retained `request-observability-evidence` is independently inspected.

The current execution environment cannot clone GitHub directly for a separate local run, so no local test result is claimed. GitHub Actions remains authoritative.

## Act

Open an RC10.1 pull request and keep its status `CI_VALIDATION_PENDING` until exact-head workflow and retained artifact inspection are complete.

Phase 6 remains `BLOCKED_EXTERNAL`; RC10.1 does not alter or close the VoiceOver/NVDA requirement.

## Claim boundary

This run does not claim distributed tracing, dependency alerting, operational dashboards, runbook exercise, on-call readiness or Phase 7 completion.

## Exactly one next priority

Inspect all required workflows on the final RC10.1 pull-request head and independently inspect retained `request-observability-evidence`; repair only the first deterministic failure, or accept and merge only after complete successful evidence.
