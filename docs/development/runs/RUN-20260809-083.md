# RUN-20260809-083 — RC8.5 exact-head acceptance and merge

## Plan

Objective: accept or reject exactly one bounded Phase 5 objective, RC8.5 queue pressure and connector burst performance, using only current exact-head CI and independently inspected retained evidence.

Blocking gates:

- every required GitHub Actions workflow must complete successfully on PR #42 exact head;
- retained queue-burst JSON and JUnit evidence must exist on that exact head and be independently inspected;
- observable backpressure is required;
- data loss and duplicate candidate creation must both remain zero;
- queue recovery must remain <= 900 seconds;
- provenance and non-publication invariants must remain intact;
- external representative load/stress and degraded-dependency gates remain explicitly out of scope and open.

## Do

Inspected PR #42 exact head `65c7949624c3770ce91d00c34a957b6b2cb9946a`, all associated workflow runs, and retained artifact `9029584698`. After the acceptance evidence was complete, merged PR #42 with expected-head protection as `37a897aa39e33353c63f7b96192f06e61e200cdb`.

No threat-intelligence feeds, CVE inputs, vendor advisories, RBAC, privacy controls, connector publication semantics or human approval behavior were changed in this run.

## Check

All 17 registered required workflows on exact head `65c7949624c3770ce91d00c34a957b6b2cb9946a` completed with conclusion `success`, including RC4 Quality, both RC6 recovery gates, all RC7 connector/provenance gates, RC8 API/Search/Ingestion performance, Open Source Governance and the RC8 Queue Burst Performance Gate.

Retained artifact:

- artifact: `9029584698` (`queue-burst-performance-evidence`);
- digest: `sha256:a934d6179f347e3bf9a198fcb155e7996c42fc670959c2cfd50453969969b974`;
- submitted: 250;
- accepted: 250;
- backpressure events: 170;
- max queue depth / capacity: 40 / 40;
- data loss: 0;
- duplicate candidates: 0;
- quarantined records: 0;
- recovery after producer: 0.602 s;
- provenance preserved: true;
- publication state preserved: true;
- load test may publish: false;
- `external_load_gate_satisfied=false`;
- `degraded_dependency_tested=false`.

JUnit evidence recorded 6 tests, 0 failures, 0 errors and 0 skipped.

## Act

Decision: `PASS` for RC8.5 only. Phase 5 remains `IN PROGRESS` because degraded-dependency correctness and capacity/scaling completion remain unevidenced, and issue #1's independent representative load/stress gate remains open.

Lesson: performance acceptance must remain exact-head and artifact-bound; historical green CI is useful context but never substitutes for current-head evidence.

## Exactly one next priority

Implement one bounded RC8.6 degraded-dependency performance/correctness gate proving zero data loss and preserved fail-closed governance when a representative internal dependency is unavailable or impaired. Do not combine this with the independent external load/stress gate or Phase 6 work.
