# RUN-20260809-132 — supported object-storage target decision

## Status

`CI_VALIDATION_PENDING`

## Objective

Execute exactly one bounded production-readiness objective: select and record an explicit supported object-storage target that removes the lifecycle ambiguity created by the archived legacy MinIO runtime. No migration implementation is included in this run.

## Plan

Blocking gates:

- PR #88 must be verified merged only after successful exact-head CI.
- The target must have first-party lifecycle, deployment and support evidence.
- Free/community/no-SLA options may not be represented as production-supported.
- Entitlement, administrative credentials and application credentials must remain separate and must not be committed.
- RBAC, separation of duties, provenance, privacy, auditability and human share approval must remain unchanged.

## Do

1. Verified PR #88 is merged as `c11e37f5c166d698b0edf05ec1969dcfb3b0fe2d` from exact head `e10eba331fca40b95bd3800fa1be93c9474ece2f`.
2. Verified all 37 registered GitHub Actions runs on that exact head completed with conclusion `success`.
3. Reviewed current first-party MinIO AIStor documentation, release artifacts, upgrade guidance, licensing, subscription and support terms.
4. Reviewed the repository dependency surface: `pyproject.toml` retains `minio>=7.2,<8`, making a drop-in S3-compatible successor lower risk than an immediate provider/API redesign, subject to migration regression evidence.
5. Accepted ADR-0001: MinIO AIStor Enterprise Lite or Enterprise, with active paid support entitlement, as the migration target. Enterprise Lite requires the support add-on for DTMO production acceptance. AIStor Free is rejected for production because it has no SLA/SLO/service agreement and lacks the required HA/support profile.
6. Defined the production deployment boundary: vendor-supported production topology, immutable release tag and digest, no `latest`, secrets/license outside source control, TLS and server-side encryption before production acceptance.

## Check

Evidence reviewed:

- Live GitHub PR #88 state and exact-head Actions evidence: 37/37 successful.
- `pyproject.toml`: `minio>=7.2,<8` client dependency.
- MinIO AIStor server documentation describes AIStor Server as a drop-in replacement and documents native S3 API support.
- MinIO container guidance states single-host distributed Docker Compose is development/test/staging only and directs production to supported production topologies.
- MinIO licensing documentation states AIStor Free has no SLA/SLO/service agreement; distributed and support capabilities require paid tiers.
- MinIO support terms define support for Enterprise/Enterprise Lite purchases, with Enterprise Lite direct engineer support requiring a separately purchased support package.
- MinIO release/upgrade documentation provides current maintained release feeds and an upgrade mechanism for pinned container versions.

Decision confidence: **high** for product/support/lifecycle facts. Migration suitability remains **not yet accepted** until the subsequent implementation and exact-head regression/recovery/security gates execute.

No CI result for this RUN-132 documentation head is yet claimed. Missing, queued, cancelled, failed or unexecuted workflows remain non-PASS.

## Act

- Added `docs/architecture/ADR-0001-supported-object-storage.md`.
- Added `docs/qa/SUPPORTED_OBJECT_STORAGE_TARGET_GATE.md`.
- Updated `docs/development/RUN_LOG.md`.
- Updated `docs/roadmap/PRODUCTION_ROADMAP.md`.
- Updated `docs/project/CURRENT_STATE.md`.
- Coordinate issue #1 external entitlement/secrets/deployment boundaries without closing them.
- Update issues #2 and #3 with the accepted target and bounded next action.

## Threat-intelligence/provenance note

The earlier affected-version finding remains valid and is not weakened by this ADR. This run does not assert configuration-specific exploitability. The purpose of the decision is to move the supported design away from the archived legacy runtime while preserving claim boundaries.

## Exactly one next priority

Implement the bounded migration from legacy `minio/minio` to the accepted AIStor target with an immutable supported release and external license/secret boundary, then execute relevant security, recovery, storage-integrity and full regression gates with retained exact-head evidence before resuming RC10.5.