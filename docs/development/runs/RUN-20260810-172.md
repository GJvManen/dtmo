# RUN-20260810-172 — Safe registered-source execution and curated source catalog

Status: `CI_VALIDATION_PENDING`

## Plan

Bounded objective: make enabled registered JSON intelligence sources executable through a fail-closed outbound trust boundary, and attach a broad curated source catalog to that same source-control plane. This directly follows the accepted 16.0.0rc8 Source Registry baseline.

Blocking gates: PR #114 / rc8 is accepted with 48/48 workflows and merged as `7351ae2ab984b6848969bc634c32e819ec413031`. Real staging, genuine VoiceOver/NVDA, independent penetration testing and other external gates remain unchanged and are not advanced by this run.

Security invariants: human admin + `manage:connectors` for manual registered-source execution; no service-account access to the human admin surface; no raw secrets; no arbitrary redirect following; no private/local egress; source execution is candidate ingestion only; review and external share approval remain separate human decisions.

## Do

Implemented 16.0.0rc9:

- pinned-HTTPS generic JSON executor;
- URL revalidation and fresh DNS resolution immediately before each request;
- fail-closed rejection when any resolved address is not globally routable;
- connection to the validated IP while retaining the configured hostname for TLS SNI/certificate verification;
- HTTPS:443 GET only, no redirects and no environment-proxy path;
- JSON-only response validation with a 5 MiB maximum body;
- source-profile normalization for NIST NVD CVE API 2.0 and GitHub Global Security Advisories;
- canonical `DTMO JSON v1` fallback contract for governed custom JSON feeds;
- admin catalog API and idempotent bootstrap of supported catalog sources, disabled by default;
- manual `/api/v1/admin/sources/{id}/run` execution with canonical raw/provenance/database/OpenSearch ingestion;
- connector health/failure isolation and alert-state integration;
- replay-safe repeated ingestion through the existing idempotent canonical/search path;
- improved `/ui/admin-sources` with curated catalog, bootstrap and run controls;
- curated project catalog covering CISA, NVD, GitHub, NCSC-NL, CERT-EU, MSRC, Cisco, Red Hat, Ubuntu, Debian, Apple, Chrome, Mozilla, Fortinet, Palo Alto, Broadcom/VMware and ENISA, plus documented education-sector onboarding targets for School-CERT/Kennisnet and SURF/SURFcert;
- regression tests for DNS rejection, parser profiles, canonical feed shape and catalog breadth.

## Check

The implementation and tests are committed, but no PASS is claimed until the complete registered GitHub workflow matrix succeeds on one exact final PR head. Runtime tests must not use real external source availability as a substitute for deterministic fixture/evidence tests.

Acceptance criteria:

- complete exact-head workflow success;
- private/non-global DNS resolution fails closed;
- redirect behavior remains fail closed;
- TLS certificate verification retains the original hostname while transport is pinned to a validated address;
- responses above 5 MiB or non-JSON content are rejected;
- only supported profiles or canonical DTMO JSON v1 normalize;
- repeated ingestion remains idempotent and search-repairable;
- connector failures feed health/isolation and alerting;
- manual execution requires the existing human admin control boundary;
- candidate ingestion cannot review or approve external sharing.

## Threat intelligence / source review

High-confidence public-source review on 2026-08-10 confirmed:

- CISA describes KEV as its authoritative catalog of vulnerabilities exploited in the wild and provides machine-readable JSON/CSV;
- NIST documents the NVD CVE API 2.0 at `services.nvd.nist.gov/rest/json/cves/2.0`, including pagination, date filtering and KEV-related filtering;
- GitHub documents a public Global Security Advisories REST endpoint and GitHub-reviewed advisory results;
- NCSC-NL publishes public Security Advisories and machine-readable CSAF, plus an official advisory RSS feed;
- CERT-EU publishes public security advisories with actionable technical recommendations and per-advisory JSON downloads;
- MSRC continues to publish the Microsoft Security Update Guide;
- School-CERT/Kennisnet public material is specifically focused on primary/secondary education threats and incident learning;
- SURF's 2025 cyber-threat picture continues to identify DDoS, malware and phishing as major education/research risks and adds AI, quantum and digital-sovereignty themes.

Confidence is high for publisher identity and relevance because these are first-party/public-sector sources. Parser/execution confidence is separately tracked: only CISA KEV, NVD, GitHub and canonical DTMO JSON v1 are executable in this run.

## Act

Lesson: SSRF prevention cannot stop at registration-time URL syntax. The network destination used by the TLS connection must be derived from the exact DNS answer set that passed policy, while redirects remain disabled. Source catalog breadth and executable parser breadth must also remain separate claims.

Backlog update: additional adapters should be added in evidence-driven order, starting with NCSC-NL CSAF and approved education-sector machine interfaces. Graphical dashboard integration remains the next product/UX objective after rc9 acceptance, unless CI identifies a higher-severity defect.

## Exactly one next priority

Verify every registered workflow on the final exact head of the rc9 PR. Merge only on complete success; otherwise remediate the first concrete failing root cause. After acceptance, integrate the existing graphical/operational dashboard building blocks into the professional console.
