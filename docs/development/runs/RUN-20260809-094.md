# RUN-20260809-094 — RC9.1 browser fixture persistence remediation

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded objective: remediate the next known deterministic RC9.1 browser failure after RUN-20260809-093. The blocking evidence from superseded head `5891fdc46b9076707467ca42b26553ecb67ea17e` was `409 intelligence item not found` during the first browser review action. No other Phase-6 scope is included.

## Do

Changed only the RC9.1 browser fixture lifecycle: after the synthetic intelligence candidate is flushed and its UUID captured, the seeding session now explicitly commits before the browser sends review requests to the separately running Uvicorn process.

This preserves all existing governance invariants: synthetic data only, backend-derived permissions, reviewer/share-approver separation, service-account denial, auditability, privacy, provenance and separate human share approval.

## Check

The earlier browser log proves PostgreSQL migrations and Uvicorn startup succeeded, but the review API returned `intelligence item not found`. The fixture was only flushed in the seeding transaction and was not committed before cross-process access. The remediation therefore addresses the observed transaction-visibility failure directly.

No prior workflow result is treated as PASS for the new head. Fresh exact-head GitHub Actions and fresh retained browser evidence are required.

No production dependency or live connector changed. Existing dependency/security gates remain blocking for any material CVE/vendor advisory; this remediation adds no new threat-intelligence surface.

## Act

RC9.1 remains `CI_VALIDATION_PENDING`; Phase 6 remains `IN PROGRESS`. The lesson is that browser E2E fixtures crossing process boundaries must be committed before the browser/API process consumes them.

## Exactly one next priority

Inspect every workflow on the final PR #50 exact head. If all required workflows succeed, independently inspect retained `browser-share-approval-evidence` and merge with expected-head protection. If any workflow fails, repair only the earliest deterministic failure.
