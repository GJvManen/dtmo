# RUN-20260812-208 — RC13 owner acceptance and Phase 8 readiness transition

## Classification

Immutable point-in-time acceptance record.

## Date

2026-08-12

## Trigger

After PR #169 completed the full final exact-head workflow matrix and merged, the accountable project owner performed the functional local product retest and explicitly reported:

> “Het project werkt! Gefelciteerd!”

The owner then supplied follow-up product recommendations covering severity colours/filters, manual source onboarding, trend/framework analytics, Administration RBAC management and deeper Governance framework support.

## Repository evidence preceding owner acceptance

PR #169 — `RC13: normalize supported source records before canonical ingest`:

- final exact head: `53aaa670c75a2f404337620bcf1a8df172efe583`;
- every returned workflow on that exact head: `completed/success`;
- actual merge SHA: `4d182879d851cd22d22ff4f0bab795ed49ee0c1b`.

The repair preserved:

- HTTP(S)-only canonical URL policy;
- stable HTTPS NVD canonical/provenance URLs while retaining raw upstream references;
- explicit `security-advisory` -> canonical `advisory` normalization only;
- fail-closed unknown item types;
- commit-before-success behavior from PR #167;
- existing RBAC/review/share-approval boundaries.

## Owner decision

The owner's explicit successful functional statement is accepted as accountable RC13 product acceptance.

Decision:

- Phases 1–7: `PASS`;
- RC13: `PASS / OWNER_ACCEPTED`;
- issue #150: closed `completed`;
- Phase 8: `READY_FOR_EXTERNAL_VALIDATION / PENDING_EXTERNAL_DEPLOYMENT_IDENTITY`;
- Phase 9: `NOT COMPLETE`;
- Phase 10: `NOT STARTED`;
- DTMO: **not production ready**.

The owner's follow-up recommendations are enhancements and do not reopen RC13.

## Phase 8 handoff

Issue #158 becomes the single active production-readiness priority. Phase 8.1 requires a real approved production-equivalent staging environment and immutable deployment identity. Repository CI, local Docker Compose, staging emulators and synthetic browser fixtures do not satisfy that acceptance boundary.

Staging must retain separate least-privilege application identities and must not reuse the local-development AIStor root/admin credential compatibility exception.

## Enhancement backlog

Issue #171 records the post-RC13 product improvements in bounded delivery order:

1. shared severity colour/filter semantics across Overview and Intelligence;
2. governed manual source onboarding;
3. Visual Analytics trend enrichment;
4. first-class evidence-backed framework mapping;
5. deeper Administration RBAC management;
6. deeper Governance framework coverage/evidence views.

Missing framework mappings remain visibly unmapped until explicit provenance-backed mappings exist. No inferred equivalence is accepted.

## Evidence lineage

RUN-206 remains immutable historical blocker evidence. PR #170 was closed without merge because the new owner acceptance superseded its `AWAITING_OWNER_RETEST_AFTER_REPAIR` lifecycle. Its branch-only RUN-207 never became authoritative on `main`.

During the prior post-#169 status handling, three connector-generated commits existed on `main`, including an immediately restored README write. A repository compare from the #169 merge to the repaired main head returned `files: []`; the repository tree was unchanged from the accepted #169 merge.

An accidental placeholder issue #172 was created during this acceptance-documentation turn, immediately renamed, closed `not_planned`, and explicitly excluded from project requirements/evidence.

## Security and governance boundary

RBAC, least privilege, separation of duties, privacy, provenance, auditability, human review and separate external share approval remain authoritative. Source creation/execution, analytics, Administration, Governance, CI and staging access do not themselves grant publication authority.

## Next production-readiness priority

**Execute Phase 8.1 real staging environment and immutable deployment identity under issue #158.**

This record must not be rewritten after merge. Later evidence must use a new run record.
