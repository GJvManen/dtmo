# RUN-20260811-187 — RC10.6 exact-head startup remediation

## Objective
Remediate exactly the first concrete root cause from PR #121 exact-head CI without advancing RC10 scope.

## Evidence
- PR #121 exact head `c81f9d77a7e91e0706a1c96fd417a1c454cebf3b` was open and mergeable but not acceptable.
- The workflow matrix contained failures; therefore no PASS was claimed.
- The first inspected failure, RC9 Reflow Accessibility Gate, failed while starting the bounded DTMO target.
- Uvicorn/FastAPI import failed with `AttributeError: type object 'Permission' has no attribute 'READ_METRICS'` at the `/metrics` route.
- `Permission.READ_METRICS` does not exist in `dtmo.auth.policy.Permission`.
- PR diff inspection showed `main.py` had unintentionally replaced accepted RC10.5 health, connectors and metrics behavior in addition to mounting the RC10.6 router.

## Remediation
Restore `backend/dtmo/main.py` to the accepted RC10.5 baseline and retain only the intended RC10.6 changes: import and mount `ux_preferences_router`. This removes the nonexistent permission reference and preserves accepted connector-management RBAC and existing health/metrics behavior.

## Governance
No RBAC authority, separation-of-duties rule, privacy/provenance behavior, audit authority, review authority or separate human share-approval boundary is changed by this remediation.

## Decision
`CI_VALIDATION_PENDING`. The remediated exact head must complete the full registered GitHub Actions matrix. Failed, skipped, queued, cancelled, missing or stale evidence is not PASS.

## Exactly one next priority
Validate the new exact PR #121 head across the full registered workflow matrix. If any workflow fails, inspect and remediate only the first concrete root cause.
