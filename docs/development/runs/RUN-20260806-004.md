# RUN-20260806-004 — Observable CI workflow contract gate

- **Started:** 2026-08-06 00:43 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Replace text-fragment-only workflow checks with structural YAML validation and add independently retrievable workflow-contract evidence.

## Plan

The previous run established a regression guard but left two weaknesses: the test only searched text fragments, and a successful contract execution would not produce a dedicated retained artifact. This run addresses those two weaknesses without claiming that GitHub Actions has executed.

## QA plan

| Gate | Method | Blocking |
|---|---|---:|
| Structural regression protection | Parse `.github/workflows/ci.yml` and validate required triggers, permissions, jobs, services, conditions and commands | Yes |
| Independent observability | Add a dedicated `workflow-contracts` job with JUnit output and retained artifact | Yes |
| Negative release behavior | Require artifact creation and preserve dependency review severity blocking | Yes |
| Dependency integrity | Add a bounded PyYAML test dependency | Yes |
| Execution evidence | Inspect commit status after the workflow update | Yes |

## Do

Implemented:

- `PyYAML>=6.0,<7` in the test dependency group;
- structural YAML contract tests in `backend/tests/test_ci_workflow_contract.py`;
- validation of required triggers, permissions, jobs, PostgreSQL service, release commands and dependency-review conditions;
- a dedicated `workflow-contracts` GitHub Actions job;
- JUnit evidence at `artifacts/workflow-contracts.xml`;
- upload of `workflow-contract-evidence` with `if: always()` and `if-no-files-found: error`.

Implementation commits:

- `79451a654664db9eea7b8dc64f282a41e41cb2bd`
- `1411d8530e735e40c6bc86b40ff49b2cce1abb22`
- `ada976021f678bfc2ed1925128e28e72ebee11ec`

## Check

| Gate | Result | Evidence |
|---|---|---|
| Changes committed | PASS | GitHub returned the three implementation commit identifiers above |
| Structural contract definition | PASS | Test parses workflow YAML and asserts release-critical structure |
| Observable evidence design | PASS | Dedicated job emits JUnit XML and uploads a named artifact for 30 days |
| Automated execution | PENDING | Commit status inspection returned no statuses for `ada976021f678bfc2ed1925128e28e72ebee11ec` |
| Release status | BLOCKED | No successful workflow-run or artifact evidence is currently observable |

The test and workflow design have been strengthened, but no claim is made that the new contract test has executed successfully.

## Act

- **Lesson:** Structural workflow validation and retained artifacts improve auditability, but repository configuration or Actions availability must still be verified independently.
- **Unresolved blocker:** No commit status or workflow-run evidence is visible for the latest workflow commit.
- **Next action:** verify repository Actions settings and obtain one observable `RC4 Quality Gate` run with downloadable `workflow-contract-evidence`.
- **Run gate:** `BLOCKED`.
- **Overall release:** `CI VALIDATION PENDING`.
