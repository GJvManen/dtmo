# RUN-20260810-164 — RC6 residual RC9 exact-head remediation

**Status:** `CI_VALIDATION_PENDING`  
**Roadmap area:** Phase 6 — frontend accessibility and operational UX  
**PR:** #112  
**Source failed exact head:** `327b3d87ff8f1748d0c306a6837948ed0377df15`

## Plan

Bounded objective: remediate only the five residual RC9 failures observed after RUN-163 while preserving the professional rc6 UX, production CSP, server-side RBAC, separation of duties, privacy, append-only auditability and human share approval.

Acceptance gate: every registered workflow must succeed on one final immutable PR head. Historical success on 43/48 workflows is useful diagnosis only and is not reusable as final PASS evidence after code changes.

## Do

The five residual workflow logs were inspected directly and reduced to four remediation points:

1. restore the accepted CISO authorized-idle semantic state from `success` to `ready`, while keeping denial as `forbidden` and successful revocation as `success`;
2. make the auditor evidence table genuinely reflow at 320 CSS px by stacking table structures within the existing semantic table DOM instead of relying on horizontal scrolling;
3. replace gradient backgrounds on measured primary/danger buttons in the role-workspace compatibility layer with solid, contrast-measurable backgrounds;
4. configure only the text-resize and text-spacing Playwright contexts with `bypass_csp=True` so their intentionally synthetic user-style overrides can be injected without weakening the product CSP.

Production response headers remain unchanged: `style-src 'self'` is retained on the affected role surfaces. No CSP exception was added to product runtime behavior.

## Check

Directly observed source-head evidence:

- CISO E2E expected `data-state="ready"` after authorization and received `success`;
- 320px reflow metrics showed document/body widths at 320 but the auditor table and descendants extended to approximately 464 CSS px;
- contrast measurement aborted with `background image unsupported for contrast measurement on BUTTON`;
- text resize aborted at Playwright `page.add_style_tag(...)` because CSP rejected the synthetic inline style;
- text spacing failed at the same test-harness boundary before spacing behavior was measured.

The remediation changes create a new exact PR head. Therefore RUN-164 remains `CI_VALIDATION_PENDING` until a fresh complete registered workflow matrix succeeds on that final head.

No dependency, container image, connector version, authorization policy or audit persistence behavior changed. No external VoiceOver/NVDA, real staging, penetration-test, external-assurance or production-readiness claim is advanced.

## Act

The release gate remains fail closed. If the new exact head has any failing workflow, the next action is the first concrete remaining root cause only. PR #112 must not merge before complete exact-head success.

## Exactly one next priority

Verify every registered workflow on the final RUN-164 exact PR head and merge PR #112 only if all workflows succeed.
