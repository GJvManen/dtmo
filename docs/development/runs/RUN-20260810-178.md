# RUN-20260810-178 — RC10.3 Threat Intelligence Workspace

## Plan

Bounded objective: integrate the already accepted governed search path with one investigation-focused result/detail workspace. The run must remain read-only from an intelligence-governance perspective: no review, share-approval, source-management or security mutation may be added.

Blocking external gates: genuine assistive-technology execution, real staging parity, independent penetration testing, external assurance and production go/no-go remain outside this repository-controlled run.

## Do

- added `/ui/intelligence-workspace` as a dedicated Threat Intelligence Workspace;
- reused `/api/v1/intelligence/search` for governed RBAC-protected search;
- added GET-only `/api/v1/intelligence/{item_id}/workspace` for canonical investigation detail;
- exposed bounded canonical fields, confidence, education relevance, review/share state and provenance;
- extracted only explicit CVE identifiers from stored canonical text/tags;
- marked `known_exploited` only when the stored item originates from the accepted `cisa-kev` source;
- exposed vendor/product only when explicitly present in stored metadata;
- allowlisted safe metadata fields and excluded raw object/storage/request/credential material;
- reused the existing per-tab local/dev/staging sessionStorage identity contract;
- added regression tests for routing, read-only semantics, governance boundaries, CVE extraction and sensitive-metadata exclusion.

## Check

Local repository code review confirms the new router exposes GET-only investigation routes and delegates authorization to `READ_INTELLIGENCE`. CI has not yet completed on the final PR head, so this run remains `CI_VALIDATION_PENDING` until every registered workflow succeeds on one immutable exact head.

No claim is made that absent vendor, product, CVE, KEV or provenance context exists; the UI explicitly distinguishes stored evidence from missing context.

## Act

Lesson: investigation context should be derived conservatively from canonical stored evidence and provenance, not invented or enriched implicitly in the presentation layer.

Exactly one next priority: complete exact-head CI validation of RC10.3. Merge only on complete success; otherwise remediate the first concrete failing root cause only.
