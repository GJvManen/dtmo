# RUN-20260806-045 — RC7.2 persistent connector state and failure isolation

- **Started:** 2026-08-06 17:28 CEST
- **Workstream:** Phase 4 — live connector reliability and provenance
- **Objective:** Persist connector run state and source-health history, isolate repeatedly failing connectors, and support human-reviewed quarantine recovery without automatic publication.

## Plan

Blocking gates:

- connector state and health history must survive process restarts in PostgreSQL;
- repeated failures must isolate only the affected connector for a bounded interval;
- successful recovery must reset the failure counter and close isolation;
- quarantined evidence must retain its raw-evidence hash and recovery history;
- quarantine release requires an identified human reviewer and review reference;
- released evidence returns only to candidate state and may never imply publication approval;
- schema migration must be reversible;
- exact-head GitHub Actions and retained evidence are mandatory.

## Do

- added persistent `connector_runtime_states`, `connector_health_events` and `connector_quarantine_records` models;
- added migration `0005_connector_state` after `0004_privacy_projection`;
- added `ConnectorStateStore` with bounded failure isolation, persistent history and successful-run reset;
- added human-only quarantine recovery to `released_to_candidate` or `rejected`;
- added database constraints forcing `publish_approved = false` for health and quarantine records;
- added unit, migration and workflow-contract regression tests;
- added `RC7 Connector State Gate` using PostgreSQL 17, Alembic, retained machine-readable evidence and an independent fail-closed gate;
- corrected workflow database settings, transient ORM defaults and lint regressions;
- inspected Connector State Gate #12 and found the first deterministic failure after successful migrations: PostgreSQL returned `circuit_open_until` without `tzinfo`, while the supplied comparison time was UTC-aware;
- added `as_utc()` normalization for persisted and supplied datetimes used by isolation checks, run recording and quarantine recovery.

## Check

Connector State Gate #12, run `31124160970`, successfully completed checkout, dependency installation and Alembic upgrades through `0005_connector_state`. The first test failed in `is_isolated()` with `TypeError: can't compare offset-naive and offset-aware datetimes`.

This is a driver-boundary normalization defect, not a failure of the isolation policy. The remediation is committed as `6fab8341e8123664bc3456eaba8daf8d603a0933`. The existing PostgreSQL-backed gate test is the regression evidence and must execute on the new exact head before acceptance.

At the same prior head, Live Connector Canary Gate #20, OpenSearch Recovery Gate #39 and Multi-store Recovery Gate #29 succeeded. Quality Gate #287 remained queued and cannot be counted as evidence for the remediated head.

## Act

Lessons:

- `DateTime(timezone=True)` does not guarantee every driver/session path returns a tz-aware Python object;
- business comparisons at persistence boundaries must explicitly normalize timestamps to UTC;
- successful migrations and unrelated recovery gates do not override a failed connector-state test;
- no `PASS` is claimed until all required workflows and retained artifacts succeed on the exact remediated head.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** exact-head validation for commit `6fab8341e8123664bc3456eaba8daf8d603a0933` has not completed.
- **Exactly one next priority:** inspect the new exact-head RC7 Connector State Gate and remediate only its earliest deterministic failure, or merge PR #29 after all required exact-head gates and retained evidence succeed.
