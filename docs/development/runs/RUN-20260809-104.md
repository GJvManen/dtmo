# RUN-20260809-104 — RC9.6 responsive-layout browser gate

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective after accepted RC9.5 keyboard navigation: add a responsive-layout browser gate across the four already accepted critical browser surfaces. Blocking gates are exact-head GitHub Actions, retained browser evidence and unchanged RBAC/governance invariants. Supported-browser breadth and broad WCAG 2.2 AA remain out of scope.

## Do

Added:

- `backend/tests/test_rc9_6_responsive_layout.py`;
- `.github/workflows/browser-responsive-layout-e2e.yml`;
- `docs/qa/RC9_6_RESPONSIVE_LAYOUT_GATE.md`;
- this run record and RUN_LOG reconciliation.

The Chromium test evaluates share approval, analyst search, CISO token revocation and auditor read-only surfaces at 360×800 mobile, 768×1024 tablet and 1440×900 desktop viewports. It verifies document/body horizontal overflow, main-content containment and viewport containment plus minimum rendered dimensions for every visible interactive control. `/api/v1/ui/session` remains real so capability visibility remains backend-derived. The test performs no business mutation.

## Check

No PASS is claimed in this implementation run. Acceptance requires every registered workflow to complete successfully on the exact final PR head and independent inspection of retained `browser-responsive-layout-evidence`. Missing, queued or unexecuted CI is not evidence.

RC9.5 remains accepted from PR #59 exact head `cbd3dfc973a6daf06347e3ba4df2b5415848a063`, retained artifact `9037726559` (`sha256:d5b0c7713abd6af3ac761e6999b3608c2b3b7093c682634d6c6f6fb2c971903d`) and merge `187928c66143e0c8470082097fafba740da691c6`.

## Threat intelligence / CVE / vendor review

RC9.6 adds no production package, provider, connector or credential. Playwright/Chromium are existing test-only dependencies. Existing dependency/security workflows remain authoritative for applicable CVE/vendor evidence. No threat-intelligence finding changes this bounded layout-gate decision.

## Act

Phase 6 remains `IN PROGRESS`. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval remain mandatory. Issue #1 external production gates remain open.

Exactly one next priority: inspect every registered workflow on the final RC9.6 PR head and independently inspect retained `browser-responsive-layout-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful exact-head evidence.
