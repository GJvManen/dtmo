# RUN-20260807-049 — RC7.3 governed connector contract validation

## Plan

**Bounded objective:** implement one Phase 4 control: fail-closed connector contract validation for approved live connectors before execution.

Blocking gates for this run:

- connector identity must be unique and explicitly approved;
- source and terms endpoints must use HTTPS and licence metadata must be present;
- credential requirements must be checked for presence without serializing credential values;
- retry, interval, timeout, backoff and record-volume limits must be bounded;
- malformed and duplicate records must be governed for quarantine;
- human review must remain mandatory and automatic publication must remain forbidden;
- new contract tests and an independently observable retained-evidence workflow must execute successfully before `PASS` can be claimed.

External issue #1 remains open for real production credential, rate-limit, licence and terms validation for every connector. This repository control does not replace that external acceptance gate.

## Do

Implemented:

- `backend/dtmo/connectors/contracts.py`
  - immutable connector contracts;
  - explicit approval and unique connector identity checks;
  - HTTPS source/terms and non-empty licence provenance checks;
  - bounded confidence, timeout, retries, minimum interval, maximum backoff and record count;
  - optional environment-backed credential requirements;
  - credential presence evidence that never serializes secret values;
  - mandatory malformed/duplicate quarantine policy;
  - mandatory human review and forbidden automatic publication;
  - deterministic SHA-256 contract digest;
  - approved CISA KEV contract.
- `backend/tests/test_rc7_connector_contracts.py`
  - positive approved CISA contract regression;
  - secret-nondisclosure regression;
  - absent credential fail-closed regression;
  - duplicate connector ID fail-closed regression;
  - negative provenance/rate/quarantine/human-review regression.
- `tools/validate_connector_contracts.py`
  - machine-readable contract evidence generation.
- `.github/workflows/connector-contracts.yml`
  - dedicated `RC7 Connector Contract Gate`;
  - focused regression execution;
  - retained `connector-contract-evidence` artifact;
  - independent aggregate job that fails closed when the primary job is absent or unsuccessful.
- `backend/tests/test_rc7_connector_contract_workflow.py`
  - regression protection for triggers, least privilege, test/evidence commands, retention and fail-closed gating.

No RBAC, separation-of-duties, privacy, audit, connector-state or share-approval policy was weakened.

## Threat-intelligence and provenance review

Public-source review performed 2026-08-07:

- CISA Known Exploited Vulnerabilities Catalog: `https://www.cisa.gov/known-exploited-vulnerabilities-catalog`
- CISA identifies KEV as its authoritative source of vulnerabilities known to have been exploited in the wild and provides JSON plus licence information.
- Approved feed used by the canary: `https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json`
- Terms/policy provenance: `https://www.cisa.gov/about/website-policies`

Confidence: **high** for CISA source authority and repository contract provenance. No claim is made that all production connector licences/credentials/rate limits have been externally accepted; that remains issue #1.

## Check

Repository review confirms the new workflow is configured to execute the focused contract regressions, generate machine-readable evidence, retain the evidence for 30 days, and fail closed on missing/failed primary execution.

At the time this run record was authored, the new exact-head GitHub Actions execution had not yet been observed. Therefore:

- connector contract tests: `CI_VALIDATION_PENDING`;
- `RC7 Connector Contract Gate`: `CI_VALIDATION_PENDING`;
- retained `connector-contract-evidence`: `CI_VALIDATION_PENDING`;
- existing quality/canary/recovery regressions on the new exact head: `CI_VALIDATION_PENDING`.

Configured or committed tests are not reported as passed.

## Act

Lesson: production connector onboarding needs an explicit machine-checkable governance boundary before network execution; runtime success alone cannot prove licence, authentication, provenance or publication safety.

Backlog effect: RC7.3 is implemented but remains unaccepted until exact-head CI and retained evidence are independently observable. The external issue #1 connector acceptance gate remains separate and open.

**Run decision:** `CI_VALIDATION_PENDING`.

## Exactly one next priority

Inspect the exact-head `RC7 Connector Contract Gate` and remediate only its earliest deterministic failure, or accept/merge RC7.3 only after the contract gate and required regression gates execute successfully with retained evidence.
