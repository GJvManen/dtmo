# Run 2026-08-27 — Canonical Visual Analytics recovery

Status: **ACTIVE / RED-FIRST CONTRACT**

## Bounded objective

Close Phase 11.10q finding FQ-06 by introducing a canonical Visual Analytics surface in the `/workbench/*` application before migrating governed screenshot UI-07.

The recovery must render meaningful time-series and distribution views from attributable DTMO API data, provide accessible table/text equivalents and non-colour cues, and remain explicit when canonical data is absent. It must not fabricate values to make the product appear populated.

## Intended canonical surface

- route: `/workbench/analytics`;
- frontend workspace: `frontend/src/VisualAnalyticsWorkspace.tsx`;
- initial attributable reads: `/api/v1/command-center` and `/api/v1/console/vulnerability-analytics`;
- read-only visualization only in this bounded slice;
- no new review, sharing, case or publication authority;
- no live-connectivity, local-exposure, compromise or remediation claim inflation.

## Lifecycle

A red-first contract is committed before implementation so the exact PR lifecycle proves that the canonical route, attributable reads, accessible equivalents and claim boundaries are all implemented before acceptance.

The existing governed `docs/visual/screenshots/visual-analytics.png` remains unchanged. UI-07 capture/promotion is a later separately governed change after this functional recovery is exact-head green.
