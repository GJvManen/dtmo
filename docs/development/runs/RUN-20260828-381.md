# RUN-20260828-381 — R3 canonical object pivots

## Scope

This bounded R3 recovery slice extends the canonical Threat Intelligence object detail with direct pivots to the already-governed Analysis & Enrichment, Knowledge Graph, Investigations, and Sharing & Exchange workspaces.

The only context transferred by browser navigation is the canonical DTMO object identifier. Each destination reloads its own server-authorized persisted state through existing same-origin APIs.

## Preserved boundaries

- No new permission or browser-side authorization is introduced.
- Analyzer execution remains explicitly authorized and is not triggered by navigation.
- Knowledge Graph continues to read DTMO-persisted OpenCTI mappings; the browser does not query OpenCTI directly and an empty graph does not prove absence of upstream knowledge.
- Investigations continues to require server-authorized `handoff:case`; opening context does not create a case or grant responder authority.
- Sharing continues to preserve review/share approval and publication boundaries; navigation does not approve or export anything.
- Server-side credentials, provenance, fail-closed behavior and request-specific evidence semantics are unchanged.

## Acceptance evidence

Repository-controlled acceptance is limited to frontend compilation and static contract tests demonstrating that the canonical object identifier is carried into the supported deep links and that the receiving workspaces reload server-side state.

A green repository run does not establish upstream health, staging or production-equivalent behavior, owner usability acceptance, penetration-test results, independent assurance, or production authorization.
