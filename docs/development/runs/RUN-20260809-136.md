# RUN-20260809-136 — bounded search-health alerting

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one Phase 7 objective after accepted RC10.5: implement bounded OpenSearch search-health alerting using cluster-health state only, preserving privacy, RBAC, separation of duties, provenance, auditability and human share approval.

Blocking gates:

- RUN-135 / PR #92 must be accepted on exact-head CI and retained artifact evidence;
- alert inputs must exclude query text, index/document names, response bodies, credentials and user identities;
- controlled red/unreachable failure and green/yellow recovery semantics must be tested;
- dedicated exact-head retained CI plus the complete registered workflow matrix must succeed before acceptance.

## Do

Verified PR #92 exact head `659fa022840e01ed6db4ebeb6a5e703f58a6d259` completed 39/39 registered workflows successfully. Retained artifact `9041987610`, digest `sha256:6a6f2aa5ea2b0b3fb081a0b376f8187a799af726ba950bcbf6fd8618c54e2eca`, was independently inspected and contained JUnit 6/6 with zero failures/errors/skips plus an exact-head machine-readable PASS record. PR #92 was squash-merged with expected-head protection as `8d6297e17c93150dacb39428ed3580e7c8cc1579`.

Implemented RC10.6:

- `SearchHealthAlertManager` observes only bounded cluster identifier plus `green|yellow|red|unreachable` state;
- raises after 2 consecutive `red` or `unreachable` observations;
- clears after 2 consecutive `green` or `yellow` observations while active;
- suppresses repeated raise transitions;
- emits bounded Prometheus health-check, streak, active-state and transition metrics;
- emits correlated actionable structured events with `publish_approved=false`;
- `probe_opensearch_health()` reads only `/_cluster/health` status and maps HTTP/parse failure to `unreachable` without exposing response bodies;
- `DTMOSearchHealthFailure` Prometheus rule added;
- controlled tests cover failure, recovery, repeat suppression, bounded probe response and response-body privacy;
- dedicated retained `RC10 Search Health Alerting Gate` added.

## Check

Fresh first-party OpenSearch review on 2026-08-09 confirms OpenSearch 2.19 remains in maintenance and 2.19.6 is the current 2.x maintenance release, with security updates included. This run does not expand into the separate production OpenSearch hardening/version-upgrade gate in issue #1.

No test or workflow is marked PASS before execution. RUN-136 remains `CI_VALIDATION_PENDING` until every registered workflow succeeds on the final exact head and retained `search-health-alerting-evidence` is independently inspected.

## Act

Lesson: search-health alerting should consume only coarse cluster health state. Search queries, document/index identifiers and backend diagnostic bodies are unnecessary for alert state and would increase privacy and cardinality risk.

External production OpenSearch hardening, TLS/authentication topology and issue #1 assurance gates remain open. Phase 6 genuine VoiceOver/NVDA behavior remains externally blocked.

## Exactly one next priority

Verify the complete exact-head workflow matrix and retained `search-health-alerting-evidence` artifact for RUN-136; merge only if both are complete and consistent. After acceptance, proceed to the next bounded Phase 7 objective: distributed tracing.
