# RUN-20260809-121 — RC9.15 complete focus-order evidence

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 6 objective: close the complete focus-order evidence gap for the four accepted critical surfaces. Blocking gates are exact-head CI success, retained sequence-level evidence, preserved backend-derived RBAC, and preserved separate human share approval.

## Do

Added `backend/tests/test_rc9_15_focus_order.py` and `.github/workflows/browser-focus-order.yml`. The test enumerates all visible enabled tabbables, rejects positive `tabindex`, drives the complete Tab sequence, compares observed order to DOM-logical order, and checks reverse navigation. Added `docs/qa/RC9_15_FOCUS_ORDER_EVIDENCE.md`.

## Check

Two deterministic oracle defects were found and repaired without changing production behavior:

1. On first exact head `f6c0d2fade7646c469dc7993d5dbefbd92397a53`, `/ui/auditor` has exactly one tabbable control. After reaching it, `Shift+Tab` correctly returned focus to the document boundary, while the test incorrectly expected the same control to retain focus.
2. On exact head `8441057557d4c25370f7fdbccdf98889e01ad48b`, the browser test itself passed and retained artifact `9039753331` was exact-head bound, but the workflow validator compared full expected objects containing `tabIndex` with observed focus-identity objects that intentionally omitted `tabIndex`. This representation-only mismatch failed validation even though the ordered focus identities matched.

No PASS is claimed. Every registered workflow must execute successfully on the final repaired exact head and the retained artifact must be independently inspected.

## Act

Repaired only the evidence oracles. Single-control reverse navigation now accepts the document boundary, while multi-control surfaces still require the immediately preceding tabbable. The machine-readable validator now compares only focus-identity fields (`tag`, `id`, `testid`, `name`, `type`, `text`) and retains separate positive-`tabindex` assertions.

Lesson: accessibility evidence validators must compare semantic identity, not incidental representation fields, or they can reject valid evidence and obscure the real accessibility signal.

Threat-intelligence/CVE/vendor review: this bounded accessibility objective introduces no new dependency, identity mechanism, connector, or externally exposed protocol; no current threat/CVE/vendor advisory changes the objective. Existing security and dependency gates remain authoritative.

Phase 6 remains `IN PROGRESS`. Genuine assistive-technology behavior remains unevidenced.

## Exactly one next priority

Verify every registered workflow on the final exact head after this validator repair and independently inspect retained `browser-focus-order-evidence`; repair only the first deterministic failure, or merge only after complete successful exact-head evidence.
