# RUN-20260809-121 — RC9.15 complete focus-order evidence

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 6 objective: close the complete focus-order evidence gap for the four accepted critical surfaces. Blocking gates are exact-head CI success, retained sequence-level evidence, preserved backend-derived RBAC, and preserved separate human share approval.

## Do

Added `backend/tests/test_rc9_15_focus_order.py` and `.github/workflows/browser-focus-order.yml`. The test enumerates all visible enabled tabbables, rejects positive `tabindex`, drives the complete Tab sequence, compares observed order to DOM-logical order, and checks reverse navigation. Added `docs/qa/RC9_15_FOCUS_ORDER_EVIDENCE.md`.

## Check

The first exact-head run on `f6c0d2fade7646c469dc7993d5dbefbd92397a53` failed deterministically in the new focus-order gate. The application sequence itself passed; the failure was the test oracle for `/ui/auditor`, which has exactly one tabbable control. After that sole control was reached, `Shift+Tab` correctly returned focus to the document boundary (`body`), while the test incorrectly expected the same control to remain focused. No PASS was claimed.

## Act

Repaired only that deterministic oracle: multi-control surfaces must still reverse to the immediately preceding tabbable, while a single-control surface must reverse to the document boundary (`body` or `html`). The repaired exact head is `4dcac8df3216d5ac72d2eb2e02dbb394e80403c6`; all workflows and retained evidence must execute again on that head before acceptance.

Lesson: reverse-focus assertions must model document-boundary behavior explicitly for a surface with one tabbable control; otherwise a conforming browser sequence can be misclassified as an accessibility defect.

Threat-intelligence/CVE/vendor review: this bounded accessibility objective does not introduce a new dependency, identity mechanism, connector, or externally exposed protocol; no current threat/CVE/vendor advisory changes the objective. Existing security and dependency gates remain authoritative.

Phase 6 remains `IN PROGRESS`. Genuine assistive-technology behavior remains unevidenced.

## Exactly one next priority

Verify every registered workflow on exact head `4dcac8df3216d5ac72d2eb2e02dbb394e80403c6` and independently inspect retained `browser-focus-order-evidence`; repair only the first deterministic failure, or merge only after complete successful exact-head evidence.
