# RUN-20260809-127 — RC10.3 bounded queue-backlog alerting

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 7 objective after accepted RC10.1 and RC10.2: define queue-backlog threshold semantics, actionable correlated alert evidence and controlled breach/recovery behavior while reusing the accepted RC8 queue-pressure/backpressure contract.

Phase 6 remains `BLOCKED_EXTERNAL` only for genuine VoiceOver/NVDA execution on supported real hosts. Issue #1 external production gates remain independently open.

## Do

Created branch `rc10/rc10-3-queue-backlog-alerting` from authoritative `main` after RUN-126 documentation reconciliation.

Implemented:

- `QueueBacklogAlertManager` with bounded queue identifiers;
- Prometheus queue depth, capacity, utilization, active-alert and transition metrics;
- explicit raise threshold `>= 80%` utilization;
- explicit clear threshold `<= 50%` utilization;
- hysteresis to suppress alert flapping between those thresholds;
- structured correlated raise/active/clear events with actionable operator guidance;
- observer-only behavior that does not move queue items or change producer/consumer policy;
- Prometheus rule `DTMOQueueBacklog` with threshold/action/clear annotations;
- regression tests for threshold breach, hysteresis, controlled recovery, invalid measurements and alert-rule contract;
- integration evidence that feeds the accepted RC8 queue-burst/backpressure result into the RC10.3 observer and preserves zero-loss/duplicate/publication invariants;
- dedicated `RC10 Queue Backlog Alerting Gate` retaining machine-readable JSON, JUnit and pytest evidence;
- RC10.3 QA contract.

## Check

Source-level checks show:

- queue names are validated as bounded operational identifiers before they can become Prometheus labels;
- utilization is derived from validated `depth/capacity` measurements;
- raise and clear thresholds are separated to provide hysteresis;
- alert transitions carry safe correlation evidence and no queue payload data;
- the observer cannot enqueue, dequeue or modify records;
- publication approval remains unchanged;
- the accepted RC8 queue-pressure harness remains the source of truth for bounded backpressure, zero-loss, duplicate and recovery behavior.

No test result is claimed before GitHub Actions executes the final exact pull-request head.

## Act

Open RC10.3 for exact-head CI and retained evidence inspection. Status remains `CI_VALIDATION_PENDING` until every registered workflow and the dedicated RC10.3 gate complete successfully.

## Claim boundary

This run does not claim a separate deployed durable queue service, external notification delivery, storage/API/search alerting, dashboards, runbook exercise, on-call handover or Phase 7 completion.

## Exactly one next priority

Inspect every registered workflow on the final RC10.3 pull-request head and retained `queue-backlog-alerting-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful evidence.
