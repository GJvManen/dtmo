# RUN-20260807-053 — RC7.4 quarantine publication-evidence remediation

## Plan

**Bounded objective:** inspect the retained RC7.4 payload-provenance artifact before merge and remediate the first higher-severity governance defect if the evidence contradicts the mandatory non-publication invariant.

Blocking gates:

- retained evidence must never represent candidate, aggregate or quarantined connector output as publication-approved;
- the dedicated workflow must assert the same fail-closed semantics;
- a regression test must protect the evidence schema from reintroducing ambiguous/unsafe approval state;
- no merge or PASS until fresh exact-head CI executes successfully after the remediation.

External issue #1 remains authoritative for real production credentials, provider limits, licences/terms, staging/deployment acceptance and independent assurance.

## Do

Before merging PR #31, downloaded retained artifact `payload-provenance-evidence` from RC7 Payload Provenance Gate #1 (run `31151672003`, artifact `8983526494`, digest `sha256:8b65659ddb9f2a23329e96f8c492ef12e69aade70c9726f25a5206e4e70a1d59`). Independent inspection found `quarantine_publish_approved: true` even though every `QuarantinedPayload.publish_approved` value is false.

The implementation had encoded the field as `all(item.publish_approved is False ...)`, so the evidence field name asserted approval while its boolean actually represented the opposite condition. The workflow also asserted that misleading value as true. This is an evidence/governance defect because downstream operators or assurance tooling could interpret the retained field literally.

Remediated `tools/validate_payload_provenance.py` so `quarantine_publish_approved` now represents actual approval state using `any(item.publish_approved for item in result.quarantined)`, which is false for fail-closed quarantine. Updated `.github/workflows/payload-provenance.yml` to require the field to be false and added `test_retained_evidence_never_reports_quarantine_as_publication_approved()` to protect aggregate, candidate and quarantine evidence semantics.

No RBAC, separation-of-duties, privacy, provenance retention, connector execution or human share-approval control was weakened.

## Check

The previous exact head `7d96a25cab1fff0fc6cf1d1235efd251618213a8` had seven successful workflows, but its retained artifact exposed the semantic governance defect above. Those successful executions therefore do not justify accepting RC7.4.

Fresh exact-head CI for the remediated branch has not yet been accepted as evidence in this run. No newly committed test or workflow assertion is marked PASS before GitHub Actions executes it.

Threat/advisory review: this objective is an internal release-evidence correctness defect; no public threat intelligence, historical education incident, CVE or vendor advisory changes the bounded remediation. Existing CISA KEV source provenance remains unchanged.

## Act

Lesson: evidence field names are part of the security control contract. A boolean that proves a negative invariant must not be exposed under a positive approval name with inverted semantics, even if code-level objects remain safe. Retained evidence must be interpretable literally by operators and independent assurance tooling.

**Run decision:** `CI_VALIDATION_PENDING`.

## Exactly one next priority

Inspect the fresh exact-head RC7 Payload Provenance Gate after commit `392bead8f7df2512fa267cd379ed9bca60ea05ad` (or the later documentation head) and remediate only its earliest deterministic failure; if all required exact-head RC4/RC6/RC7 gates succeed and the retained artifact records aggregate, candidate and quarantine publication approval as false, accept and merge RC7.4.
