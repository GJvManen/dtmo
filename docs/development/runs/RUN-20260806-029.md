# RUN-20260806-029 — RC5.4 asymmetric JWKS key rotation

- **Started:** 2026-08-06 Europe/Amsterdam
- **Workstream:** Phase 2 — Application security and identity
- **Objective:** Replace production shared-secret JWT validation with one bounded RS256/JWKS trust slice that supports overlapping active and previous signing keys selected by `kid`.

## Plan

Blocking gates: exact-head lint, strict type checking, regression tests with coverage, compile, migration cycle, dependency audit, container smoke, workflow contracts and aggregate release gate. No PASS before execution.

## Do

- added `PyJWT[crypto]` support;
- added production `jwt_jwks_json` configuration;
- production rejects shared-secret token validation;
- added RS256-only header validation and mandatory `kid` selection;
- rejects algorithm confusion, unknown or duplicate trusted keys, non-RSA keys and non-signing keys;
- supports overlapping current and previous JWKS keys for controlled rotation;
- preserved issuer, audience, temporal, principal-type, RBAC and human/machine separation controls;
- added focused rotation and negative regression tests.

## Check

Repository changes are committed. The exact PR-head Quality Gate has not yet completed; all CI-dependent evidence is `PENDING`.

## Act

Lesson: key rotation must be represented as an explicit trust set with deterministic `kid` resolution; accepting caller-selected algorithms or ambiguous keys would undermine the identity boundary.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Exactly one next priority:** inspect the first completed exact-head Quality Gate and resolve only its earliest deterministic failure, or accept and merge after full success.
