# RUN-20260811-186 — RC10.6 local display preferences

## Objective
Implement exactly one bounded RC10.6 UX-polish increment: safe browser-local theme and density preferences without adding privileged authority or server-side state mutation.

## Implementation
- adds GET-only `/ui/preferences`;
- allowlists theme to `dark|light` and density to `comfortable|compact`;
- stores preferences only in browser `localStorage`;
- invalid stored values fall back to safe defaults;
- explicitly states the governance boundary in the UI;
- adds dedicated regression tests and `RC10_6_UX_POLISH_GATE.md`.

## Governance
The preference surface is presentation-only. It does not modify intelligence, connector/source configuration, tokens, review state, share approval, audit evidence or any server-side authorization decision. Existing RBAC, separation of duties, privacy, provenance, auditability, human review and separate external share approval remain authoritative.

## Decision
`CI_VALIDATION_PENDING`. This increment must not be accepted until every registered workflow succeeds on one final exact PR head.

## Claim boundary
No claim is made for real staging parity, genuine VoiceOver/NVDA execution, independent penetration testing, external assurance or production readiness.

## Exactly one next priority
Open the RC10.6 pull request and complete full exact-head GitHub Actions validation. If a workflow fails, remediate only the first concrete root cause.
