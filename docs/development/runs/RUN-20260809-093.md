# RUN-20260809-093 — RC9.1 first deterministic CI remediation

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded objective: inspect PR #50 exact-head CI, identify the first deterministic release-blocking failure, and remediate only that failure. Do not accept or merge RC9.1 unless fresh exact-head workflows subsequently succeed and retained browser evidence is independently inspected.

## Do

- Inspected all 20 workflows for superseded PR #50 head `5891fdc46b9076707467ca42b26553ecb67ea17e`.
- Confirmed 18 workflows succeeded while RC4 Quality Gate and RC9 Browser Share Approval E2E Gate failed.
- Inspected RC4 Quality Gate job logs. The generic pytest suite collected `backend/tests/test_rc9_browser_share_approval.py` outside the dedicated browser environment and attempted to resolve database host `postgres`, which does not exist in that job.
- Remediated only this release-wide test-isolation failure by making the RC9.1 browser module skip unless `DTMO_E2E_BASE_URL` is explicitly present. The dedicated RC9.1 workflow already sets that variable, so the browser test remains mandatory there.
- Did not remediate the separate dedicated-browser failure observed on the superseded head (`intelligence item not found` after synthetic fixture seeding); fresh exact-head CI must determine the next deterministic blocker.

## Check

The superseded-head RC4 evidence is an actual failure, not a missing test: 214 tests passed before the browser test failed because `postgres` could not be resolved in the generic Quality Gate job. Lint and mypy had succeeded before that failure.

The dedicated RC9.1 job also actually executed Chromium and failed during the governed journey, so it is not treated as PASS. Its retained artifact is failure evidence only and cannot authorize merge.

The remediation changes the PR head. Therefore every required workflow must execute again and old green evidence cannot be reused as merge authorization.

## Act

RC9.1 remains `CI_VALIDATION_PENDING`. Phase 6 remains `IN PROGRESS`. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval are unchanged.

## Exactly one next priority

Inspect all workflows on the new PR #50 exact head. If RC4 Quality is fixed, remediate only the earliest remaining deterministic failure (expected to be validated from fresh evidence, not assumed from the superseded head); otherwise repair only RC4 Quality. Merge only after complete exact-head success and independent retained browser-evidence inspection.
