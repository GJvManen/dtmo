# RUN-20260809-085 — RC8.6 exact-head acceptance and merge

## Plan

Objective: decide the bounded RC8.6 degraded-dependency gate on the exact PR #45 head only. Blocking gates were: all registered required workflows must complete successfully on exact head `e3c157505f4619ef2accbd1e2990fdc673c1cf86`; retained degraded-dependency evidence must be independently inspectable; older-head evidence could not be reused; external production gates in issue #1 remain out of scope.

## Do

No additional product changes were introduced. The run inspected exact-head GitHub Actions and the retained RC8.6 artifact, then merged PR #45 with expected-head protection after acceptance evidence was complete.

## Check

All 18 registered workflows for exact head `e3c157505f4619ef2accbd1e2990fdc673c1cf86` completed with `success`, including RC4 Quality, RC6 recovery, RC7 connector/provenance gates, all prior RC8 performance regressions, Open Source Governance, and `RC8 Degraded Dependency Performance Gate` run `31289663568`.

Retained artifact `9030972060` (`degraded-dependency-performance-evidence`) has digest `sha256:f8ca3ccaac5b3bfb5ad9fbc30004d02f45b57ea6c84f4a7f33899178f160abbf` and was independently inspected. Evidence records 100 submitted / 100 delivered, 20 buffered during a 0.25 s injected outage, 300 dependency-failure events, dependency unavailability observed, 0 data loss, 0 duplicate candidate deliveries, recovery in 1.013 s against a 900 s budget, provenance preserved and publication state preserved. JUnit records 6 tests, 0 failures, 0 errors and 0 skipped.

The evidence explicitly retains `external_load_gate_satisfied=false` and `production_opensearch_hardening_satisfied=false`; those external gates are not closed by this run.

PR #45 was merged with expected-head protection as merge commit `fc42e76e60783bdf1670fe2e208ef9eff70e68bc`.

## Act

RC8.6 is `PASS`. Phase 5 remains `IN PROGRESS` because the independent representative production load/stress gate in issue #1 is still open and must not be inferred from the bounded synthetic CI harness. RBAC, separation of duties, privacy constraints, provenance, auditability and human share approval remain unchanged.

No new code/dependency change occurred in this acceptance-only run, so no new CVE/vendor/threat-intelligence decision was required.

## Exactly one next priority

RC8.7: define and evidence one bounded scalability/concurrency saturation gate that exercises sustained concurrent read/ingest pressure without claiming the still-open independent representative production load/stress gate.
