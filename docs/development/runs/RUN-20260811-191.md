# RUN-20260811-191 — RC13.2 acceptance and RC13.3 governed Administration/RBAC

Date: 2026-08-11

## RC13.2 acceptance

The project owner reported PR #152 green. The exact PR head `6c0a0018fed9b2185211a3395897b3cc2a646a13` was independently verified before merge.

All returned exact-head workflows were `completed/success`, including RC4 Quality Gate #805, RC13 Functional Console Browser E2E Gate #6 and RC13 Single-session Visual Analytics Gate #1.

PR #152 was merged with expected-head protection as `b8c254c5d099cde5dca624aa85b17c320594847e`.

Decision: RC13.2 `PASS` within its slice evidence boundary. RC13 overall remains `BLOCKED_INTERNAL`.

## RC13.3 implementation

The next and only priority is governed Administration/RBAC. Implemented on branch `rc13/rc13-3-administration-rbac` from the accepted #152 merge baseline:

- persistent `managed_principals` and `managed_role_assignments`;
- Alembic migration `0009_managed_rbac_assignments` after `0008_grafana_reporting_views`;
- immutable built-in role/permission catalog derived from server-side policy;
- RBAC APIs protected by `manage:users` plus a human `admin` role;
- strict human/service-account role separation;
- administrator self-management blocking;
- final active managed-admin lockout protection;
- atomic tamper-evident audit records for principal create/update mutations;
- canonical Administration-panel create/update/activate/deactivate functionality;
- explicit external identity-provider/token-reissue boundary for production claim changes;
- unit/contract and Chromium tests;
- fail-closed `RC13 Governed Administration RBAC Gate`.

## Security boundary

DTMO does not operate an internal production bearer-token issuer in the accepted baseline. Managed role assignments represent governed provisioning state; they do not forge or silently rewrite active bearer tokens. Production role changes require identity-provider reconciliation or token reissue.

Arbitrary browser-defined custom token roles are deliberately excluded. Built-in role definitions remain code-controlled and existing JWT/token validation remains authoritative.

## Current decision

- RC13.1: `PASS` within slice boundary.
- RC13.2: `PASS` within slice boundary.
- RC13.3: `PENDING_CI` until the complete exact-head workflow set succeeds.
- RC13.4: pending.
- RC13.5: pending.
- Phase 8: `PAUSED_PENDING_RC13`.
- DTMO: not production ready.

Historical evidence records are not rewritten to manufacture current acceptance.
