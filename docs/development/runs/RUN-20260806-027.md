# RUN-20260806-027 — Restore direct-call-safe principal resolution

## Plan

- **Objective:** Fix the first deterministic failure from RC4 Quality Gate #189 without changing production authentication behaviour.
- **Blocking gate:** `python -m pytest --cov=dtmo --cov-report=term-missing --cov-fail-under=80` must execute successfully on the exact PR head.
- **Security invariants:** production identity remains bearer-token based; caller-supplied subject and role headers remain development/test only; RBAC and separation of duties remain unchanged.

## Do

Changed `resolve_principal` header parameters from `Header(...)` default objects to `Annotated[str, Header()]` metadata with real string defaults. This preserves FastAPI request injection while making direct unit-test invocation deterministic.

Implementation commit: `6cfbddd5c38cc8e908c77b5e580ca65fdceea385`.

## Check

Quality Gate #189 failed because a direct call omitted `authorization`, causing the FastAPI `Header` descriptor object to reach `.partition()`. The user-provided CI log showed one failed test and no completed coverage gate. No PASS is claimed for the replacement head yet.

## Act

Lesson: FastAPI dependency metadata should be expressed with `Annotated` when the dependency is also directly unit tested. Backlog remains unchanged except for this resolved test defect.

**Run decision:** `BLOCKED` pending exact-head CI.

**Exactly one next priority:** Inspect the replacement Quality Gate and either accept RC5.3 on full success or fix only its earliest deterministic failure.
