# RUN-20260808-076 — RC8.5 queue pressure and connector burst

Release gate: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 5 objective: implement a synthetic queue-pressure and connector-burst performance harness driven by the accepted RC8.1 workload profile. The objective is limited to proving bounded backpressure, complete processing, queue recovery, zero data loss and zero duplicate candidate creation under a scaled burst fixture.

Blocking conditions are any missing/failed exact-head workflow; absent retained evidence; no observed backpressure; queue depth exceeding the configured bound; recovery exceeding the profile budget; data loss or duplicate candidates above zero; lost provenance; any state implying publication approval; or any attempt to claim the external representative load/stress gate from the scaled CI fixture.

Degraded-dependency behavior is explicitly outside this run.

## Do

- Added `backend/dtmo/performance/queue_burst.py` with an asyncio bounded-queue harness.
- Bound the harness to RC8.1 values: 250 burst records/s, connector parallelism 20, recovery <= 900 seconds, zero data loss and zero duplicate candidates.
- Added a scaled one-second CI burst. Consumers intentionally process at the accepted sustained 100 records/s aggregate rate so burst pressure must saturate the bounded queue and exercise backpressure.
- Reused the governed connector normalization path so accepted records retain source provenance, confidence, payload digest and `publish_approved=false`.
- Added focused regressions covering profile governance, privacy, actual backpressure, complete processing, duplicate prevention, provenance, publication state and fail-closed recovery-budget violation.
- Added `RC8 Queue Burst Performance Gate` with retained JSON/JUnit evidence and an independent fail-closed aggregate job.

## Check

Source review confirms the new gate uses only generated synthetic records and cannot approve review, sharing or publication. Evidence explicitly records `external_load_gate_satisfied=false` and `degraded_dependency_tested=false`.

Actual exact-head GitHub Actions execution is not yet accepted in this implementation run. No configured, queued, absent or unexecuted workflow is treated as PASS, and no measured burst result is claimed until retained evidence is independently inspected.

No public threat-intelligence, education-incident, CVE or vendor-advisory change materially alters this bounded queue/backpressure control objective; the run therefore does not introduce unrelated threat-content changes.

## Act

RC8.5 remains `CI_VALIDATION_PENDING`. The bounded harness makes queue pressure observable instead of inferring resilience from nominal ingestion throughput. It preserves the distinction between a scaled deterministic CI fixture and the independent representative production load/stress gate.

RBAC, separation of duties, privacy, auditability, provenance and mandatory human share approval are unchanged.

## Exactly one next priority

Inspect every required exact-head workflow for the RC8.5 pull request. If any fails, remediate only the earliest deterministic failure. If all succeed, independently inspect retained queue-burst evidence and merge only with expected-head protection. Do not implement degraded-dependency testing in that acceptance run.
