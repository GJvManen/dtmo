# RUN-20260806-015 — RC4 test gate remediation

## Plan

Resolve the sole remaining RC4 aggregate-gate blocker reported by run #139: the `test` job. Preserve the 80% coverage threshold and remediate Ruff, strict MyPy and coverage findings rather than weakening the release gate.

## Do

- Added targeted Ruff per-file exceptions for test-only hardcoded credentials, deliberate BaseLoader workflow parsing and existing import-layout findings.
- Added an APScheduler MyPy override for the upstream package's missing typing marker.
- Made the structlog logger return type explicit with `cast`.
- Added a strict return annotation to the FastAPI lifespan context manager.
- Added `backend/tests/test_quality_gate_coverage.py` covering:
  - connector catalog validation, sorting and health state;
  - intelligence ingestion, duplicate handling and share approval;
  - connector-run success and degraded completion;
  - MinIO bucket creation, object round trip and health failure;
  - scheduler registration, start, status and shutdown.

## Check

Source changes and tests are committed to `main`. The preceding diagnostic run established the baseline:

- 33 existing tests passed;
- coverage was 69.93% against the blocking 80% threshold;
- Ruff reported import ordering plus test-security findings;
- MyPy reported four errors.

The new commits automatically trigger both `RC4 Quality Gate` and `RC4 Quality Gate Diagnostics`. No PASS is claimed until GitHub Actions reports successful Ruff, MyPy, Pytest coverage and aggregate release-gate results.

## Act

Inspect the newly triggered workflows. If all release-critical jobs succeed, update RC4.8 to `RC_READY`; otherwise use the new phase diagnostics to address the remaining concrete failure.

## Gate

`PENDING` — implementation complete; automated verification in progress.
