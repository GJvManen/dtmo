# RUN-20260806-030 — RC5.4 PyJWK type-contract remediation

- **Started:** 2026-08-06 Europe/Amsterdam
- **Workstream:** Phase 2 — Application security and identity
- **Objective:** Resolve exactly one deterministic RC5.4 CI blocker: strict MyPy rejected an untyped JWKS key object passed to `jwt.decode`.
- **Plan:** Preserve RS256-only validation, mandatory `kid`, deterministic JWKS selection and algorithm-confusion protection. Do not use `Any`, ignores or scanner suppression.
- **Do:** Changed `_select_jwks_key` to return `jwt.PyJWK` directly rather than its untyped `.key` attribute. `jwt.decode` accepts `PyJWK` as a supported key type.
- **Check:** Quality Gate #199 showed the first deterministic failure at strict MyPy: argument 2 had type `object`. Tests and later release jobs were not executed and are not marked pass.
- **Act:** Added the narrow type-safe remediation. Replacement exact-head CI is required.
- **Known blockers:** Replacement Quality Gate not yet complete.
- **Next action:** Inspect the first completed Quality Gate for the updated PR #12 head and resolve only its earliest deterministic failure, or accept full success.
- **Release gate:** `BLOCKED`
