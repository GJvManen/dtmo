# RUN-20260806-037 — RC5.10 revocation reconciliation and recovery

- **Started:** 2026-08-06 14:29 CEST
- **Workstream:** Phase 2 — application security and identity
- **Objective:** Reconcile active token revocations from append-only database evidence back into Redis without weakening fail-closed authentication.

## Plan

Blocking gates:

- durable revocation evidence must carry a machine-readable expiry boundary;
- the append-only audit chain must verify before any recovery action;
- active missing Redis revocations must be restored idempotently;
- expired revocations must not be recreated;
- malformed or tampered evidence must stop reconciliation;
- exact-head CI must execute before PASS.

## Do

- added versioned canonical JSON provenance for new token-revocation audit events;
- added `TokenStateStore.is_revoked()` with backend failure translated to `TokenStateError`;
- added `reconcile_token_revocations()` and a deterministic reconciliation report;
- reconciliation verifies the entire persistent audit chain before reading revocation evidence;
- active missing markers are restored with the original expiry, present markers are left unchanged and expired markers are skipped;
- added regressions for restoration, idempotence, expiry and tamper failure.

## Check

- Repository changes and tests are committed on the RC5.10 branch.
- No local or GitHub CI execution is claimed as PASS.
- Exact-head Quality Gate evidence remains pending.

## Act

Lesson: a volatile enforcement store may be rebuilt only from integrity-verified, expiry-complete durable evidence. Human-readable rationale alone is not sufficient recovery state.

- **Known blockers:** exact-head CI has not completed.
- **Next action:** inspect the RC5.10 Quality Gate and resolve only its earliest deterministic failure, or merge after complete success.
- **Release gate:** `CI_VALIDATION_PENDING`
