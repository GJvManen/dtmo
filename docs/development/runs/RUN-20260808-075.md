# RUN-20260808-075 — RC8.4 exact-head acceptance and merge

Release gate: `PASS`

## Plan

Accept or reject RC8.4 only from objective evidence on PR #41 exact head `d3ab690ea2b4144e21598f8c2d74ef55c6a066c6`. Blocking conditions were any missing, queued, cancelled or failed required RC4/RC6/RC7/RC8 workflow; absent or expired retained ingestion evidence; any data loss or duplicate candidate creation; replay creating additional candidates; loss of provenance or publication-state invariants; or PR head movement before merge.

## Do

- Re-read PR #41 and confirmed it remained open, mergeable and unchanged at exact head `d3ab690ea2b4144e21598f8c2d74ef55c6a066c6`.
- Inspected all GitHub Actions runs associated with that exact head.
- Retrieved retained artifact `ingestion-performance-evidence` from RC8 Ingestion Performance Gate run `31268483024` / run number `1`.
- Independently inspected `ingestion-performance-evidence.json` and `ingestion-performance-tests.xml`.
- Merged PR #41 with expected-head protection. Merge commit: `781bc043da64fdeb7fc18c69f25521a2f7f22f91`.

## Check

All 15 required exact-head workflows completed successfully:

- RC4 Quality Gate #369;
- RC6 OpenSearch Recovery Gate #121;
- RC6 Multi-store Recovery Gate #111;
- RC7 Connector State Gate #49;
- RC7 Live Connector Canary Gate #102;
- RC7 Connector Contract Gate #73;
- RC7 Payload Provenance Gate #64;
- RC7 Connector Replay Gate #20;
- RC7 Connector Freshness Gate #16;
- RC7 Connector Failure Isolation Gate #12;
- RC7 Connector Retry Gate #9;
- RC7 Connector Timeout Gate #8;
- RC8 API Read Performance Gate #5;
- RC8 OpenSearch Search Read Performance Gate #4;
- RC8 Ingestion Performance Gate #1.

Retained artifact `9024869189` has digest `sha256:bf419775b1ae51df4970e8e1ecceb319ab2841a574559d93d557394a72623b06` and was not expired at inspection time.

The evidence records `decision=pass` for the accepted Phase 5 profile. The bounded synthetic run submitted 500 records, accepted 500 candidates, recorded 0 quarantines on the first pass, 0 data-loss records, 0 duplicate candidate records and 0% errors. Achieved throughput was 108081.257 records/s against the minimum 100 records/s budget. A second identical pass produced 500 replayed records with no additional candidates. Provenance and publication state were preserved and `load_test_may_publish=false` remained invariant. The artifact explicitly records `external_load_gate_satisfied=false`.

JUnit evidence records 6 executed tests with 0 failures, 0 errors and 0 skips, including zero-loss governance, synthetic-fixture privacy, replay quarantine, duplicate-candidate fail-closed behavior, publication-permission rejection and production-personal-data rejection.

No absent or unexecuted CI was interpreted as PASS.

## Act

RC8.4 is accepted as `PASS` and PR #41 is merged. The lesson is that ingestion throughput is release-eligible only when correctness is measured alongside speed: zero loss, zero duplicate candidate creation, replay idempotency, provenance and non-publication are mandatory acceptance conditions.

This bounded 500-record synthetic CI fixture is not representative external load/stress evidence and does not close issue #1's independent load/stress gate.

RBAC, separation of duties, privacy, security, auditability and human review/share approval remain unchanged.

## Exactly one next priority

Implement and evidence a bounded Phase 5 queue-pressure and connector-burst test driven by the accepted workload profile, proving backpressure behavior and zero data loss under burst load. Degraded-dependency testing remains outside that next run.
