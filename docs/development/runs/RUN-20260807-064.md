# RUN-20260807-064 — RC7.9 connector timeout and cancellation budgets

Date: 2026-08-07

Release gate: `CI_VALIDATION_PENDING`

Roadmap phase: Phase 4 — Live connector reliability and provenance

## Plan

Execute exactly one bounded objective: add connector timeout and cancellation budgets after accepted RC7.8 retry/backoff controls.

Blocking gates for this run:

1. timeout must be explicit, positive and deterministic;
2. slow upstream work must fail closed rather than remain unbounded;
3. timeout/cancellation must remain connector-local;
4. connector/run/source provenance must remain observable;
5. neither successful completion nor timeout may grant publication approval;
6. no PASS or merge without actual exact-head GitHub Actions execution and retained evidence;
7. issue #1 external credentials, provider limits, licences/terms and acceptance remain open.

## Do

Created branch `rc7-9-connector-timeout-cancellation` from main baseline `78807a81cc65f459b6ab8fc2540f82ed8403c803`.

Implemented:

- `backend/dtmo/connectors/timeout.py` — connector-local async timeout guard, mandatory connector/run/source provenance, fail-closed timeout decision and propagation of external cancellation;
- `backend/tests/test_rc7_connector_timeout.py` — regression coverage for successful completion, slow-operation cancellation, connector-local isolation, scheduler cancellation and invalid policy/provenance;
- `.github/workflows/connector-timeout.yml` — dedicated RC7 timeout gate with retained JUnit and JSON evidence and a fail-closed terminal gate;
- `docs/qa/RC7_9_CONNECTOR_TIMEOUT_GATE.md` — acceptance contract and evidence requirements.

Security/governance properties retained:

- `publish_approved=false` is immutable on every timeout decision;
- no RBAC or separation-of-duties boundary was changed;
- successful ingestion remains distinct from review/share approval;
- source URI and run identity are retained in operational evidence;
- failure is scoped to one connector invocation and does not cancel an independent connector.

## Check

At documentation time, code and workflow configuration exist on the branch, but GitHub Actions has not yet been accepted as evidence for the final exact PR head. Therefore the gate remains `CI_VALIDATION_PENDING` and no PASS is claimed.

Public-source review performed for this objective:

- NVD developer terms state that API access is provided on an `as-available` basis and is not warranted to be continuous or uninterrupted;
- NVD developer guidance documents enforced request-rate limits and recommends deliberate spacing between API requests;
- current CVE/threat-intelligence review did not identify an objective-specific CVE requiring a change to the timeout design. CVE content remains subject to existing provenance, freshness, replay, quarantine and human share-approval controls.

This provider behavior supports a client-side bounded execution budget, while provider credentials, enforced limits, licences/terms and production acceptance remain external gates in issue #1.

## Act

Lesson: retry/backoff and source-health isolation are insufficient if a single upstream await can remain unresolved; timeout must be a separate connector-local fail-closed boundary with provenance and an independently observable CI gate.

Backlog/status updates:

- RC7.9 implementation is ready for exact-head CI validation;
- Phase 4 remains `IN PROGRESS`;
- issues #1, #2 and #3 must record that no external gate is closed by this internal control.

## Exactly one next priority

Inspect the exact-head RC7 Connector Timeout Gate and all required RC4/RC6/RC7 regressions after PR creation; remediate only the earliest deterministic failure, or accept and merge only if every required workflow succeeds and retained timeout evidence proves connector-local cancellation with `publish_approved=false`.
