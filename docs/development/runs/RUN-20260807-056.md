# RUN-20260807-056 — RC7.5 exact-head replay evidence acceptance

## Plan

**Bounded objective:** accept and merge RC7.5 only if the exact PR head has executed all required RC4/RC6/RC7 gates successfully and retained replay evidence independently demonstrates durable cross-run replay protection without weakening publication governance.

Blocking gates:
- exact-head RC7 Connector Replay Gate execution;
- retained `connector-replay-evidence` available and unexpired;
- existing RC4 Quality, RC6 recovery and RC7 connector regression gates successful on the same head;
- candidate/quarantine/replay behavior must remain fail closed and `publish_approved=false`;
- issue #1 external production credentials/provider limits/licence/terms gates remain open and must not be inferred from CI.

## Do

Inspected PR #32 at head `b1cdcfbdead00b8b687691a2b749407842fa0a14` and the completed exact-head GitHub Actions executions. Retrieved retained artifact `connector-replay-evidence` (artifact ID `8987942539`, digest `sha256:724333380a09072c25d933f4ab6d73063ce83391634ab079d9eb2a269015dcdd`, unexpired).

No product code or control was changed in this acceptance run. This run only records independently observed evidence and prepares the already-reviewed implementation for merge. Because adding this run record changes the PR head, the documentation head must itself receive fresh exact-head CI before merge.

## Check

Observed successful executions on implementation head `b1cdcfbdead00b8b687691a2b749407842fa0a14`:
- RC7 Connector Replay Gate #1 — run `31163250434` — success;
- RC7 Connector State Gate #30 — success;
- RC4 Quality Gate #317 — success;
- RC6 OpenSearch Recovery Gate #69 — success;
- RC6 Multi-store Recovery Gate #59 — success;
- RC7 Connector Contract Gate #21 — success;
- RC7 Live Connector Canary Gate #50 — success;
- RC7 Payload Provenance Gate #12 — success.

Retained replay evidence independently inspected:
- `decision=pass`;
- `claim_count=1`;
- first run candidate count `1`;
- replay run candidate count `0`;
- replay quarantine reason contains `replayed_record`;
- aggregate `publish_approved=false`;
- JUnit evidence contains 2 tests, 0 failures, 0 errors, 0 skipped;
- changed-payload regression passed, demonstrating materially changed upstream content is not incorrectly suppressed.

Evidence supports RC7.5 behavior on the implementation head. It does **not** evidence issue #1 external production gates.

## Act

Lesson: replay protection must be evidenced at the persistent database boundary and separately from publication approval; successful ingestion or deduplication can never imply approval to publish.

Backlog/status:
- RC7.5 implementation evidence: `PASS` on implementation head;
- PR merge: pending fresh exact-head CI after this documentation commit;
- Phase 4 remains `IN PROGRESS` until PR #32 is merged and the next roadmap gate is selected;
- issue #1 external production dependencies remain open.

## Exactly one next priority

Inspect the fresh exact-head workflows created by this documentation commit; if all required RC4/RC6/RC7 gates succeed and replay evidence remains retained, merge PR #32 with expected-head protection. Otherwise remediate only the earliest deterministic failure.
