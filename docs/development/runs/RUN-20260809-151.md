# RUN-20260809-151 — production-equivalent staging emulator baseline

## Plan

Objective: create one bounded repository-controlled staging emulator that mirrors production security and deployment constraints closely enough to rehearse configuration and topology without inventing external staging evidence.

Blocking gates at start: Phase 8 remained `BLOCKED_EXTERNAL`; there was no real staging endpoint or ten-class deployment-parity package. The emulator must therefore preserve that external claim boundary.

## Do

Added `infrastructure/staging-emulator/docker-compose.yml` with production-mode DTMO configuration, digest-pinned image inputs, internal backend networking, loopback-only TLS ingress, PostgreSQL, authenticated Redis, secured OpenSearch configuration, AIStor object-storage contract, Prometheus and Grafana. All secrets, certificates and license material are external inputs. Live connectors and AI analyst features default off; human publication approval remains required.

Added a TLS nginx gateway, non-secret environment example, regression tests and `Phase 8 Staging Emulator Gate`. The gate renders Compose to JSON and validates topology without pulling or starting images, then retains exact-head JSON/JUnit/log/config evidence.

## Check

Local execution is not claimed. The new PR must earn a fresh complete exact-head workflow matrix and retained `phase8-staging-emulator-evidence` artifact. CI evidence may validate the emulator contract only; it cannot satisfy the ten external deployment-parity classes.

## Act

Lesson: a production-equivalent rehearsal can reduce staging ambiguity while preserving the distinction between configuration equivalence and deployed-environment evidence. The emulator becomes the source-controlled deployment specification for later real staging, but Phase 8 remains externally blocked until the ten-class evidence package exists.

Decision: `CI_VALIDATION_PENDING`.

## Exactly one next priority

Verify the complete exact-head CI matrix and retained staging-emulator artifact. Merge only on full success; after merge, provision the real approved staging environment from this contract and retain all ten deployment-parity evidence classes.
