# RUN-20260809-149 — Phase 8 staging-readiness regression remediation

## Plan

Objective: resolve the single release-critical RC4 regression failure on PR #102 without changing the Phase 8 external staging-environment blocker or weakening any acceptance control.

Blocking gates: all registered workflows must succeed on the exact final PR head. Missing, queued, cancelled, failed or stale-head execution is not PASS.

## Do

Live exact-head inspection of PR #102 head `afa10ddac6551b21f0c974dd807ed45849f8f323` showed 45/46 workflows successful and `RC4 Quality Gate` failed. Job `test` completed dependency installation, lint and mypy successfully, then failed in `backend/tests/test_phase8_staging_readiness.py::test_phase8_qa_preserves_claim_boundary` because the test still required `CI_VALIDATION_PENDING` after RUN-147 had been legitimately accepted and `docs/qa/PHASE8_STAGING_READINESS_GATE.md` had been reconciled to `PASS`.

The regression test was updated to assert the accepted RUN-147 baseline (`PASS`, PR #101 exact-head evidence and 46/46 workflows) while continuing to require the explicit claim boundary that no staging environment, staging test execution or production acceptance is claimed and that the deployment-parity gate remains authoritative.

No workflow, test category, RBAC control, separation-of-duties rule, privacy/provenance rule or human share-approval control was disabled or bypassed.

## Check

The previous exact head is not accepted because RC4 failed. The remediation creates a new exact head and therefore requires a complete fresh workflow matrix. No previous-head success may be reused as final acceptance evidence.

Phase 8 remains `BLOCKED_EXTERNAL` for real staging environment/deployment-parity evidence regardless of CI outcome.

## Act

Lesson: tests that assert lifecycle state must be reconciled when an evidence gate transitions from pending to accepted; claim-boundary assertions should remain invariant while transient status assertions must reflect the accepted evidence state.

Backlog: retain the ten external deployment-parity evidence classes unchanged.

Exactly one next priority: verify every registered workflow on the new PR #102 exact head. Merge only if all succeed. After merge, obtain the approved production-equivalent staging environment and all ten deployment-parity evidence classes before any staging acceptance suite executes.
