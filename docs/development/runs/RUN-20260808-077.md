# RUN-20260808-077 — Current-state documentation, workflow and graph reconciliation

Release gate: `CI_VALIDATION_PENDING`

## Plan

Reconcile the human-visible project documentation with the actual state of `main` without changing product behavior. The bounded objective is limited to current-state documentation, workflow inventory and architecture/CI graphs.

Blocking conditions:

- claiming a workflow is missing when it is actually present on `main`;
- claiming PR #42 / RC8.5 is on `main` before merge;
- treating configured or queued CI as PASS;
- altering RBAC, separation of duties, privacy, provenance, auditability or human share approval;
- performing Phase 5 product changes in this documentation-only run.

## Do

- Inspected recent `main` commits and PR history.
- Verified `.github/workflows/api-read-performance.yml`, `.github/workflows/search-read-performance.yml` and `.github/workflows/ingestion-performance.yml` exist on `main`.
- Confirmed open PR #42 contains RC8.5 queue-pressure/connector-burst work and is not yet merged.
- Identified `README.md` as materially stale: it still described RC7.2 as active and Phase 5 as not started.
- Added `docs/project/CURRENT_STATE.md` with:
  - roadmap-state graph;
  - runtime/governance dataflow graph;
  - CI/evidence graph;
  - workflow inventory;
  - accepted RC8.2–RC8.4 measurements;
  - explicit RC8.5 pending status.
- Reconciled `README.md` with the actual Phase 5 state.
- Added a dedicated QA reconciliation record and updated the run log.

## Check

Evidence read directly from GitHub shows:

- RC8.2 API-read workflow is present on `main`;
- RC8.3 search-read workflow is present on `main`;
- RC8.4 ingestion-performance workflow is present on `main`;
- `README.md` on `main` was stale and still referenced RC7.2;
- PR #42 is open and mergeable, therefore its RC8.5 workflow/capability must remain described as pending rather than accepted on `main`;
- `RUN_LOG.md` on `main` is current through RUN-20260808-075; RUN-20260808-076 belongs to the still-open PR #42 path and is not treated as an accepted mainline run.

No product, security, identity, privacy, publication or connector control was changed.

Fresh exact-head GitHub Actions evidence for this documentation branch is still required before this reconciliation can be merged. Therefore this run is not marked PASS yet.

## Act

The documentation model now distinguishes three things explicitly:

1. capabilities/workflows already present on `main`;
2. evidence already accepted for RC8.1–RC8.4;
3. RC8.5 changes that remain in open PR #42.

This avoids the previous inconsistency where implementation and workflow files were ahead of the README and diagrams.

## Exactly one next priority

Inspect exact-head CI for the current-state reconciliation PR. If required workflows are green, merge the documentation reconciliation with expected-head protection; otherwise remediate only the earliest deterministic failure. Do not advance RC8.5 in this run.