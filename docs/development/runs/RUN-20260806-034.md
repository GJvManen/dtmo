# RUN-20260806-034 — RC5.8 token revocation and replay-state enforcement

- **Started:** 2026-08-06 14:08 CEST
- **Workstream:** Phase 2 — application security and identity
- **Objective:** Enforce centrally observable token revocation, stable JTI binding and one-time-token replay prevention in production.

## Plan

Blocking gates: validated tokens must be checked against shared state in production; revoked JTIs must fail closed; a JTI may not be rebound to another principal; explicitly one-time tokens must be rejected after first use; backend unavailability must deny authentication; existing RBAC and human approval controls remain unchanged.

## Do

- Added Redis-backed `TokenStateStore` with SHA-256 namespaced keys and expiry-aligned TTLs.
- Added durable revocation markers, immutable JTI-to-principal binding and atomic one-time-token consumption through `SET NX`.
- Extended authenticated token context with expiry and a strictly boolean optional `one_time` claim.
- Wired production bearer authentication to shared token state; Redis errors fail closed as HTTP 401.
- Added focused regressions for reusable tokens, revocation, replay, JTI rebinding and input validation.

## Check

Repository changes are committed on `rc5/rc5-8-token-revocation-replay-state`. No local or CI execution is represented as PASS. Exact-head GitHub Actions evidence remains pending.

## Act

Lesson: signature validation alone cannot revoke a still-valid token or safely implement single-use privileged credentials; shared state must be expiry-bounded and unavailable-state must deny production authentication.

- **Known blockers:** exact-head CI has not completed.
- **Next action:** Inspect the first exact-head Quality Gate and resolve only its earliest deterministic failure, or merge after full success.
- **Release gate:** `CI_VALIDATION_PENDING`
