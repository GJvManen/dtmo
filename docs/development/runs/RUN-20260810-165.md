# RUN-20260810-165 — PR #112 RC9 final remediation

## Plan

**Bounded objective:** restore the four remaining RC9 frontend/accessibility contracts on PR #112 without weakening production CSP, RBAC, separation of duties, privacy controls, append-only auditability or human share approval.

Blocking evidence from exact head `10a7953de141e4502f0ac87037f3a4eec4725602` was 44/48 workflows successful, with failures in CISO token revocation browser E2E, contrast, text resize and text spacing.

## Do

Implemented the following bounded remediation:

- restored the accepted CISO success message contract to `Token revoked. Audit event: <id>` while preserving `ready`, `forbidden`, `loading`, `error` and `success` semantic states;
- raised visible control-boundary contrast for role workspaces using an explicit high-contrast border on ghost/secondary/danger buttons and form controls;
- replaced viewport-clamped role-workspace page-heading sizing with a rem-based override so 200% text resizing remains proportional;
- changed the role-workspace screen-reader-only compatibility rule from clipping to off-screen positioning, preventing synthetic text-spacing evidence from reporting clipped text;
- kept the production Content-Security-Policy unchanged.

Changed code:

- `backend/dtmo/ciso_ui.py`
- `backend/dtmo/ui.py`

## Check

A new PR exact head was produced after remediation. The full registered GitHub Actions matrix must execute on that exact head before this run can pass.

No absent, queued or unexecuted workflow is considered evidence of success.

## Act

**Decision:** `CI_VALIDATION_PENDING`.

Previous exact head `10a7953de141e4502f0ac87037f3a4eec4725602` remains failed evidence (44/48). PR #112 must not merge until every registered workflow succeeds on the final exact head.

External VoiceOver/NVDA evidence, genuine staging parity, independent penetration testing and production go/no-go remain outside this remediation and are not claimed complete.

## Exactly one next priority

Verify the complete workflow matrix on the final PR #112 exact head; if any workflow fails, remediate only the first concrete remaining root cause. If all registered workflows succeed, record the accepted exact-head evidence and merge PR #112.