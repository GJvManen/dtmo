# RUN-20260809-110 — RC9.9 manual/non-automatable WCAG 2.2 AA critical-journey review

Status: `BLOCKED`

## Plan

Execute exactly one bounded Phase-6 objective after accepted RC9.8: manually review the four accepted critical browser journeys for WCAG 2.2 AA criteria not fully evidenced by existing automation. Blocking gates were criterion-level evidence, no unsupported PASS for absent rendered/assistive-technology testing, preserved backend-derived RBAC and separate human share approval, and a precise backlog item for the first deterministic accessibility blocker.

## Do

- Inspected the current critical-surface HTML/CSS/JavaScript in `backend/dtmo/ui.py`, `backend/dtmo/ciso_ui.py` and `backend/dtmo/auditor_ui.py`.
- Reused only already accepted RC9.5–RC9.8 evidence for keyboard operation, responsive target dimensions, supported-browser compatibility and the bounded automated accessibility subset.
- Compared the remaining evidence boundary to the current W3C WCAG 2.2 Recommendation/Understanding material.
- Created `docs/qa/RC9_9_MANUAL_WCAG_CRITICAL_JOURNEY_REVIEW.md` with criterion-level PASS/PARTIAL/NOT-EVIDENCED/BLOCKED decisions.
- Identified deterministic blocker `A11Y-001`: asynchronous principal/session resolution changes identity/role text and reveals RBAC-governed controls, but the principal/session state element is not exposed as a live/status region on any of the four critical surfaces.

## Check

The review did not find justification for a product-wide WCAG 2.2 AA PASS. Existing evidence supports several bounded criteria, but measured contrast, 200% text resizing, 320 CSS px reflow, text-spacing overrides, a complete focus-order transcript and genuine VoiceOver/NVDA behavior remain unexecuted or incomplete.

`A11Y-001` is source-evidenced with HIGH confidence. Each page initially renders `Resolving authenticated principal…`; after `/api/v1/ui/session`, JavaScript replaces that text and reveals/hides controls according to backend-derived permissions. The principal elements have no `role="status"` or `aria-live` semantics. Because the resulting capability state is operationally meaningful and changes without moving focus, this cannot be treated as conforming status-message evidence.

No production code, dependency, connector, credential or data was changed in this review run. Threat/CVE/vendor review is not material to the accessibility finding; existing security/dependency workflows remain authoritative.

## Act

Phase 6 remains `IN PROGRESS` and this run is `BLOCKED`. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval remain mandatory. The backlog now contains `A11Y-001` plus explicitly unevidenced WCAG checks; none are waived.

Exactly one next priority: remediate `A11Y-001` by making asynchronous principal/session resolution programmatically announced on all four critical surfaces, add bounded regression evidence, and preserve backend-derived RBAC and separate human share approval.