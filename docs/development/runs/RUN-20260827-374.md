# RUN-20260827-374 — Canonical UI-07 Visual Analytics screenshot candidate

## Scope

This bounded documentation slice adds an unreviewed canonical replacement candidate for UI-07 after the accepted canonical Visual Analytics recovery in PR #373.

The candidate is `visual-analytics-workbench.png`, captured from the actual canonical route `/workbench/analytics` in Chromium/Playwright. The application HTML/CSS/JavaScript is the real DTMO workbench; the analytics values are sanitized deterministic documentation fixtures intercepted at same-origin API boundaries.

The capture journey is:

`canonical intelligence trends -> severity distribution -> vulnerability observations -> evidence boundary`

The screenshot must render the accessible chart views and their table equivalents before capture. It must not replace `visual-analytics.png` until the exact-head artifact has completed separate governed visual review and promotion.

## Evidence and authority boundaries

This artifact is a **documentation illustration only**. Fixture-backed values do not prove runtime external-source behavior and do not become production or production-equivalent evidence.

The capture:

- does not prove live connectivity;
- does not prove local exposure, exploitability, compromise or remediation;
- does not prove owner acceptance or production-equivalent validation;
- does not grant or exercise review, sharing or publication authority;
- does not expose upstream credential values;
- does not modify canonical records or execute connector, scanner or sharing mutations.

The candidate remains unpromoted until an independent screenshot-review step confirms that canonical navigation, all three analytics views, table equivalents, evidence boundaries and safe disclosure are fully rendered and representative.
