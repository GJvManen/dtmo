# RUN-20260806-008 — Identity-bound CI evidence manifest

- **Started:** 2026-08-06 04:44 CEST
- **Workstream:** Phase 1 — CI and workflow integrity
- **Objective:** Bind primary workflow evidence to an exact GitHub Actions run, attempt, repository and head SHA so it can be matched independently with observer evidence.

## Plan

The existing JUnit artifact proves test output but does not itself carry enough execution identity to prevent ambiguous or mismatched evidence. This run adds a machine-readable manifest to the same artifact and regression-protects its required fields.

## Do

- Updated `.github/workflows/ci.yml` to generate `artifacts/workflow-contract-evidence.json` with schema version, workflow name, run ID, run attempt, head SHA, repository, event, conclusion and canonical run URL.
- Uploaded the JSON manifest together with `workflow-contracts.xml` under the existing `workflow-contract-evidence` artifact.
- Updated `backend/tests/test_ci_workflow_contract.py` so removal of the manifest, identity fields or both evidence files fails the workflow contract.

## Check

| Gate | Result | Evidence |
|---|---|---|
| Workflow configuration committed | PASS | Commits `d6fcba5d2535aedab8ec087f5bca817acf6672e6` and `d6923f4ddf8d3b2705f21267448356a1d5629a7a` |
| Evidence identity is regression protected | PASS (structural) | Contract assertions cover manifest path, fields and GitHub runtime variables |
| Automated tests executed | PENDING | No observable status context exists for head `d6923f4ddf8d3b2705f21267448356a1d5629a7a` |
| Downloadable primary evidence artifact | PENDING | Requires a real `RC4 Quality Gate` run |
| Matching independent observer artifact | PENDING | Requires an observer run for the same head SHA |

## Act

**Lesson:** JUnit alone is not sufficient release evidence when it cannot be unambiguously tied to the run and commit being approved. Primary and observer artifacts must contain matching immutable execution identifiers.

**Release decision:** `BLOCKED` — implementation is committed, but execution evidence remains absent.

**Exactly one next priority:** obtain one real `RC4 Quality Gate` run and verify that its `workflow-contract-evidence` artifact contains both XML and the identity-bound JSON manifest for the same head SHA.