# RUN-20260828-389 — R5 IOC type analytics

## Scope

One bounded R5 recovery slice after merged PR #388: expose persisted canonical IOC observable-type counts in Visual Analytics.

## Repository evidence

- Command Center groups `IntelOwlEnrichmentRecord.observable_type` directly from canonical persistence.
- These are the same persisted observable records used by the server-authorized IOC inventory; no text-derived or search-index-inferred IOC population is introduced.
- Visual Analytics renders IOC type distribution as an accessible chart and table.
- Canonical-store failure returns `ioc_type_distribution: []`; values are never synthesized.
- Existing RBAC, server-side credentials/analyzer allowlists, provenance, review/share/publication and responder boundaries are unchanged.

## Claim boundary

Persisted IOC observable presence and type do not prove maliciousness, verdict, local compromise, live connectivity, source reachability, review completion, sharing approval, publication authority, staging operation, production-equivalent operation, penetration-test results or independent assurance.

## CI discipline

The PR must be accepted only on its exact head after all registered workflows for that head complete successfully. A green repository gate is engineering evidence only.
