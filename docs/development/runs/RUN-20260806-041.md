# RUN-20260806-041 — RC6.2 clean MinIO backup and restore evidence

- **Started:** 2026-08-06 15:58 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove that raw intelligence objects can be backed up from one MinIO instance and restored into a separate clean MinIO instance while preserving byte-level digests, object names, content types and provenance references.

## Plan

Blocking gates:

- source and target object stores must be separate MinIO instances;
- the target bucket must be verified empty before recovery;
- the backup archive must be non-empty and receive a SHA-256 digest;
- every object must retain its SHA-256 content digest and explicit provenance reference;
- source and restored manifests must match exactly;
- measured recovery timing and the quiesced-fixture RPO basis must be retained as machine-readable evidence;
- the MinIO restore job must be included in the fail-closed aggregate release gate;
- exact-head GitHub Actions must succeed before this run may be marked `PASS`.

## Do

- added `tools/verify_minio_backup_restore.py`;
- created deterministic source objects with purpose-specific `provenance-reference` and content digest metadata;
- created a manifest-bound compressed backup archive;
- restored into a separate clean MinIO service and target bucket;
- compared source and restored object names, sizes, content types, SHA-256 digests and provenance references;
- added release-critical `minio-restore` CI execution with two isolated MinIO containers;
- retained `minio-restore-evidence` containing the backup archive and JSON evidence;
- added regression protection in `backend/tests/test_rc6_minio_restore_workflow.py`;
- updated README, QA status, runlog and issues in the same branch;
- remediated Quality Gate #236 by accepting both equivalent fail-closed dependency-audit exit forms in the workflow contract;
- retained the fixed internal argv-only PostgreSQL subprocess invocation and `shell=False` behavior with a narrowly scoped lint justification;
- remediated Quality Gate #240 by restoring the explicit aggregate `required = set(results)` contract before evaluating failures;
- replaced `yaml.BaseLoader` with `yaml.safe_load` and normalized PyYAML's YAML 1.1 boolean interpretation of the GitHub Actions `on` key.

## Check

Quality Gate #236 failed on a dependency-audit contract spelling mismatch and two Ruff findings. Quality Gate #240 then exposed two remaining deterministic regressions:

- the aggregate release workflow still enforced all jobs fail-closed but no longer contained the explicitly contracted `required = set(results)` statement;
- the inline Ruff suppression for `yaml.BaseLoader` did not cover the flagged loader argument.

Both issues are now corrected without reducing release-gate strictness. The latest exact branch head still requires an independently observable GitHub Actions execution. No MinIO recovery pass is claimed until that execution and its retained evidence succeed.

## Act

Lesson: release contracts must remain structurally explicit as well as semantically fail-closed. Workflow YAML should be parsed with a safe loader and GitHub-specific YAML 1.1 key behavior normalized explicitly rather than bypassing safe loading.

- **Release gate:** `CI_VALIDATION_PENDING`
- **Known blocker:** a new exact-head Quality Gate and retained MinIO evidence must complete successfully.
- **Exactly one next priority:** inspect the new exact-head RC6.2 Quality Gate and remediate only its earliest deterministic failure, or merge after complete success.
