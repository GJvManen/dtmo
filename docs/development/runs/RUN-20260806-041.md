# RUN-20260806-041 — RC6.2 clean MinIO backup and restore evidence

- **Started:** 2026-08-06 15:58 CEST
- **Workstream:** Phase 3 — data integrity, backup and recovery
- **Objective:** Prove that raw intelligence objects can be backed up from one MinIO instance and restored into a separate clean MinIO instance while preserving byte-level digests, object names, content types and provenance references.

## Plan

Blocking gates:

- source and target object stores must be separate MinIO instances;
- the target bucket must be verified empty before recovery;
- the backup archive must be non-empty and receive a SHA-256 digest;
- every object must retain its SHA-256 content digest and explicit provenance reference;
- source and restored manifests must match exactly;
- measured recovery timing and the quiesced-fixture RPO basis must be retained as machine-readable evidence;
- the MinIO restore job must be included in the fail-closed aggregate release gate;
- exact-head GitHub Actions must succeed before this run may be marked `PASS`.

## Do

- added `tools/verify_minio_backup_restore.py`;
- created deterministic source objects with purpose-specific `provenance-reference` and content digest metadata;
- created a manifest-bound compressed backup archive;
- restored into a separate clean MinIO service and target bucket;
- compared source and restored object names, sizes, content types, SHA-256 digests and provenance references;
- added release-critical `minio-restore` CI execution with two isolated MinIO containers;
- retained `minio-restore-evidence` containing the backup archive and JSON evidence;
- added regression protection in `backend/tests/test_rc6_minio_restore_workflow.py`;
- updated README, QA status, runlog and issues in the same branch;
- remediated Quality Gate #236 by accepting equivalent fail-closed dependency-audit exit forms and documenting fixed argv-only subprocess execution;
- remediated Quality Gate #240 by restoring the explicit aggregate `required = set(results)` contract;
- replaced `yaml.BaseLoader` with `yaml.safe_load` and normalized PyYAML's YAML 1.1 interpretation of the GitHub Actions `on` key.

## Check

Quality Gate #243 completed successfully on exact PR head `bd2fa4f16d09e924ae3aa0cfb40946aba1fc9084` (run ID `31110100789`). All release-critical jobs passed:

- workflow contracts;
- lint, strict MyPy, tests, coverage and compile;
- migrations;
- PostgreSQL clean-target restore;
- MinIO isolated backup and clean-target restore;
- dependency audit;
- container build and smoke test;
- aggregate release gate.

Retained evidence artifacts:

- `minio-restore-evidence` — artifact `8971294887`;
- `postgres-restore-evidence` — artifact `8971307002`;
- `release-gate-evidence` — artifact `8971313962`;
- `dependency-audit-evidence` — artifact `8971291122`;
- `workflow-contract-evidence` — artifact `8971287078`.

PR #24 was merged to `main` as merge commit `2b623ada2d5dc2faa5d04414da719fe6b9ff6509`.

## Act

Lesson: object-store recovery evidence must prove clean-target isolation, byte-level integrity and preservation of provenance metadata. Release contracts must remain structurally explicit and safe workflow parsing must account for GitHub Actions YAML semantics without unsafe loaders.

- **Release gate:** `PASS`
- **Known blocker:** Phase 3 still lacks OpenSearch reconstruction and combined multi-store recovery acceptance.
- **Exactly one next priority:** implement clean-environment OpenSearch reconstruction from canonical PostgreSQL and immutable MinIO evidence, with deterministic index-manifest verification.
