# RUN-20260828-393 — R5 latest connector execution-state distribution

## Scope

One bounded R5 recovery slice after merged PR #392: expose the latest attributable connector execution-state distribution in Visual Analytics from canonical Command Center runtime observations.

## Repository evidence

- The Command Center already projects `runtime_observation` only when a persisted mapped connector run exists.
- The Visual Analytics distribution counts only non-empty canonical `runtime_observation` values.
- Integrations without attributable connector-run evidence remain absent from this distribution; no status is synthesized.
- The view uses the existing accessible chart plus table equivalent.
- Existing collection volume, collection observation age, KEV, CVSS and EPSS analytics remain intact.

## Claim boundary

A runtime observation is historical latest-run evidence only. It does not prove live connectivity, connector health, source reachability, operational freshness, current upstream availability, successful present-time collection, review authority, sharing approval, publication authority, staging operation, production-equivalent operation, penetration-test results or independent assurance.

## Planned continuation

After this slice, R5 continues with persisted investigation handoff-status analytics, canonical operational/integration readiness-state analytics, then an R5 close-out acceptance contract before R6 Sources & Collection.

## CI discipline

The PR must be accepted only on its exact head after every registered workflow for that head completes successfully. A green repository gate is engineering evidence only.
