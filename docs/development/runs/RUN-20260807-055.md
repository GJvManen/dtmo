# RUN-20260807-055 — RC7.5 cross-run connector replay protection

## Plan

Bounded objective: prevent repeated ingestion of the same upstream intelligence from creating duplicate candidates across connector runs, while preserving immutable provenance, quarantine behavior and mandatory human publication approval.

Blocking gates: persistent replay state must survive run boundaries; exact repeated connector/external-ID/payload tuples must fail closed; materially changed upstream payloads must remain processable; replay records and outputs must never imply publication approval; migration and regression evidence must execute in GitHub Actions before acceptance.

## Do

- Added persistent `connector_replay_claims` storage with a database uniqueness boundary over connector ID, upstream external ID and canonical payload digest.
- Added migration `0006_connector_replay`.
- Added `ConnectorReplayStore` and integrated an optional replay registry into connector normalization.
- Exact cross-run replays are quarantined as `replayed_record`; materially changed payloads with the same external ID remain eligible as new non-publishable candidates.
- Added focused regression tests and a dedicated fail-closed `RC7 Connector Replay Gate` with retained evidence.
- Publication approval remains false for candidates, quarantine entries and replay claims.

## Check

Repository changes are committed on branch `rc7-5-replay-protection`. GitHub Actions execution evidence is not yet available for the final branch head at the time of this run record. Therefore RC7.5 is `CI_VALIDATION_PENDING`; no unexecuted test or configured workflow is treated as PASS.

Threat-intelligence relevance review: CISA continues to treat the KEV catalog as a living prioritization source for vulnerabilities with evidence of exploitation. Current public CISA material also continues to document operational impact from exploited vulnerabilities, including school operations. This reinforces preserving source identity and deterministic replay handling; it does not alter the bounded implementation objective.

## Act

Lesson: same-run deduplication is insufficient for a periodically refreshed live feed. Idempotency requires durable cross-run state and a database-enforced uniqueness boundary, while allowing upstream records to evolve.

Backlog/status: Phase 4 remains `IN PROGRESS`; external production connector acceptance in issue #1 remains open.

## Exactly one next priority

Inspect the first exact-head `RC7 Connector Replay Gate` execution and remediate only its earliest deterministic failure; if it and all required RC4/RC6/RC7 regression gates succeed with retained evidence, accept and merge RC7.5.
