# RUN-20260809-117 — RC9.13 320 CSS px reflow evidence

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective after accepted RC9.12: establish retained WCAG 2.2 SC 1.4.10 Reflow evidence at a 320 CSS px viewport across the four accepted critical browser surfaces. Blocking gates are exact-head CI success, retained machine-readable evidence, no horizontal page overflow or off-viewport ordinary critical content, continued visibility/focusability of governed critical controls, real backend-derived RBAC, preserved separation of duties and separate human share approval, and no unsupported product-wide WCAG claim.

Normative reference: W3C WCAG 2.2 SC 1.4.10 Reflow. The bounded test uses a 320 CSS px wide viewport and does not invoke the two-dimensional-content exception because these critical surfaces are ordinary forms/read-only evidence views rather than content whose meaning requires two-dimensional layout.

## Do

- Added `backend/tests/test_rc9_13_reflow_320.py`.
- The Chromium test visits governed share approval, analyst search, CISO token revocation and auditor read-only with real `/api/v1/ui/session` role resolution at a 320x900 CSS-pixel viewport.
- It fails closed if document/body horizontal overflow exceeds the viewport, if the `main` region exceeds the viewport, or if any visible descendant of `main` extends outside the horizontal viewport.
- It verifies every governed critical control remains visible, horizontally contained and programmatically focusable.
- It retains exact-head JSON with per-surface document metrics/control boxes, plus JUnit and server logs.
- Added fail-closed workflow `RC9 Reflow Accessibility Gate`.
- Added `docs/qa/RC9_13_REFLOW_320_EVIDENCE.md`.

## Check

Implementation and source review are complete, but the dedicated browser gate has not yet executed on the final PR head. Therefore missing, queued or unexecuted CI is not PASS and this run remains `CI_VALIDATION_PENDING`.

This objective proves only the bounded SC 1.4.10 reflow behavior on the four accepted critical surfaces in Chromium at 320x900 CSS px. It does not claim text-spacing behavior, complete focus-order evidence, genuine VoiceOver/NVDA behavior, or product-wide WCAG 2.2 AA conformance.

## Threat intelligence / CVE / vendor review

This bounded accessibility evidence change introduces no production dependency, connector, credential, provider or data flow. Public threat intelligence, historical education incidents, CVE data and vendor advisories are not material to the reflow objective; existing security, dependency and regression gates remain authoritative and any material failure there blocks acceptance.

## Act

Phase 6 remains `IN PROGRESS`. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval remain mandatory.

Exactly one next priority: verify every registered workflow on the final RC9.13 PR head and independently inspect retained `browser-reflow-320-evidence`; repair only the first deterministic failure, or merge only after complete successful exact-head evidence.
