# RUN-20260828-394 — R5 investigation handoff-status distribution

## Scope

One bounded R5 recovery slice after merged PR #393: expose persisted governed TheHive handoff-state distribution in Visual Analytics through the canonical Command Center read model.

## Repository evidence

- The Command Center groups only durable `TheHiveHandoffState.status` records.
- The persistence model constrains status to `reserved`, `delivered`, `ambiguous`, or `failed`.
- The Visual Analytics surface renders the canonical distribution with the existing accessible chart and table equivalent.
- Empty canonical data remains an attributable empty state; values are not synthesized to populate the dashboard.
- Existing connector, collection, intelligence, enrichment, IOC and vulnerability analytics remain intact.

## Authority and evidence boundary

The distribution is historical DTMO handoff lifecycle evidence only. A persisted status does not prove upstream TheHive case completeness, responder execution, downstream action, local compromise, review completion, external-share authority, sharing approval, publication authority, live connectivity, production-equivalent operation, penetration-test results, independent assurance or production authorization.

The existing TheHive persistence constraints that keep `external_share_authorized` and `local_compromise_proven` false are unchanged. This slice adds no mutation path and no credential handling.

## Planned continuation

After this slice, R5 continues with canonical operational/integration readiness-state analytics, then an R5 close-out contract before R6 Sources & Collection.

## CI discipline

The PR must be accepted only on its exact head after every registered workflow for that head completes successfully. A green repository gate is engineering evidence only.
