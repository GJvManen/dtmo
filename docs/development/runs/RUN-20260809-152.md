# RUN-20260809-152 — Phase 8 staging emulator CI-integrity remediation

## Plan

Objective: remediate the single deterministic release-critical CI failure on PR #104 without weakening tests, governance controls, or the Phase 8 claim boundary.

Blocking gates: RC4 Quality Gate must pass on the changed exact head; every other registered workflow must also complete successfully; regenerated staging-emulator retained evidence must be exact-head bound and internally consistent before merge.

## Do

Inspected RC4 run `31337673148`. Lint and mypy passed. The pytest job failed only in `backend/tests/test_phase8_staging_emulator.py::test_qa_preserves_emulation_claim_boundary` because `docs/qa/PHASE8_STAGING_EMULATOR_GATE.md` used the phrase `Human publication/share approval` but omitted the canonical governance phrase `human share approval` required by the regression contract.

Updated the QA document to state explicitly that human share approval is not granted by emulator access, CI success, service identity, or technical responder status. No test assertion, workflow, RBAC control, separation-of-duties rule, privacy/provenance control, or publication approval requirement was disabled or suppressed.

## Check

Previous exact head `03611ee74eb2521a85942a34cec6e060ee989a0c`: 46/47 workflows succeeded. `Phase 8 Staging Emulator Gate` itself succeeded, but RC4 failed, so RUN-151 was not acceptable. Fresh complete CI on the changed head is required; prior-head success and artifacts cannot be reused for final acceptance.

## Act

Lesson: governance wording used as a machine-checked contract must remain canonical across QA documents. Documentation tests should fail closed when required human approval language is absent.

Phase 8 claim boundary is unchanged: the emulator is configuration/topology rehearsal only and does not prove a real staging deployment, runtime behavior, production topology parity, the ten deployment-parity evidence classes, Phase 8 completion, or production acceptance.

## Exactly one next priority

Verify every registered workflow on PR #104's new exact head and independently inspect regenerated `phase8-staging-emulator-evidence`. Merge only if the full matrix succeeds and the artifact is exact-head bound and internally consistent.
