# RUN-20260809-133 — bounded supported object-storage migration

## Status

`PASS`

## Plan

Execute exactly one higher-severity remediation objective before resuming Phase 7: remove the archived/vulnerable legacy `minio/minio` server runtime from the repository-supported Docker Compose path and replace it with the AIStor target accepted in ADR-0001, while preserving the existing S3 service contract and keeping licensing and administrative credentials outside source control.

## Do

PR #89 target decision was accepted first. RUN-133 then removed the legacy MinIO image, introduced a fail-closed external `AISTOR_IMAGE` contract requiring release tag plus `@sha256` digest, moved the AIStor license to an external file/Compose-secret boundary, removed runnable administrative defaults, preserved `minio:9000`, `minio_data:/data`, S3 compatibility and `DTMO_PUBLISH_REQUIRES_HUMAN_APPROVAL=true`, and added the independently observable `Supported Object Storage Migration Gate`.

## Check

PR #90 exact head `0fe5c5f0003211fe9df8535954d9276a2090af35` completed all **38/38 registered workflows successfully**. Relevant recovery and storage-integrity workflows were green, including `RC6 Multi-store Recovery Gate`, `RC6 OpenSearch Recovery Gate`, `RC10 Storage Integrity Alerting Gate`, and the dedicated migration workflow.

Dedicated workflow run `31326861369` completed successfully. Retained artifact `9041774769`, digest `sha256:24e7241138dc0b293957f5e2cd06a4d3a6606b7ba68d688097795047f114ccf8`, is exact-head bound. Independent artifact inspection found `supported-object-storage-migration-junit.xml` with **4 tests, 0 failures, 0 errors, 0 skips** covering legacy runtime removal, fail-closed digest-pinned image configuration, external license/admin credential boundaries, and S3/persistence contract continuity.

PR #90 was squash-merged with expected-head protection as `383702bec6ba07cba065524efa451fd89cbd3b50`.

Fresh post-merge CVE review adds a deployment-time constraint: as of 2026-08-09 the selected production AIStor release must not predate `RELEASE.2026-04-14T21-32-45Z`, the latest relevant fixed threshold observed in the reviewed public MinIO CVE set, and advisories must be rechecked immediately before deployment. This does not invalidate RUN-133 because the repository deliberately does not pin an unverified concrete production release.

## Act

The repository migration contract is accepted for its bounded scope. Commercial entitlement, production topology, TLS/network encryption, server-side encryption, secrets-manager acceptance, deployment-time registry digest verification, staging acceptance and all remaining issue #1 external gates remain open.

RBAC, provenance, separation of duties, auditability and human share approval remain unchanged.

## Exactly one next priority

Perform one bounded post-migration security/recovery/storage-integrity acceptance reconciliation, then resume Phase 7 with RC10.5 API-error alerting only if that reconciliation introduces no unresolved internal blocker.
