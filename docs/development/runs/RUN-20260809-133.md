# RUN-20260809-133 — bounded supported object-storage migration

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one higher-severity remediation objective before resuming Phase 7: remove the archived/vulnerable legacy `minio/minio` server runtime from the repository-supported Docker Compose path and replace it with the AIStor target accepted in ADR-0001, while preserving the existing S3 service contract and keeping licensing and administrative credentials outside source control.

Blocking gates:

- PR #89 target-decision evidence must be accepted and merged;
- legacy `minio/minio` must be absent from the migrated Compose path;
- AIStor image selection must fail closed unless the deployer supplies a vendor-supported release reference pinned by `sha256` digest;
- AIStor license and administrative credentials must have no runnable repository defaults;
- the DTMO S3 endpoint name, persistent volume contract, RBAC, provenance and human share-approval invariants must remain unchanged;
- dedicated migration CI and the complete registered regression matrix must execute successfully on the exact final head before acceptance.

## Do

Verified PR #89 exact head `79c5684b7e65064efe480e6da7913fd437d52b6d` completed 37/37 registered workflows successfully and merged as `83e880a289467151c6604e28cd4141118fb538a9`.

Implemented the bounded migration contract:

- removed `minio/minio:RELEASE.2025-07-23T15-54-02Z` from `docker-compose.yml`;
- replaced it with required external `AISTOR_IMAGE`, whose error contract requires `quay.io/minio/aistor/minio:RELEASE.<timestamp>@sha256:<vendor-verified-digest>`;
- prohibited an implicit runnable image default and prohibited `latest` in the migration regression test;
- mounted the AIStor license through a Compose secret sourced from required external `AISTOR_LICENSE_FILE`;
- changed AIStor administrative user/password inputs to required external values with no source-controlled defaults;
- preserved the service name `minio`, internal S3 endpoint `minio:9000`, persistent `minio_data:/data` volume and existing application S3 configuration contract;
- retained `DTMO_PUBLISH_REQUIRES_HUMAN_APPROVAL=true`;
- added `no-new-privileges:true` to the AIStor service;
- added bounded regression tests in `backend/tests/test_supported_object_storage_migration.py`;
- added independently observable workflow `Supported Object Storage Migration Gate` with retained JUnit evidence.

No license, registry credential, root credential, production data, object key, digest or raw evidence was committed.

## Threat intelligence / vendor review

Primary vendor documentation was freshly reviewed on 2026-08-09. It confirms:

- AIStor container images are published under `quay.io/minio/aistor/minio`;
- an active AIStor license is required for production deployment;
- Docker Compose is suitable for development/test/staging but not accepted by the vendor as a production single-host distributed topology;
- production container deployments should use external/file-based secret handling.

This run does not claim that an external paid entitlement has been purchased, that a production topology is deployed, or that TLS/server-side encryption is complete. Those remain external/staging production-acceptance concerns.

Confidence: high for the repository migration contract and vendor statements; production suitability remains conditional on exact-head CI and later external deployment evidence.

## Check

Pre-CI review confirms the diff removes the legacy image and default MinIO root credentials from the Compose/example path while preserving the S3 service name, endpoint and persistent volume contract.

No test or workflow is marked PASS before execution. The run remains `CI_VALIDATION_PENDING` until the dedicated migration gate and every registered GitHub Actions workflow complete successfully on the exact final PR head. A queued, cancelled, failed, missing or unexecuted workflow is not PASS.

The production image digest is intentionally not invented in-repository. Production deployment must resolve and verify the vendor registry digest for the approved release and supply the complete digest-pinned image reference through the deployment secret/configuration boundary.

## Act

Lesson: production immutability is better enforced as a fail-closed deployment input contract than by guessing or freezing an unverified registry digest in source control. This preserves vendor release provenance while preventing `latest` or legacy fallback.

Backlog/state updates:

- supported-object-storage migration implementation is present but not yet accepted;
- issue #1 external entitlement, secrets-manager, hardening, deployment and assurance gates remain open;
- Phase 6 genuine VoiceOver/NVDA evidence remains `BLOCKED_EXTERNAL`;
- RC10.5 remains deferred until this migration exact head passes the relevant security, recovery, storage-integrity and full regression evidence.

## Exactly one next priority

Verify the complete exact-head CI matrix and retained migration evidence for the migration PR; merge only after every registered workflow succeeds. If accepted, perform one bounded post-migration security/recovery/storage-integrity acceptance reconciliation before resuming RC10.5 API-error alerting.
