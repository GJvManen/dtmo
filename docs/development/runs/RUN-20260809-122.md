# RUN-20260809-122 — RC9.16 genuine assistive-technology behavior

Status: `BLOCKED_EXTERNAL`

## Plan

Execute exactly one bounded Phase 6 objective: obtain genuine assistive-technology behavior evidence for the four accepted critical surfaces. Blocking gates are execution with real screen readers on supported host/browser combinations, retained criterion-level observations, preserved backend-derived RBAC, and preserved separate human share approval.

## Do

Reviewed the accepted Phase 6 evidence boundary after RC9.15. Automated browser, keyboard, focus-order, contrast, resize, reflow, text-spacing and status-message gates now cover their bounded claims, but none executes an actual screen reader. This environment exposes GitHub Actions and repository/browser automation only; it does not provide a macOS VoiceOver host or a Windows NVDA host, and therefore cannot truthfully execute or retain genuine VoiceOver/NVDA announcements, rotor/browse-mode behavior, virtual cursor traversal, live-region announcements, form-control announcements, or screen-reader interaction with the governed critical controls.

Prepared the required external/manual evidence contract:

- VoiceOver on a supported macOS/Safari combination across `/ui/share-approval`, `/ui/analyst-search`, `/ui/ciso-security`, and `/ui/auditor`.
- NVDA on a supported Windows/Firefox or Windows/Chrome combination across the same four surfaces.
- For each surface, record page title/landmarks/headings, accessible names and roles, reading order, focus announcements, session-status live-region announcement, error/status feedback where applicable, and confirmation that RBAC-gated controls are exposed only to the authorized role.
- On `/ui/share-approval`, confirm the external-share action remains a separate explicit human approval step and is announced as such.
- Retain tester, OS, browser, screen-reader version, timestamp, result per observation, defects with severity, and evidence attachments or transcript references.

No production credentials, production data, or business mutations are required for this validation.

## Check

No genuine assistive-technology execution is available in this environment. Existing Chromium/Firefox/WebKit automation and DOM accessibility semantics cannot substitute for actual VoiceOver/NVDA behavior. Therefore no PASS is claimed and Phase 6 cannot be closed from automated evidence alone.

## Act

Recorded the blocker as external/manual rather than weakening the acceptance criterion. Lesson: semantic and keyboard automation can reduce risk but cannot prove screen-reader announcements or interaction behavior without the actual assistive technology stack.

Threat-intelligence/CVE/vendor review: this run changes no dependency, identity mechanism, connector, protocol, or production exposure. No threat/CVE/vendor advisory changes the bounded accessibility blocker; existing security and dependency gates remain authoritative.

Phase 6 remains `IN PROGRESS` and is blocked on genuine assistive-technology evidence.

## Exactly one next priority

Execute the defined VoiceOver and NVDA critical-journey evidence procedure on real supported host/browser/screen-reader combinations and attach retained results; only then may Phase 6 accessibility acceptance be reconsidered.
