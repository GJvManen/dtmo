# RUN-20260807-058 — RC7.6 Connector Freshness/Staleness Handling

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase 4 objective after RC7.5: add deterministic connector source freshness/staleness and clock-skew policy at ingestion. Blocking gates are: fail-closed timestamp handling, preserved provenance, no automatic publication approval, dedicated independently observable CI evidence, and no regression in required RC4/RC6/RC7 gates. External production credential/licence/provider gates in issue #1 remain out of scope and open.

## Do

- added `SourceFreshnessPolicy` with bounded `max_age`, bounded future clock skew, and explicit `allow_missing`;
- source timestamps are ISO parsed and normalized to UTC;
- stale timestamps quarantine as `stale_source_timestamp`;
- timestamps beyond future skew quarantine as `future_source_timestamp`;
- malformed timestamps quarantine as `malformed_source_timestamp`;
- missing timestamps quarantine as `missing_source_timestamp` unless explicitly allowed by policy;
- candidate and quarantine records retain original timestamp, normalized timestamp/classification where available, source URI, connector/run identity, fetch time, digest/raw evidence and confidence;
- publication approval remains false for candidates, quarantine and aggregate result;
- added `backend/tests/test_rc7_connector_freshness.py` covering fresh, stale, bounded skew, excessive skew, malformed, missing, explicitly allowed missing and invalid-policy behavior;
- added `.github/workflows/connector-freshness.yml` with fail-closed gate and retained `connector-freshness-evidence` artifact;
- added `docs/qa/RC7_6_FRESHNESS_GATE.md`.

## Threat-intelligence / provenance review

Reviewed CISA's Known Exploited Vulnerabilities catalog. CISA describes KEV as an authoritative prioritization input, exposes JSON/CSV/schema formats, and retains per-record `Date Added` provenance. This reinforces the design choice to preserve provider timestamps separately from fetch timestamps and apply explicit connector policy to freshness rather than silently replacing provider time with ingestion time.

Source: https://www.cisa.gov/known-exploited-vulnerabilities-catalog
Confidence: high (primary source).

No new public intelligence reviewed in this run justified reordering the roadmap objective or weakening existing controls.

## Check

Code and regression/evidence workflow are committed on `rc7-6-connector-freshness`. At this point no exact-head GitHub Actions execution has been completed and inspected, so the objective is not accepted. No unexecuted test is counted as PASS.

## Act

Lesson: source time, fetch time and policy freshness are distinct provenance facts and must remain independently auditable. Timestamp absence must be an explicit connector-policy decision rather than an implicit acceptance path.

Backlog: RC7.6 remains pending exact-head CI and retained artifact inspection. Phase 4 remains `IN PROGRESS`. External connector acceptance gates remain open in issue #1.

## Exactly one next priority

Open the RC7.6 pull request, inspect the resulting exact-head RC7 Connector Freshness Gate and all required RC4/RC6/RC7 regressions, then remediate only the earliest deterministic failure; accept/merge only if every required gate succeeds and retained freshness evidence proves fail-closed timestamp handling with `publish_approved=false`.
