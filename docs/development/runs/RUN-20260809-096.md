# RUN-20260809-096 — RC9.2 analyst browser operational states

Date: 2026-08-09
Roadmap phase: 6 — Frontend accessibility and operational UX
Objective: implement one bounded critical analyst browser journey for intelligence search with backend-RBAC consistency and explicit loading, empty, success and backend-error states.

## Plan

Blocking gates:

- browser-visible search capability must derive from backend `read:intelligence` permission;
- Chromium must exercise loading, empty, success and error states;
- the error case must use the real backend fail-closed search dependency path rather than fabricate success;
- the journey must remain read-only and must not alter review/share-approval governance;
- all repository regression workflows plus the new dedicated RC9.2 browser workflow must execute on the exact final PR head;
- retained browser evidence must be exact-head bound before PASS;
- issue #1 external production gates remain separate.

## Do

Added a dedicated analyst search page and script under `/ui/analyst-search`, backed by the existing `/api/v1/ui/session` permission model and `/api/v1/intelligence/search` API. The UI exposes a status live region and explicit `loading`, `empty`, `success` and `error` states.

Added a Chromium Playwright journey that authenticates as an `analyst`, verifies the backend-derived search capability, uses synthetic deterministic responses for loading/empty/success rendering, then removes interception and exercises the real backend search endpoint with intentionally unavailable OpenSearch so the UI must surface the backend `503 search backend unavailable` state.

Added `.github/workflows/browser-analyst-search-e2e.yml` with retained JUnit/server/machine-readable evidence and a fail-closed gate job.

No production dependency, live connector, publication behavior or share-approval rule was changed.

## Check

At code-completion time no GitHub Actions execution for the final PR head had yet been accepted as evidence. No local or unexecuted test is treated as PASS. The dedicated workflow must demonstrate actual Chromium execution and the repository-wide workflow set must remain green.

## Threat, CVE and vendor context

No new production dependency or vendor integration was introduced. Existing Playwright/Chromium test infrastructure is reused. Existing dependency, security and governance workflows remain authoritative. No new public threat-intelligence, education-incident, CVE or vendor-advisory finding materially changes this bounded UI-state objective at implementation time.

## Act

Decision: `CI_VALIDATION_PENDING`.

Lesson: operational UX states should be connected to real backend failure semantics where practical; deterministic browser fixtures are appropriate for rendering states, but the error path should still prove fail-closed backend behavior.

Exactly one next priority: inspect every workflow on the exact final RC9.2 PR head and the retained `browser-analyst-search-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful evidence.
