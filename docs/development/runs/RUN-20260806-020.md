# RUN-20260806-020 — Remediate pytest dependency advisory

- **Started:** 2026-08-06 11:43 CEST
- **Workstream:** Phase 1 — CI and workflow integrity (higher-severity regression blocker)
- **Objective:** Resolve the single known vulnerable dependency reported by the exact current PR #9 quality-gate execution without changing application behaviour.

## Plan

1. Bind conclusions to PR #9 head `0e573d80fc06262f52f8ad3f71dbdec118fbe5cd` and `RC4 Quality Gate` run `31090145413` (#159).
2. Inspect all jobs and the failing dependency-audit evidence.
3. Remediate only the earliest blocking advisory.
4. Preserve RBAC, separation of duties, privacy, provenance, auditability and human publication approval.
5. Do not mark the replacement dependency audit or release gate PASS until GitHub Actions executes the updated head.

## Do

- Confirmed workflow-contracts, tests, lint, type checking, compilation, migrations and container smoke succeeded in run `31090145413`.
- Confirmed the release gate failed because dependency-review found one vulnerable package.
- Preserved pip-audit JSON evidence in artifact `dependency-audit-evidence` (artifact ID `8963074009`, SHA-256 `b00f5410873b384e62c6f0c79b9fa95192d450a7cf51b6a0cac6fc7aa2158e5e`).
- The evidence identifies `pytest 8.4.2`, advisory `PYSEC-2026-1845`, aliases `CVE-2025-71176` and `GHSA-6w46-j5rx-g56g`, with fixed version `9.0.3`.
- Updated the test dependency constraint from `pytest>=8.4,<9` to `pytest>=9.0.3,<10` on PR #9. The branch moved concurrently and the corrected constraint was verified at head `892c4ce4bf682cf21ee982cc7b9027ebf3e9d33b`.

## Check

- The failed audit is real executed evidence, not an inferred result.
- The advisory describes unsafe predictable `/tmp/pytest-of-{user}` directories on UNIX, allowing local denial of service or possible privilege gain.
- The dependency constraint now excludes all affected pytest versions and requires the vendor-advised fixed version or later within major version 9.
- No application runtime dependency, identity policy, permission mapping, publication workflow or data model was changed by this objective.
- A replacement workflow result for the updated head was not yet verified when this record was written; therefore dependency review and release status remain `PENDING`.

## Act

- **Run decision:** `BLOCKED` pending replacement CI execution.
- **Phase 1:** temporarily `BLOCKED` by the latest exact-head regression until a successful replacement quality gate is evidenced.
- **Release:** `CI VALIDATION PENDING`.
- **Lesson:** a previously successful numbered run does not override a later failed run on the exact current head; dependency intelligence must be evaluated continuously and bound to immutable execution evidence.
- **Exactly one next priority:** inspect the replacement `RC4 Quality Gate` run for PR #9 head `892c4ce4bf682cf21ee982cc7b9027ebf3e9d33b` and, if successful, verify its primary and observer evidence; otherwise resolve only its earliest deterministic failure.
