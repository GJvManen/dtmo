# RUN-20260809-141 — RC10.9 CI-integrity remediation

## Status

`CI_VALIDATION_PENDING`

## Plan

Execute exactly one higher-severity bounded objective discovered during RC10.9 acceptance: restore CI/workflow integrity for PR #96 without weakening the RC4 Quality Gate or reducing the runbook governance assertions.

Blocking gates:

- PR #96 exact head `42d7104915a5e424e9cebc2e4f0a093cf7948f94` completed 42/43 registered workflows successfully;
- `RC4 Quality Gate` failed and must be diagnosed from actual job logs;
- remediation must preserve the full RC4 test suite and the explicit human share-approval control;
- every registered workflow must rerun successfully on the new exact head;
- retained `operational-runbooks-evidence` must be regenerated and exact-head bound before RC10.9 acceptance.

## Do

Inspected workflow run `31329981268`, failed job `93286565269`. Lint and type checking succeeded. The full pytest step failed in `backend/tests/test_rc10_9_operational_runbooks.py::test_runbook_set_exists_and_has_common_response_controls` because `docs/operations/runbooks/README.md` did not contain the exact required phrase `human share approval`.

The runbook already stated that human review/share approval remains mandatory, but the explicit test contract intentionally requires the canonical phrase used elsewhere in DTMO governance documentation. Remediated the documentation, not the test: the index now states that **human share approval** is never granted to technical responders, connectors, observability components or service accounts by incident status alone.

No test assertion, workflow, security rule, RBAC control, separation-of-duties requirement or publication-approval boundary was suppressed or weakened.

## Check

The failed head is not accepted despite 42 successful workflows and a successful dedicated `RC10 Operational Runbooks Gate`. The aggregate RC4 Quality Gate is release-critical and therefore blocks acceptance.

No new threat-intelligence or vendor-advisory change is material to this bounded documentation-contract remediation. RUN-140's authoritative CISA/FBI provenance remains applicable. This run makes no new exploitability or attribution claim.

Fresh exact-head CI is mandatory. Missing, queued, cancelled, failed or unexecuted workflows are not PASS. Retained runbook evidence from the prior head cannot be reused after the documentation change.

## Act

Lesson: governance terminology used as a machine-checked release contract must be explicit and canonical. Near-equivalent wording is insufficient where a test protects a critical approval boundary.

RC10.9 remains `CI_VALIDATION_PENDING`. Phase 7 does not advance to runbook exercise/on-call readiness until PR #96 earns a complete exact-head PASS and regenerated retained runbook evidence is independently verified.

## Exactly one next priority

Verify the complete registered workflow matrix and regenerated retained `operational-runbooks-evidence` on the new PR #96 exact head; merge only if all evidence is complete and internally consistent.
