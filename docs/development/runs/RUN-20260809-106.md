# RUN-20260809-106 — RC9.7 supported-browser critical-journey gate

Status: `CI_VALIDATION_PENDING`

## Plan

Execute exactly one bounded Phase-6 objective after accepted RC9.6 responsive layout: validate the four accepted critical browser journeys in Chromium, Firefox and WebKit. Blocking gates are exact-head GitHub Actions, retained browser evidence, unchanged backend-derived RBAC and no unsupported broad WCAG 2.2 AA claim.

## Do

- Added `backend/tests/test_rc9_7_supported_browsers.py` parametrized across Chromium, Firefox and WebKit.
- Added `.github/workflows/browser-supported-browsers-e2e.yml` with retained JUnit, server logs and machine-readable exact-head evidence.
- Covered share approval, analyst search, CISO token revocation and auditor read-only journeys.
- Kept `/api/v1/ui/session` real so capability visibility remains backend-derived.
- Intercepted only business-operation calls with synthetic responses to avoid duplicating or mutating accepted backend fixtures.
- Added `docs/qa/RC9_7_SUPPORTED_BROWSER_GATE.md`.

## Check

No PASS is claimed in this implementation run. Acceptance requires every registered workflow to complete successfully on the exact final PR head and independent inspection of retained `browser-supported-browsers-evidence`. Missing, queued or unexecuted CI is not evidence.

RC9.7 introduces no production dependency, connector, credential or provider. Playwright browser engines remain test-only. Existing dependency/security gates remain authoritative for CVE/vendor advisories. No new threat-intelligence or historical education-sector incident changes this bounded compatibility decision.

## Act

Phase 6 remains `IN PROGRESS`. RBAC, separation of duties, privacy, provenance, auditability and separate human share approval remain mandatory. Broad WCAG 2.2 AA remains a separate later objective. Issue #1 external production gates remain open.

Exactly one next priority: inspect every registered workflow on the final RC9.7 PR head and independently inspect retained `browser-supported-browsers-evidence`; repair only the first deterministic failure, or accept/merge only after complete successful exact-head evidence.
