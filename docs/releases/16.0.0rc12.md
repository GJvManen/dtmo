# DTMO 16.0.0rc12

Status: `PASS` within the repository-controlled engineering boundary. **RC13 functional product acceptance is also `PASS`; Phase 8 is `READY_FOR_EXTERNAL_VALIDATION / PENDING_EXTERNAL_DEPLOYMENT_IDENTITY`.**

## Post-release functional status correction

After the RC12 engineering close-out, project-owner testing on 2026-08-11 identified blocking product gaps that prior component/presence tests did not establish. RC13 repaired and re-proved those paths through PRs #151–#155.

PR #155 merged as `d6f83557ab18d26f82ad6289b1b95f728346631d` after exact head `56805ec4ead5a14e9a2f776f84df42eb772302a4` completed the full returned workflow matrix successfully. PR #156 then reconciled the repository status after that exact-head acceptance.

## RC13 owner acceptance — 2026-08-12

The accountable project owner explicitly accepted the repaired canonical product with:

`RC13 owner retest akkoord`

No unprovided test-environment metadata is inferred. RC13 is `PASS` and issue #150 is closed.

## Release objective

`16.0.0rc12` established the consolidated DTMO engineering baseline: one canonical console, governed source operations, threat intelligence investigation, native analytics, governed Administration/RBAC, authenticated operational components and repository-controlled CI/security/recovery/performance/observability acceptance.

RC13 established and accepted the complete usable operator journey in addition to component-level evidence.

## Phase 8 boundary

Phase 8 may now begin external validation, but it is not accepted.

The first gate is **Phase 8.1 — external deployment identity**. `docs/staging/PHASE8_DEPLOYMENT_IDENTITY_RECORD.md` must establish one approved production-equivalent staging environment and immutable deployment identity before later staging test evidence can be credited.

Current Phase 8.1 decision: `PENDING_EXTERNAL_DEPLOYMENT_IDENTITY` with `evidence_complete: false`.

Repository CI, Docker Compose, staging emulators and source-controlled readiness contracts do not substitute for a real staging deployment.

## Identity boundary

DTMO validates externally issued production bearer tokens. Managed principal/role state is auditable provisioning/assignment state and does not mint, forge or silently rewrite active bearer-token claims. Production role changes require identity-provider reconciliation or token reissue.

## Governance mapping boundary

`docs/governance/GOVERNANCE_MAPPING_REGISTRY.md` is authoritative. Normenkader IBP and MITRE ATT&CK remain `UNMAPPED`, CVSS remains `CONTEXT_ONLY`, and DTMO internal governance mappings remain repository-backed. No framework/control/technique equivalence may be inferred from semantic similarity, tags or free metadata.

## Security and governance

Server-side RBAC, least privilege, service-account separation, administrator safety, separate human review/share approval, provenance, privacy, auditability, no inferred mappings and no publication authority from source execution, analytics, Administration, Governance, CI or staging access remain authoritative.

## Release boundary

DTMO is **not production ready** until Phase 8 staging acceptance, Phase 9 external assurance and Phase 10 production go/no-go are complete.