# DTMO 16.0.0rc12

Status: `PASS` within the repository-controlled engineering boundary. **Current functional product acceptance is RC13 `REOPENED / BLOCKED_INTERNAL`; Phase 8 is `PAUSED_PENDING_RC13_REPAIR_AND_OWNER_RETEST`.**

## Functional status history

After the RC12 engineering close-out, project-owner testing on 2026-08-11 identified blocking product gaps that prior component/presence tests did not establish. RC13 repaired and re-proved those paths through PRs #151–#155. PR #156 reconciled the post-CI state.

On 2026-08-12 the accountable project owner explicitly accepted that repaired product with `RC13 owner retest akkoord`. PR #157 then opened the fail-closed Phase 8 deployment-identity gate.

A **subsequent project-owner functional retest on 2026-08-12** found additional blocking canonical-console defects. Newer owner-observed evidence therefore reopens RC13 for the current release decision while preserving the earlier acceptance as historical evidence.

## Reopened blockers

The newer owner findings cover:

- unreliable Overview `Alles vernieuwen` behavior;
- misleading `Data bijgewerkt` when no canonical intelligence exists;
- buttons not reliably functional under Chrome;
- an unnecessary release/version badge in navigation;
- unclear Administration composition;
- misleading graph rendering when datasets are empty.

Issue #150 is reopened. Phase 8 and issue #158 are paused.

## Current repair

The bounded repair adds truthful refresh lifecycle state, explicit zero-data graph empty states, explicit button semantics, Google Chrome-channel regression coverage with zero page/console errors, a simplified Administration layout and removal of the navigation version badge.

Successful CI will remain repository-controlled evidence. The accountable project owner must retest the merged local product before RC13 can return to `PASS`.

## Release objective

`16.0.0rc12` remains the consolidated DTMO engineering baseline: canonical console, governed source operations, threat intelligence investigation, native analytics, governed Administration/RBAC, authenticated operational components and repository-controlled CI/security/recovery/performance/observability acceptance.

The reopened RC13 decision means this engineering baseline is **not yet accepted as a current production candidate product experience**.

## Phase 8 boundary

PR #157 and `docs/staging/PHASE8_DEPLOYMENT_IDENTITY_RECORD.md` remain valid historical/preparatory evidence. No external staging evidence may advance while RC13 is reopened.

## Identity boundary

DTMO validates externally issued production bearer tokens. Managed principal/role state does not mint, forge or silently rewrite active bearer-token claims. Production role changes require identity-provider reconciliation or token reissue.

## Governance mapping boundary

`docs/governance/GOVERNANCE_MAPPING_REGISTRY.md` is authoritative. Normenkader IBP and MITRE ATT&CK remain `UNMAPPED`, CVSS remains `CONTEXT_ONLY`, and DTMO internal governance mappings remain repository-backed. No framework/control/technique equivalence may be inferred.

## Security and governance

Server-side RBAC, least privilege, service-account separation, administrator safety, separate human review/share approval, provenance, privacy, auditability, no inferred mappings and no publication authority from source execution, analytics, Administration, Governance, CI or staging access remain authoritative.

## Release boundary

DTMO is **not production ready** until reopened RC13 acceptance, Phase 8 staging acceptance, Phase 9 external assurance and Phase 10 production go/no-go are complete.
