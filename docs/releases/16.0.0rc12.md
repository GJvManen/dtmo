# DTMO 16.0.0rc12

Status: `PASS` within the repository-controlled RC12 claim boundary.

## Purpose

16.0.0rc12 completes the repository-controlled product consolidation requested before Phase 8 real staging acceptance: one canonical DTMO console, executable source operations for the current operational catalog, integrated administration, and production-grade graphical operational/intelligence dashboards without weakening existing governance boundaries.

## Accepted RC12 slices

- **RC12.1 / PR #142** — unify source administration and operations in the canonical console.
- **RC12.2 / PR #143** — adopt self-hosted Grafana for operational dashboarding.
- **RC12.3 / PR #144** — add dedicated least-privilege Grafana intelligence reporting access.
- **RC12.4 / PR #145** — embed DTMO Operations and Intelligence dashboards in the unified console.
- **RC12.5a / PR #146** — add hardened same-origin gateway support for `/grafana/`.
- **RC12.5b / PR #147** — switch browser-facing Grafana embeds from direct `:3000` access to relative `/grafana/...` paths.
- **RC12.6 / PR #148** — reconcile the authoritative README, project status, roadmap, source matrix, release notes and completion gate with the accepted RC11/RC12 implementation.

PR #147 exact head `339207dd5ad038727da34e0a0058c74076847eea` completed the returned exact-head workflow set successfully and merged as `6e74c5e45b6683e1fceba3ff14f554e36815b95f`.

PR #148 exact head `17c914af8a579c813b82849bab773b4449e8f178` completed the complete returned exact-head workflow set successfully and merged with expected-head protection as `8e614abc0277025957cab433c0e824c25dbb7eeb`. Issue #125 closed as `completed`.

## Source framework prerequisite

RC11 completed the current operational vendor onboarding set through the unified `SourceAdapterRegistry`. The authoritative source status is maintained in `docs/qa/SOURCE_CONNECTION_MATRIX.md`. Research-reference entries remain deliberately non-executable where appropriate.

## Security and governance boundaries

- server-side RBAC remains authoritative;
- source operations and dashboards do not grant review or publication authority;
- human review and separate external share approval remain distinct decisions;
- Grafana anonymous access remains disabled;
- Grafana intelligence reporting uses a dedicated least-privilege role and explicit reporting views rather than the DTMO application database identity;
- credential values are not stored in the source catalog or registry;
- provenance, privacy, auditability and fail-closed execution requirements remain in force;
- native accessible chart/table fallbacks remain available.

## User-facing architecture

The intended product entry point is `/` with `/ui/console` as an alias. Source catalog/operations, administration, intelligence investigation and graphical analytics are integrated in this shell. Legacy `/ui/*` routes may remain for compatibility but are not separate intended product shells.

Grafana browser integration is served through the managed same-origin `/grafana/` path.

## Release boundary

This repository-controlled `PASS` does **not** establish production readiness. Phase 6 genuine assistive-technology evidence, Phase 8 real staging deployment parity, Phase 9 external assurance and Phase 10 final go/no-go remain incomplete.

## Exactly one next priority

Obtain the approved real Phase 8 staging deployment-parity evidence package tied to one immutable release/deployment identity.
