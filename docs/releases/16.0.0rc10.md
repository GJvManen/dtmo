# DTMO 16.0.0rc10

Status: `CI_VALIDATION_PENDING` for RC10.4

16.0.0rc10 is the staged professional workspace programme.

## Accepted

- RC10.1 — unified Operations Workspace shell.
- RC10.2 — unified graphical operational dashboards.
- RC10.3 — Threat Intelligence Workspace; PR #118 merged as `1377899e7096c01362ab803c502c1d40812ef581` after complete exact-head workflow success.

## RC10.4 — current

- new `/ui/source-center` operational source workspace;
- GET-only `/api/v1/source-center/status` protected by existing `MANAGE_CONNECTORS` plus human-admin boundary;
- integrates registered source identity, enabled state, configured interval, reliability, runtime health, latest success/failure, consecutive failures/isolation and bounded endpoint provenance;
- excludes secret references, raw quarantine evidence and request/response bodies;
- delegates source mutation and manual execution to the existing accepted `/ui/admin-sources` control plane;
- grants no intelligence review or external share-approval authority;
- regression tests and dedicated RC10.4 QA gate added.

## Governance

Server-side RBAC remains authoritative. Source Center is an operational projection, not a new mutation or publication authority surface. Human review and separate external share approval remain distinct governed decisions.

## Acceptance

RC10.4 merges only after every registered workflow succeeds on one final exact PR head. External staging, genuine assistive-technology testing, penetration testing and external assurance gates remain unchanged.
