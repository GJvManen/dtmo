# DTMO 16.0.0rc10

Status: `CI_VALIDATION_PENDING` for RC10.3

16.0.0rc10 is the staged professional workspace programme.

## RC10.1 — accepted

Unified Operations Workspace shell, consolidated navigation, command palette, notification drawer, workspace tabs, responsive layout and reduced-motion support. PR #116 exact head `d41d9e60a4a67ddb30345eecee4042d1c19a6cf5` completed all registered workflows and merged as `b000ef2275d52ff098d2d2bd8df76136cea3b051`.

## RC10.2 — accepted

GET-only `/api/v1/operations/summary` backed by the existing Prometheus client registry; bounded aggregate request/latency/in-flight, alert, queue, trace and connector telemetry; live accessible dashboard widgets in `/ui/operations`; sensitive high-cardinality/request dimensions excluded. PR #117 exact head `d4e35a5fa0c463438299d6cdd3638de162a69026` completed every registered workflow and merged as `db9e72d871fb1c4d536912419ffbb4d68ad680c2`.

## RC10.3 — current

- new `/ui/intelligence-workspace` investigation workspace;
- reuse of the accepted RBAC-protected `/api/v1/intelligence/search` path;
- GET-only `/api/v1/intelligence/{item_id}/workspace` canonical detail projection;
- stored confidence, education relevance, review/share state and provenance in one investigation view;
- explicit CVE identifiers extracted only from stored canonical text/tags;
- known-exploited context asserted only for stored `cisa-kev` records;
- vendor/product shown only when explicitly stored;
- safe metadata allowlist; raw storage/request/credential metadata excluded;
- no review, share-approval, source-management, connector-run, admin or token-revocation mutation path;
- regression tests and dedicated RC10.3 QA gate.

## Governance

Server-side RBAC remains authoritative. Search/investigation does not grant review or publication authority. Human review and independent external share approval remain separate decisions. Missing enrichment is displayed as missing and is never invented by the UI.

## Acceptance

RC10.3 merges only after every registered workflow succeeds on one final exact PR head. External staging, genuine assistive-technology testing, penetration testing and external assurance gates remain unchanged.
