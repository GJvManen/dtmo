# DTMO 16.0.0rc10

Status: `CI_VALIDATION_PENDING`

16.0.0rc10 begins the RC10 professional workspace programme with one bounded change: a unified Operations Workspace shell at `/ui/operations`.

## Added

- consolidated operations sidebar and role-workspace navigation;
- command-center header and breadcrumbs;
- Ctrl/Cmd+K command palette;
- operational notification drawer;
- Overview, Intelligence, Connectors and Governance workspace tabs;
- read-only runtime and connector KPI cards backed by `/health` and `/connectors`;
- responsive layout and reduced-motion support;
- regression tests and a dedicated operations-workspace QA gate.

## Governance

The new shell introduces no new privileged write path. Existing RBAC, human review, independent share approval, audit and source-management controls remain authoritative. Placeholder graphical elements are explicitly labelled until real metrics are bound in a later bounded run.

## Acceptance

Merge only after every registered workflow succeeds on one final exact PR head. External staging and assurance gates remain unchanged.
