# DTMO 16.0.0rc10

Status: `CI_VALIDATION_PENDING`

16.0.0rc10 is the staged professional workspace programme. RC10.1 established the unified Operations Workspace shell; RC10.2 binds that shell to real operational telemetry.

## RC10.1 — accepted

- consolidated operations sidebar and role-workspace navigation;
- command-center header and breadcrumbs;
- Ctrl/Cmd+K command palette;
- operational notification drawer;
- Overview, Intelligence, Connectors and Governance workspace tabs;
- responsive layout and reduced-motion support.

PR #116 exact head `d41d9e60a4a67ddb30345eecee4042d1c19a6cf5` completed all registered workflows and merged as `b000ef2275d52ff098d2d2bd8df76136cea3b051`.

## RC10.2 — current

- GET-only `/api/v1/operations/summary` backed by the existing Prometheus client registry;
- bounded aggregate request volume, average latency and in-flight metrics;
- active API-error, connector, storage-integrity and search-health alert state;
- queue backlog utilization, trace-context totals and connector-run totals;
- real accessible graphical snapshot bars in `/ui/operations`;
- removal of the synthetic RC10.1 placeholder chart;
- explicit exclusion of sensitive request dimensions and credential-bearing data;
- regression tests and dedicated RC10.2 QA gate.

## Governance

The dashboard is read-only observability. Existing RBAC, human review, independent external-share approval, audit, source-management and CISO controls remain authoritative. Operational metrics do not grant intelligence-review or publication authority.

## Acceptance

RC10.2 merges only after every registered workflow succeeds on one final exact PR head. External staging, genuine assistive-technology testing, penetration testing and external assurance gates remain unchanged.
