[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "dtmo"
version = "16.0.0rc10"
description = "Education-focused cyber threat intelligence platform"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116,<1",
  "uvicorn[standard]>=0.35,<1",
  "pydantic-settings>=2.10,<3",
  "httpx>=0.28,<1",
  "sqlalchemy>=2.0,<3",
  "psycopg[binary]>=3.2,<4",
  "alembic>=1.16,<2",
  "redis>=6,<7",
  "minio>=7.2,<8",
  "apscheduler>=3.11,<4",
  "prometheus-client>=0.22,<1",
  "structlog>=25,<26",
  "PyJWT[crypto]>=2.10,<3",
]

[project.optional-dependencies]
test = [
  "pytest>=9.0.3,<10",
  "pytest-asyncio>=1.1,<2",
  "pytest-cov>=6,<7",
  "ruff>=0.12,<1",
  "mypy>=1.17,<2",
  "PyYAML>=6.0,<7",
  "pip-audit>=2.9,<3",
  "playwright>=1.54,<2",
]

[tool.hatch.build.targets.wheel]
packages = ["backend/dtmo"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests"]
addopts = "-q --disable-warnings --maxfail=1"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["backend/dtmo"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_also = [
  "if TYPE_CHECKING:",
  "raise NotImplementedError",
  '\.\.\.$',
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "B", "S"]
ignore = ["S101", "E501"]

[tool.ruff.lint.per-file-ignores]
"backend/tests/*.py" = ["S106"]
"backend/tests/test_ci_workflow_contract.py" = ["S506"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["dtmo"]

[[tool.mypy.overrides]]
module = ["apscheduler.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["jwt.*"]
ignore_missing_imports = true
